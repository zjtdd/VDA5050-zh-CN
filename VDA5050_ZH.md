![标志](./assets/logo.png)

# 移动机器人与车队控制之间的通信接口

## VDA 5050

## 版本 3.0.0

![车队控制系统与移动机器人](./assets/csagv.png)

# 免责声明
以下说明旨在为实现移动机器人与车队管理系统之间通信的接口提供指导。这些说明供所有用户自由获取，且不具约束力。任何选择采用本指南的一方，均有责任确保其在每个具体场景中得到正确、适当的应用。
用户必须考虑应用本指南时适用的现有技术水平。采用这些建议并不免除任何一方对其自身行为的责任。本声明无意穷尽所有情形，也不构成对现行法律的权威解释，亦不能替代对相关政策、法律或法规的审查与遵守。
此外，还必须考虑各产品的具体特性及其各种潜在应用。所有用户均自行承担风险。VDA、VDMA 以及任何参与制定或应用这些建议的个人均不承担责任。
如果您发现这些建议在应用中存在任何不准确之处或可能引发误解的风险，请立即通知 VDA，以便进行必要的修正。

**出版商**
德国汽车工业协会（VDA）
Behrenstraße 35, 10117 Berlin,
德国
www.vda.de

**版权**
德国汽车工业协会（VDA）
仅允许在注明来源的情况下复制或以其他形式再现。

版本 3.0.0


## 目录
[0 前言](#0-前言)<br>
[1 引言](#1-引言)<br>
[2 范围](#2-范围)<br>
[3 定义](#3-定义)<br>
  [3.1 移动机器人](#31-移动机器人)<br>
  [3.2 移动](#32-移动)<br>
  [3.3 行驶](#33-行驶)<br>
  [3.4 自动行驶](#34-自动行驶)<br>
  [3.5 手动行驶](#35-手动行驶)<br>
  [3.6 线导引移动机器人](#36-线导引移动机器人)<br>
  [3.7 自由导航式移动机器人](#37-自由导航式移动机器人)<br>
[4 传输协议](#4-传输协议)<br>
  [4.1 连接处理、安全与 QoS](#41-连接处理安全与-qos)<br>
  [4.2 主题层级](#42-主题层级)<br>
  [4.3 通信主题](#43-通信主题)<br>
[5 通信流程与内容](#5-通信流程与内容)<br>
  [5.1 概述](#51-概述)<br>
  [5.2 实施阶段](#52-实施阶段)<br>
  [5.3 车队控制的功能](#53-车队控制的功能)<br>
  [5.4 移动机器人的功能](#54-移动机器人的功能)<br>
[6 协议规范](#6-协议规范)<br>
  [6.1 订单](#61-订单)<br>
    [6.1.1 概念与逻辑](#611-概念与逻辑)<br>
    [6.1.2 订单与订单更新](#612-订单与订单更新)<br>
    [6.1.3 订单取消](#613-订单取消)<br>
    [6.1.4 订单拒绝](#614-订单拒绝)<br>
    [6.1.5 走廊](#615-走廊)<br>
  [6.2 动作](#62-动作)<br>
    [6.2.1 即时动作](#621-即时动作)<br>
    [6.2.2 动作阻塞类型与顺序](#622-动作阻塞类型与顺序)<br>
    [6.2.3 预定义动作](#623-预定义动作)<br>
  [6.3 地图](#63-地图)<br>
    [6.3.1 地图分发](#631-地图分发)<br>
    [6.3.2 移动机器人状态中的地图](#632-移动机器人状态中的地图)<br>
    [6.3.3 地图下载](#633-地图下载)<br>
    [6.3.4 启用已下载的地图](#634-启用已下载的地图)<br>
    [6.3.5 删除移动机器人上的地图](#635-删除移动机器人上的地图)<br>
  [6.4 区域](#64-区域)<br>
    [6.4.1 区域类型](#641-区域类型)<br>
    [6.4.2 区域集传输](#642-区域集传输)<br>
    [6.4.3 交互式区域通信](#643-交互式区域通信)<br>
    [6.4.4 区域之间的交互](#644-区域之间的交互)<br>
    [6.4.5 区域内的错误处理](#645-区域内的错误处理)<br>
  [6.5 连接](#65-连接)<br>
  [6.6 状态](#66-状态)<br>
    [6.6.1 概念与逻辑](#661-概念与逻辑)<br>
    [6.6.2 节点与边的遍历](#662-节点与边的遍历)<br>
    [6.6.3 基础区请求](#663-基础区请求)<br>
    [6.6.4 信息](#664-信息)<br>
    [6.6.5 错误](#665-错误)<br>
    [6.6.6 操作模式](#666-操作模式)<br>
    [6.6.7 清除移动机器人上的订单](#667-清除移动机器人上的订单)<br>
    [6.6.8 移动机器人的空闲状态](#668-移动机器人的空闲状态)<br>
    [6.6.9 动作状态](#669-动作状态)<br>
    [6.6.10 走廊使用请求](#6610-走廊使用请求)<br>
  [6.7 可视化](#67-可视化)<br>
  [6.8 自由导航式移动机器人规划路径的共享](#68-自由导航式移动机器人规划路径的共享)<br>
  [6.9 请求/响应机制](#69-请求响应机制)<br>
  [6.10 信息表](#610-信息表)<br>
[7 消息规范](#7-消息规范)<br>
  [7.1 表格符号与格式含义](#71-表格符号与格式含义)<br>
    [7.1.1 可选字段](#711-可选字段)<br>
    [7.1.2 允许的字符与字段长度](#712-允许的字符与字段长度)<br>
    [7.1.3 字段、主题与枚举的表示法](#713-字段主题与枚举的表示法)<br>
    [7.1.4 JSON 数据类型](#714-json-数据类型)<br>
  [7.2 协议头](#72-协议头)<br>
  [7.3 订单消息的实现](#73-订单消息的实现)<br>
    [7.3.1 动作参数格式](#731-动作参数格式)<br>
  [7.4 `instantActions` 消息的实现](#74-instantactions-消息的实现)<br>
  [7.5 `responses` 消息的实现](#75-responses-消息的实现)<br>
  [7.6 `zoneSet` 消息的实现](#76-zoneset-消息的实现)<br>
  [7.7 `connection` 消息的实现](#77-connection-消息的实现)<br>
  [7.8 `state` 消息的实现](#78-state-消息的实现)<br>
  [7.9 `visualization` 消息的实现](#79-visualization-消息的实现)<br>
  [7.10 `factsheet` 消息的实现](#710-factsheet-消息的实现)<br>


# 0 前言

该接口规范由德国汽车工业协会（Verband der Automobilindustrie e. V.，VDA）与德国机械设备制造业联合会（VDMA e. V.，Mechanical Engineering Industry Association）联合制定。
VDA 代表德国汽车行业，包括整车制造商（OEM）以及一级和多级供应商，并在车辆架构、系统集成和安全关键型通信方面提供专业知识。
VDMA 代表欧洲机械与设备工程行业的企业，并在自动化技术、机械设备互操作性和生产系统标准化方面拥有广泛的专业知识。
双方通过合作确保接口规范反映当前工程要求、支持稳健且可扩展的系统集成，并在异构环境中实现一致的数据交换。联合开发过程注重统一通信模型、兼容现有工业标准以及跨领域接口的长期可维护性。这种合作确保最终规范能够可靠地应用于汽车、机械及跨行业场景，为高度互操作性、运行安全和面向未来的系统架构提供支持。
卡尔斯鲁厄理工学院（KIT）的物料搬运与物流研究所（IFL）隶属于机械工程系，致力于将科研、教学与工业应用相结合。其跨学科团队研究未来物流领域的挑战，包括物料流分析、自动化、机器人技术、数字化、人工智能、可持续发展和系统设计。
该研究所受 VDA 和 VDMA 委托监督 VDA 5050 的开发，并通过牵头开发、协助审查问题和管理官方 GitHub 仓库参与这一过程。


# 1 引言
本建议描述了中央车队控制与移动机器人之间交换信息所使用的通信接口。
本建议旨在支持移动机器人车队在集中式车队控制系统监管下的集成与高效运行。为此，本建议定义了标准化且供应商中立的通信接口，以确保车队控制系统与各台移动机器人之间的互操作性。
在此背景下，各国技术指南和法律框架可提供一般性参考，例如为自动化系统的规划、运行、安全或协调提供指导性信息。此外，各国标准和监管规定有助于在一致的总体框架下考虑技术流程和术语。
本建议采用语义化版本控制方案。主版本变更（x.0.0）通常包含不兼容变更，例如引入新的必填字段。次版本变更（3.x.0）通常引入新功能，例如新增可选的可视化参数。补丁版本变更（3.0.x）通常用于较小的修正，例如修正文档中的拼写错误。
欢迎利益相关者提出接口修改或增强建议。此类建议应通过 GitHub 仓库提交：<https://github.com/vda5050/vda5050>。


# 2 范围

本文档描述了车队控制系统与移动机器人之间标准化且供应商中立的通信接口，旨在为多台移动机器人在车队控制系统协调下运行的环境提供通用参考并支持互操作性。本规范的使用是自愿且不具约束力的，是否采用由相关利益相关者自行决定。

本规范的目标包括：

- 降低移动机器人接入车队控制系统时的复杂性。
- 实现不同制造商异构移动机器人车队在共享物理环境中的协调运行。
- 提供一套通用且与应用领域无关的接口定义，适用于导航原理、物理尺寸、载荷搬运或操纵能力以及自主程度各不相同的移动机器人。

本规范未涉及以下主题：

- 安全要求：本文件未定义功能安全、运行安全或系统安全要求，不得将其视为或用作安全标准。
- 交通管理逻辑：本文件不包含交通协调策略、算法或决策过程，例如路径规划、优先级管理、拥堵处理或死锁解决。
- 其他通信接口：除车队控制系统与移动机器人之间的通信接口外，其他接口均不在本文件范围内，例如与外围设备、基础设施组件或外部 IT 系统连接的接口。
- 项目协调与实施程序：本文件不涉及项目管理活动、集成方法、调试与投运流程、验证和验收程序以及类似的组织流程。
- 运行责任：本文件不划分运营方、系统集成方、车辆制造商或车队控制提供商在规划、运行、维护或安全方面的责任。
- 网络安全措施：本文件未规定安全通信或数据保护的机制、技术或流程。

# 3 定义
就本文件而言，适用以下术语和定义。未经标准化组织正式定义的术语在其他语境中可能有不同解释。

## 3.1 移动机器人
一种主要在运行环境中用于物料运输、通过自动化进行控制的无人驾驶系统，与其自主程度无关。[来源：ISO 3691-4]

## 3.2 移动
移动机器人或其任何组件的空间位置或朝向发生变化的状态，包括车轮、载荷搬运装置或机器人本体的运动。

## 3.3 行驶
移动机器人的平移和/或旋转速度为非零的工作状态。

## 3.4 自动行驶
移动机器人在无人干预下运行的行驶状态。

## 3.5 手动行驶
移动机器人由人员直接控制运行的行驶状态。

## 3.6 线导引移动机器人
遵循预定义轨迹的移动机器人。预定义轨迹可由车队控制作为订单的一部分发送，也可在移动机器人上显式定义，或隐式定义为节点之间的直接连接。

## 3.7 自由导航式移动机器人
自主规划轨迹的移动机器人。如果车队控制在订单中发送了轨迹，移动机器人应遵循该轨迹。


# 4 传输协议

通信预计通过无线网络进行，同时需要考虑连接中断和消息可能丢失的影响。

消息协议采用消息队列遥测传输（MQTT），并与 JSON 格式结合使用。
MQTT 3.1.1 是兼容性所需的最低版本。
MQTT 支持将消息分发到称为“主题”的子通道。
MQTT 网络中的参与方通过订阅这些主题接收与自身相关的信息。

JSON 格式支持通过增加参数来扩展协议，并支持依据 Schema 进行验证。

### 4.1 连接处理、安全与 QoS

MQTT 协议支持为客户端设置遗嘱消息。
如果客户端因任何原因意外断开连接，代理服务器会将该遗嘱消息发布给其他已订阅的客户端。
该功能的使用方式见 [6.5 连接](#65-连接)。

如果移动机器人与代理服务器断开连接，它仍会保留所有订单信息，并执行订单直至最后一个已释放节点。

为减少通信开销，`order`、`instantActions`、`state`、`factsheet`、`zoneSet`、`responses` 和 `visualization` 主题应使用 MQTT QoS 0（尽力而为）；`connection` 主题应使用 QoS 1（至少一次）。

协议安全性需要在代理服务器配置中予以考虑，但不在本指南的讨论范围内。


### 4.2 主题层级

由于云服务提供商对主题结构有强制要求，因此本规范未严格定义 MQTT 主题结构。
对于云端 MQTT 代理服务器，可能需要单独调整主题结构，但仍应大致遵循建议的结构。
以下章节定义的主题名称是强制性的。

对于本地代理服务器，建议采用以下 MQTT 主题层级：

**interfaceName/majorVersion/manufacturer/serialNumber/topic**

示例：
```
vda5050/v3/KIT/0001/order
```

MQTT 主题层级 | 数据类型 | 描述
--- | --- | ---
interfaceName | string | 所用接口的名称。
majorVersion | string | VDA 5050 建议的主版本号，以 `v` 为前缀。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人唯一序列号，由以下字符组成：<br>A-Z<br>a-z<br>0-9<br>_<br>.<br>:<br>-
topic | string | 主题（例如 `"order"` 或 `"state"`），参见 [4.3 通信主题](#43-通信主题)。

>表 1 - 建议的 MQTT 主题层级说明

由于 `/` 字符用于定义主题层级，因此不得在上述任何字段中使用该字符。
通配符 `+` 和 `#` 以及保留给代理服务器内部主题的字符 `$` 也不应使用。

### 4.3 通信主题

该协议使用以下主题在车队控制与移动机器人之间交换信息。

主题名称 | 发布方 | 订阅方 | 用途 | 实现要求 | Schema
--- | --- | --- | --- | --- | ---
`order` | 车队控制 | 移动机器人 | 传输订单 | 强制性 | `order.schema`
`instantActions` | 车队控制 | 移动机器人 | 传输需要立即执行的动作 | 强制性 | `instantActions.schema`
`state` | 移动机器人 | 车队控制 | 传输移动机器人状态 | 强制性 | `state.schema`
`visualization` | 移动机器人 | 可视化系统 | 高频传输位置和规划路径 | 可选 | `visualization.schema`
`connection` | 代理服务器 / 移动机器人 | 车队控制 | 表示移动机器人连接何时丢失。车队控制不得使用该主题检查移动机器人的健康状态；该主题用于在 MQTT 协议层检查连接状态 | 强制性 | `connection.schema`
`factsheet` | 移动机器人 | 车队控制 | 提供参数或供应商特定信息，以帮助在车队控制中配置移动机器人 | 强制性 | `factsheet.schema`
`zoneSet` | 车队控制 | 移动机器人 | 将区域集从车队控制传输到移动机器人 | 可选 | `zoneSet.schema`
`responses` | 车队控制 | 移动机器人 | 车队控制对移动机器人状态内请求的响应 | 可选 | `responses.schema`

>表 2 - 车队控制与移动机器人之间的通信主题


# 5 通信流程与内容

## 5.1 概述

无人驾驶运输系统（DTS）的运行至少涉及以下参与方：

- DTS 的运营方提供基本信息
- 车队控制负责组织和管理运行
- 移动机器人执行订单

图 1 描述了运行阶段的通信内容。
在实施或变更期间，需要手动配置移动机器人和车队控制。

![图 1 信息流结构](./assets/information_flow_VDA5050.png)
>图 1 - 信息流结构

## 5.2 实施阶段

在实施阶段，将搭建由车队控制和移动机器人组成的 DTS。
必要的框架条件由运营方定义；所需信息由运营方手动输入，或从其他系统导入并存储到车队控制中。
主要涉及以下内容：

- 路线定义：
  通过布局交换格式（LIF），可以将路线导入车队控制。LIF 用于无人驾驶移动机器人系统集成方与（第三方）车队控制系统之间交换布局信息（LIF – 布局交换格式，VDMA 2024-03）。
  运营方也可以在车队控制中手动创建路线。
  路线可以设为单向，也可以根据尺寸比例限制特定移动机器人组使用等。

- 路网配置：
  在路线上定义装卸站、电池充电站、外围设施（门、电梯、道闸）、等待位置、缓冲站等。

- 移动机器人配置：运营方存储移动机器人的物理属性，例如尺寸、可用的载荷承载位置等。
  移动机器人应通过 `factsheet` 主题传输这些信息，具体方式见本文档 [7.10 factsheet 消息的实现](#710-factsheet-消息的实现)。

上述路线和路网配置不属于本文档的范围。
它们是车队控制依据这些信息和待完成的运输需求实施订单控制及分配行驶路线的基础。
由此生成、需由移动机器人车队执行的订单通过 MQTT 传输给各台移动机器人。
移动机器人随后在订单执行的同时，持续向车队控制报告其状态，同样使用 MQTT。

## 5.3 车队控制的功能

车队控制系统至少执行以下功能：

- 向移动机器人分配订单
- 线导引移动机器人的路线计算与引导（考虑每台移动机器人的物理特性限制，如尺寸、机动性等）
- 阻塞（“死锁”）的检测与解决
- 能源管理：充电订单可以中断运输订单
- 交通控制：缓冲路线与等待位置
- 对环境进行（临时）变更，例如开放某些区域或更改最高速度
- 与门、闸门、电梯等外围系统通信
- 通信错误的检测与解决

## 5.4 移动机器人的功能

每台移动机器人应执行以下功能：

- 定位
- 执行相应路线（线导引或自由导航）
- 执行动作
- 持续传输自身状态


# 6 协议规范

以下部分将详细介绍通信协议。
该协议规定了车队控制与移动机器人之间的通信。

## 6.1 订单

`order` 是移动机器人接收订单的 MQTT 主题，订单中包含要求机器人移动或执行动作的指令。


### 6.1.1 概念与逻辑

运输订单的核心是定义待行驶路线的一段节点-边图。
移动机器人预计会穿越节点和边以完成订单。
所有连接节点和边的完整图由车队控制持有。它可能包含限制，例如允许哪台移动机器人穿越哪条边。
这些限制不会传达给移动机器人。
车队控制在订单中仅包含相关移动机器人允许遍历的边。

![图 2 车队控制中的图形表示及订单中传输的图表](./assets/graph_representation_transmission.png)
>图 2 - 车队控制中的图形表示及订单中传输的图表

节点和边作为两个列表传递订单消息。
这些列表中节点和边的顺序也决定了节点和边的遍历顺序。`sequenceId` 在节点和边之间共享，用于定义遍历序列。第一个节点的 `sequenceId` 为 0，第一条边的 `sequenceId` 为 1，第二个节点的 `sequenceId` 为 2，依此类推。`sequenceId` 为 n 的边连接 `sequenceId` 分别为 n-1 和 n+1 的节点。`sequenceId` 在一个订单内应连续。

对于有效的订单，至少应有一个节点，且边的数量应等于节点数减去一。

订单的第一个节点（`sequenceId` = 0）应可由移动机器人直接到达，并且应始终为已释放状态。
这意味着移动机器人要么已经站在节点上，要么移动机器人处于节点的偏离范围内。因此，第一个节点不应在 `nodeStates` 中报告。

节点和边都有一个布尔属性 `released`。
如果节点或边已释放，则预期移动机器人会通过该节点或边。
如果节点或边未释放，移动机器人不得遍历该节点或边。

只有当边的起点和终点都被释放时，边才能被释放。

在未释放的边之后，序列中不能跟随释放的节点或边。

释放节点和边的集合称为“基础区”。
未释放的节点和边集合称为“预测区”。

发送没有预测区的订单是有效的。

订单消息不一定描述完整的运输订单。
为了交通控制并适应资源有限的移动机器人，完整的运输订单（可能包含多个节点和边）可以被拆分为多个子订单，这些子订单通过它们的 `orderId` 和 `orderUpdateId` 连接。
订单更新的过程将在下一节中介绍。


### 6.1.2 订单与订单更新

为支持交通管理，车队控制可以将通过订单传达的路径分为两部分：

- *“基础区”*：这是移动机器人被允许行驶的既定路线。基础区路线的所有节点和边均已由车队控制向移动机器人释放。基础区的最后一个节点称为决策点。
- *“预测区”*：这是车队控制当前为移动机器人规划的、在决策点之后行驶的路线。预测区路线尚未由车队控制释放。

如果基础区没有新增节点和边，移动机器人应在决策点停下。为保证移动机器人连续、平稳行驶，如果交通状况允许，车队控制应在移动机器人到达决策点之前扩展基础区。

由于 MQTT 为异步协议，且通过无线网络传输不可靠，基础区无法更改。因此，车队控制应假设该基础区已被移动机器人执行。后面的部分描述了取消订单的程序，但由于上述通信限制，该程序也被认为不可靠。

车队控制可以通过向移动机器人发送包含更改节点和边的更新路线来改变预测区。改变预测区路线的过程如图 3 所示。

![图 3 扩展“预测区”行驶路线的过程](./assets/driving_route_horizon.png)
>图 3 - 扩展“预测区”行驶路线的过程

在图 3 中，车队控制在时间 `t = 0` 时首次发送初始订单。
图 4 展示了一个可能订单的伪代码。
为了便于阅读，这里省略了完整的 JSON 示例。

```
{
	orderId: "1234",
	orderUpdateId:0,
	nodes: [
	 	 f {released: true},
	 	 d {released: true},
	 	 g {released: true},
	 	 b {released: false},
	 	 h {released: false}
	],
	edges: [
		e1 {released: true},
		e3 {released: true},
		e8 {released: false},
		e9 {released: false}
	]
}
```
>图 4 - 订单的伪代码

在后续时间点，通过发送订单更新（见图 5 中的伪代码）来扩展订单。
注意 `orderUpdateId` 是递增的，订单更新的第一个节点对应于前一个订单消息的最后一个基础区节点，即拼接节点。前一基础区的其他节点和边不会被重新发送。

这确保移动机器人也能执行订单更新，即通过执行移动机器人已知的边，能够到达订单更新的第一个节点。

```
{
	orderId: "1234",
	orderUpdateId: 1,
	nodes: [
		g {released: true},
		b {released: true},
		h {released: true},
		i {released: false}
	],
	edges: [
		e8 {released: true},
		e9 {released: true},
		e10 {released: false}
	]
}
```
>图 5 - 订单更新的伪代码；注意 `orderUpdateId` 的变化

这也有助于处理订单更新丢失的情况（例如因无线网络不稳定而丢失）。
移动机器人可以随时检查最后已知的基础区节点是否与新订单更新的第一个节点拥有相同的 `nodeId`（和 `sequenceId`）。

另外注意，节点 `g` 是唯一再次发送的基础区节点。
由于基础区不可更改，节点 `f` 和 `d` 的重传不成立。

![图 6 常规更新流程 - 订单扩展](./assets/update_order_extension.png)
>图 6 - 常规更新流程 - 订单扩展

图 6 描述了订单应如何扩展。
它显示了移动机器人当前可用的信息。
`orderId` 保持不变，`orderUpdateId` 是递增的。

重要的是决策点（图 6 中的节点 `g`）的内容不能被更改。这意味着动作、偏差范围等都应重新发送（见图 7，`orderUpdateId` 为 1）。
为了通过订单更新释放移动机器人在其当前所在节点上执行的动作，车队控制应先重新发送一次该节点，并包含上一次订单更新中的全部元数据（包括可能已处于 'FINISHED' 或 'RUNNING' 状态的动作）；移动机器人不会再次执行这些动作。随后，车队控制应添加一个包含本次新释放动作的节点，以便通过此次订单更新执行这些动作。该节点可以与决策节点具有相同的 `nodeId`，也可以具有不同的 `nodeId` 但与决策节点位置相同。新节点的 `sequenceId` 始终为决策节点的 `sequenceId` 加 2。

![图 7 通过额外拼接节点更新订单](./assets/update_order_stitching_node.png)
>图 7 - 通过额外拼接节点更新订单（例如执行决策点的新动作）

预测区可以通过任何订单更新被修改或完全删除，或者基础区可能以不同于之前预测区的方式延长。

一旦分配 `sequenceId` 并释放节点，订单更新时节点不会改变（见图 6）。

图 8 描述了接受订单或订单更新的过程。

![图 8 订单或订单更新的接受过程](./assets/process_order_update.png)
>图 8 - 订单或订单更新的接受过程

1) **收到的订单有效吗？**：
所有格式和 JSON 数据类型都正确吗？

2) **收到的是新订单还是当前订单的更新？**：
收到订单的 `orderId` 是否不同于移动机器人当前持有订单的 `orderId`？

3) **移动机器人是否处于空闲状态且未等待更新？**：
移动机器人是否处于 [6.6.8 移动机器人的空闲状态](#668-移动机器人的空闲状态) 中定义的空闲状态，且未等待更新？由于状态中还包含订单预测区的节点、边及相应动作状态，移动机器人可能仍有预测区，因此仍在执行订单并等待更新。

4) **`orderUpdateId` 是否为 0？**：新订单的 `orderUpdateId` 是否为 0？

5) **新订单的起点是否足够接近当前位置？**：移动机器人是否已位于节点上，或处于该节点的偏差范围内（参见 [6.1.1 概念与逻辑](#611-概念与逻辑)）？

6) **收到的订单更新是否已过期？**：`orderUpdateId` 是否小于移动机器人当前持有的值？

7) **订单更新是否在 `cancelOrder` 之后收到？**：车队控制不得再发送已取消订单的更新，移动机器人也不得接受此类更新。

8) **移动机器人是否已持有该订单更新？**：`orderUpdateId` 是否等于移动机器人当前持有的值？

9) **收到的更新是否是当前仍在运行的订单的有效延续？**：根据订单更新章节，接收订单的第一个节点是否为当前决策点？移动机器人仍在移动或执行与之前订单更新中基础区相关的动作，或者仍有预测区，因此等待订单的继续。在这种情况下，只有当新基础区的第一个节点等于前一基础区的最后一个节点时，订单更新才被接受。

10) **收到的更新是否有效延续了之前完成的订单？**：根据订单更新章节，收到订单的第一个节点是否为当前决策点？移动机器人不再执行任何动作，也不再等待订单的继续（意味着它已完成所有相关动作的基础区，且没有预测区）。在这种情况下，只有当新基础区的第一个节点等于前一基础区的最后一个节点时，订单更新才被接受。

11) **填充/附加**新状态到 `actionStates`/`nodeStates`/`edgeStates`。

#### 6.1.2.1 完成订单

当移动机器人遍历订单的最后一个节点，并完成所有与订单相关的移动和动作后，它将处于空闲状态，并应准备好接收新订单（参见 [6.6.8 移动机器人的空闲状态](#668-移动机器人的空闲状态)）。

### 6.1.3 订单取消

车队控制可以通过 instantAction `cancelOrder` 取消当前的订单。

车队控制可以选择性地传递 `orderId`，以指明应取消哪个订单。
收到 instantAction `cancelOrder` 后，移动机器人应尽快尝试停止。
对于线导引移动机器人，这可能是下一个可行的节点。自由导航的移动机器人应尽快停下，而不仅仅是在下一个节点。

对于 `actionStates` 中已计划但尚未开始的动作，移动机器人应取消这些动作，并将各自的 `actionState` 报告为 'FAILED'。
对于 `actionStates` 中正在执行的动作，移动机器人应尝试取消，并在取消后将其报告为 'FAILED'。
对于无法取消的动作，其 `actionState` 在执行期间应保持为 'RUNNING'；执行结束后，成功时报告为 'FINISHED'，否则报告为 'FAILED'。
只要 `actionStates` 中仍有正在执行的动作，`cancelOrder` 动作就应保持 'RUNNING'，直到所有动作均已取消或结束。不允许取消的动作（`cancelAllowed = false`）应继续执行直至结束。
当移动机器人已停止移动，且 `actionStates` 中的所有动作均已停止后，`cancelOrder` 的动作状态应报告为 'FINISHED'。
随后，移动机器人应处于空闲状态，并准备好接收新订单。

`orderId` 和 `orderUpdateId` 都被保留了。

图 9 展示了不同移动机器人能力的预期行为。

![图 9 cancelOrder 后的预期行为](./assets/process_cancel_order.png)
>图 9 - `cancelOrder` 后的预期行为

#### 6.1.3.1 取消后收到新订单

订单取消后，移动机器人处于空闲状态，并应准备好接收新订单。车队控制不得再发送已取消订单的更新。如果移动机器人收到此类订单更新，应报告类型为 'ORDER_UPDATE_FOLLOWING_CANCEL'、错误级别为 'WARNING' 的错误。

对于只能定位在节点上的移动机器人，新的订单应从其当前站立的节点开始（另见图 4）。

如果有移动机器人可以在节点之间停车，车队控制可以决定如何启动下一单。
移动机器人应接受这两种方法。

有两个选择：

- 新订单的第一个节点是一个临时节点，位于移动机器人当前的位置。移动机器人随后应识别出该节点可直接到达，并接受该订单。
- 新订单的第一个节点是前一订单最后一个穿越的节点。该节点允许的偏差设置得足够大，以确保移动机器人处于该范围内。因此，移动机器人应立即将该节点视为已经过并接受该订单。

#### 6.1.3.2 移动机器人空闲时接收 `cancelOrder` 动作

如果移动机器人收到 `cancelOrder` 即时动作，但移动机器人当前处于空闲状态，或者动作中指定的 `orderId` 与移动机器人当前活动订单的 `orderId` 不符，则 `cancelOrder` 动作应报告为 'FAILED'。

移动机器人应报告 'NO_ORDER_TO_CANCEL' 类型的错误，且错误级别设置为 'WARNING'。`instantAction` 的 `actionId` 应作为 `errorReference` 传递。


### 6.1.4 订单拒绝

在以下几种情况下，订单应被拒绝。
这些情景如图 8 所示，并有如下描述。


#### 6.1.4.1 移动机器人收到格式错误的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应报告类型为 'VALIDATION_FAILURE'、错误级别为 'WARNING' 的错误。
3. 在移动机器人接受新订单之前，应持续报告该警告。

#### 6.1.4.2 移动机器人接收带有其无法使用的可选字段的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区的新订单
2. 移动机器人应报告类型为 'UNSUPPORTED_PARAMETER'、错误级别为 'CRITICAL' 的错误，并在 `errorReferences` 中提供错误字段的引用。
3. 在移动机器人接受新订单之前，应持续报告该错误。

#### 6.1.4.3 移动机器人接收订单时，其无法执行的动作

示例：

- 提升高度高于最大允许提升高度
- 在未安装升降机构或没有升降行程的情况下要求执行提升动作等

解决方案：

1. 移动机器人不得接管其内部缓冲区的新订单
2. 移动机器人应报告类型为 'INVALID_ORDER_ACTION'、错误级别为 'WARNING' 的错误，并在 `errorReferences` 中提供错误字段的引用。
3. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.4 移动机器人收到的订单 `orderId` 相同，但 `orderUpdateId` 低于当前 `orderUpdateId`

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应将前一订单保留在缓冲区中。
3. 移动机器人应报告类型为 'OUTDATED_ORDER_UPDATE'、级别为 'WARNING' 的错误。
4. 移动机器人应继续执行之前的订单。
5. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.5 移动机器人收到与当前 `orderUpdateId` 具有相同 `orderId` 和 `orderUpdateId` 的订单

示例：

- 车队控制重新发送订单，因为尚未收到包含相应 `orderUpdateId` 的状态消息。

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应将前一订单保留在缓冲区中。
3. 根据消息内容进行报告：
	- 如果新订单的内容与前一个订单相同，移动机器人应忽略新订单。
	- 如果新订单的内容不同，移动机器人应报告类型为 'SAME_ORDER_UPDATE_ID'、错误级别为 'WARNING' 的错误。
4. 移动机器人应继续执行之前的订单。
5. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.6 移动机器人收到 `orderId` 不同于活动订单 `orderId` 的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人会将前一订单保留在缓冲区中。
3. 移动机器人应报告类型为 'OTHER_ORDER_ACTIVE'、级别为 'WARNING' 的错误。
4. 移动机器人应继续执行之前的订单。
5. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.7 移动机器人接收起始节点超出范围的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应报告类型为 'START_NODE_OUT_OF_RANGE'、级别为 'WARNING' 的错误。
3. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.8 移动机器人接收到至少一个节点无法访问的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应报告类型为 'NO_ROUTE_TO_TARGET'、级别为 'WARNING' 的错误。
3. 在移动机器人接受新订单之前，应持续报告该警告。


#### 6.1.4.9 移动机器人在不允许新增订单的操作模式下接收订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应报告类型为 'MOBILE_ROBOT_NOT_AVAILABLE'、级别为 'WARNING' 的错误。
3. 应持续报告该警告，直到移动机器人进入允许接收新订单的操作模式。

#### 6.1.4.10 移动机器人接收包含引用未知 `mapId` 的节点的订单

解决方案：

1. 移动机器人不得接管其内部缓冲区内的新订单。
2. 移动机器人应报告类型为 'UNKNOWN_MAP_ID'、级别为 'WARNING' 的错误。
3. 在移动机器人接受新订单之前，应持续报告该警告。



### 6.1.5 走廊

可选的 `corridor` 边属性允许移动机器人偏离边轨迹以避开障碍物，并定义移动机器人可操作的边界。
要使用 `corridor` 属性，需要预先定义的轨迹，如果没有定义 `corridor` 属性，移动机器人会遵循该轨迹。这可以是车队控制所知的移动机器人上定义的轨迹，也可以是订单中发送的轨迹。使用 `corridor` 属性的移动机器人的行为仍然是线导引移动机器人的行为，只是允许它暂时偏离轨迹以避开障碍物。
注意，订单中传达的走廊默认已由车队控制授权给移动机器人使用。如果 `releaseRequired` 标志位设置为 `true`，移动机器人在使用走廊前应先向车队控制请求批准，如 [6.6.10 走廊使用请求](#6610-走廊使用请求)所述。

*备注：
订单中的边定义了两个节点之间的逻辑连接，但不一定表示移动机器人从起始节点行驶到终止节点时实际遵循的轨迹。
根据移动机器人类型，移动机器人在起点和终点节点之间的轨迹由车队控制通过轨迹边属性定义，或者作为预设轨迹分配给移动机器人。
根据移动机器人的内部状态，所选的轨迹可能会有所不同。*

![图 10 带有走廊属性的边](./assets/edges_with_corridors.png)
>图 10 - 带有 `corridor` 属性的边，定义了移动机器人允许偏离预定轨迹以避开障碍物的左右边界。左侧由运动学中心定义允许的偏差，右侧由移动机器人的轮廓（可能因载荷而扩展）定义允许的偏差。具体方式由 `corridorReferencePoint` 参数指定。
移动机器人允许独立导航（并偏离原始边轨迹）的区域由左边界和右边界定义。
可选的 `corridorReferencePoint` 字段指定移动机器人控制点或移动机器人轮廓应位于定义边界内。
边的边界应定义为移动机器人在经过节点时立即进入当前新边边界内。
如果移动机器人不偏离轨迹，车队控制不应将走廊边界设为零，而是不使用 `corridor` 属性。

移动机器人的运动控制软件应持续检查移动机器人是否位于规定边界内。
如果移动机器人超出走廊边界，应停止行驶，因为它已离开允许的导航空间，并报告类型为 'OUTSIDE_OF_CORRIDOR'、错误级别为 'CRITICAL' 的错误。
车队控制可以决定是否需要用户互动，或者移动机器人是否可以通过取消当前订单并发送带有走廊信息的新订单，从而让移动机器人再次移动。

*备注：允许移动机器人偏离轨迹，会增加移动机器人在驾驶时的占地面积。此情况应在初始操作时考虑，并在车队控制基于移动机器人的足迹做出交通控制决策时予以考虑。*
更多信息参见 [6.6.2 节点与边的遍历](#662-节点与边的遍历)。


## 6.2 动作

如果移动机器人支持驾驶以外的动作，可通过附加到节点或边的 `actions` 数组、单独的 `instantActions` 主题（参见 [6.2.1 即时动作](#621-即时动作)），或动作区域配置（参见 [6.4.1 区域类型](#641-区域类型)）向移动机器人发出这些动作指令。
在边上执行的动作只能在移动机器人位于该边时运行（参见 [6.6.2 节点与边的遍历](#662-节点与边的遍历)）。

由节点触发的动作可以持续所需的时间。这类动作应能自行结束（例如持续五秒的音频信号，或在拾取载荷后结束的 `pick` 动作），或者应成对定义（例如 `activateWarningLights` 和 `deactivateWarningLights`）。

### 6.2.1 即时动作

在某些情况下，必须向移动机器人发送需要立即执行的动作。
这可以通过向 `instantActions` 主题发布 `instantAction` 消息来实现。
这些动作不得与移动机器人当前订单的内容冲突（例如 `instantAction` 要求降下货叉，而订单要求升起货叉）。

即时动作可能相关的一些例子包括：

- 暂停移动机器人，但不更改当前订单
- 暂停后继续执行订单
- 激活信号（光学、音频等）

当移动机器人接收到 `instantAction` 时，应在移动机器人状态的 `instantActionStates` 数组中添加适当的 `actionState`。
`actionStatus` 应根据动作进展进行更新。
另见图 11，了解 `actionStatus` 的不同转换。
即时动作的 `blockingType` 始终为 'NONE'。

当移动机器人收到无法执行的 `instantAction` 时，应报告类型为 'INVALID_INSTANT_ACTION'、级别为 'WARNING' 的错误，并在 `errorReferences` 中关联该 `instantAction` 的 `actionId`。

### 6.2.2 动作阻塞类型与顺序

列表中多个动作的排列顺序定义了移动机器人应执行这些动作的顺序。

动作的并行执行由其各自的 `blockingType` 控制。
动作可以有四种不同的阻塞类型，详见表 3。

- | 支持并行执行 | 不允许并行执行
--- | --- | ---
允许自动驾驶 | NONE | SINGLE
不允许自动驾驶 | SOFT | HARD

>表 3 - 影响行驶和并行执行的动作阻塞类型定义

图 11 描述了移动机器人处理动作阻塞类型的流程。每当移动机器人到达需要执行新动作的位置（节点、边或动作区域）时，应按照动作在 `actions` 数组中的顺序将其加入队列，并持续按图 11 所示流程处理。队列中存在 `blockingType` 为 'SOFT' 或 'HARD' 的动作时，移动机器人应停止自动行驶。`blockingType` 为 'NONE' 或 'SOFT' 的动作可以组成并行执行组。执行 `blockingType` 为 'SINGLE' 或 'HARD' 的动作前，已收集的所有并行动作都应进入 'FINISHED' 或 'FAILED' 状态。当队列中不再存在 `blockingType` 为 'SOFT' 或 'HARD' 的动作时，移动机器人可以恢复自动行驶。进入 'FINISHED' 或 'FAILED' 状态的动作应从队列中移除。


![图 11 处理多个动作](./assets/handling_multiple_actions.png)
>图 11 - 处理多个动作

### 6.2.3 预定义动作

本节定义了一组预定义动作。如果移动机器人的能力与某个动作的描述相符，则应使用该预定义动作。
如果已定义的参数适用于该动作，则应使用这些参数。
如果成功执行动作还需要其他参数，可以定义额外参数。
每台移动机器人都应支持 `cancelOrder`、`startPause` 和 `stopPause` 动作。

如果某项能力无法映射为下文任一预定义动作，移动机器人制造商可以定义由车队控制使用的扩展动作。

#### 6.2.3.1 定义、参数、效果与范围

动作类型 | 反向动作 | 描述 | 幂等 | 参数 | 关联状态 | 即时 | 节点 | 边 | 区域
--- | --- | --- | --- | --- | --- | --- | --- | --- | ---
`startPause` | `stopPause` | 启动暂停模式。<br>需要关联状态，因为许多移动机器人可以通过硬件开关暂停。<br>不再允许自动行驶，无需到达下一个节点。可暂停的动作（`pauseAllowed = true`）应暂停，其他动作继续。执行 `stopPause` 后，订单将继续执行。 | 是 | - | `paused` | 是 | 否 | 否 | 否
`stopPause` | `startPause` | 关闭暂停模式。<br>移动和所有其他动作都会恢复（如有）。<br>需要关联状态，因为许多移动机器人可以通过硬件开关暂停。<br>`stopPause` 还可以重新启动由硬件按钮触发 `startPause` 而停止的移动机器人（如已配置）。 | 是 | - | `paused` | 是 | 否 | 否 | 否
`startHibernation` | `stopHibernation` | 启动休眠模式。在此模式下，移动机器人应保持与 MQTT 代理的连接，但无需继续发送状态消息。在停止发布状态消息并发布 'HIBERNATING' 连接状态之前，移动机器人应将该动作报告为 'FINISHED'。如果移动机器人有活动订单，则应将其清除。无需到达下一个节点。<br>处于 'HIBERNATING' 连接状态时，移动机器人不得移动。移动机器人只能接收并响应即时动作 `stopHibernation`，不得响应任何其他命令，例如订单或其他即时动作。<br>如果移动机器人的电池在此模式下达到临界低电量，移动机器人可以自主退出 'HIBERNATING' 状态以报告错误。如果设置了唤醒时间，移动机器人可以在指定时间自主退出 'HIBERNATING' 连接状态，并应在恢复正常运行之前发布相应的连接状态转换。参数 `wakeUpTime` 是时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。 | 是 | `wakeUpTime`（字符串，可选） | - | 是 | 否 | 否 | 否
`stopHibernation` | `startHibernation` | 结束休眠模式。要在移动机器人处于 'HIBERNATING' 状态时启动唤醒，控制设备（车载或外部）应订阅 `instantActions` 主题并保持与 MQTT 代理的连接。由于移动机器人的标准控制设备在休眠期间可能会部分关闭，因此可以由另一个 MQTT 客户端触发唤醒，该客户端不同于移动机器人通常使用的通信客户端。<br>成功后，移动机器人应发布 'ONLINE' 连接状态。 | 是 | - | - | 是 | 否 | 否 | 否
`shutdown` | - | 启动移动机器人的协调关机，并断开其与 MQTT 代理的连接。执行 `shutdown` 动作要求移动机器人处于空闲状态。由于连接已终止，无法通过 VDA 5050 协议自动重新启动移动机器人。<br>如果移动机器人处于休眠模式但需要关机，则应先通过 `stopHibernation` 退出休眠，然后再执行 `shutdown`。 | 是 | - | - | 是 | 否 | 否 | 否
`startCharging` | `stopCharging` | 启动充电过程。<br>充电可以在充电点（移动机器人停车时）或充电通道（行驶时）进行。<br>防止过充的防护是移动机器人的责任。 | 是 | - | `powerSupply.charging` | 是 | 是 | 否 | 否
`stopCharging` | `startCharging` | 停止充电过程。<br>充电过程也可能被移动机器人或充电站打断，例如电池已满时。 | 是 | - | `powerSupply.charging` | 是 | 是 | 否 | 否
`initializePosition` | - | 用给定参数重置（覆盖）移动机器人的姿态。 | 是 | `x`（float64）<br>`y`（float64）<br>`theta`（float64）<br>`mapId`（字符串）<br>`lastNodeId`（字符串） | `mobileRobotPosition.x`<br>`mobileRobotPosition.y`<br>`mobileRobotPosition.theta`<br>`mobileRobotPosition.mapId`<br>`lastNodeId`<br>`maps` | 是 | 是<br>（电梯） | 否 | 否
`enableMap` | - | 显式启用已下载的地图，使其可在订单中使用，而无需重新初始化移动机器人的位置。 | 是 | `mapId`（字符串）<br>`mapVersion`（字符串） | `maps` | 是 | 是 | 否 | 否
`downloadMap` | - | 请求移动机器人下载新地图。下载期间，该动作保持活动；下载失败时，移动机器人应在状态中报告错误。移动机器人验证下载成功、完成地图使用准备并在状态中登记该地图后，该动作结束。 | 是 | `mapId`（字符串）<br>`mapVersion`（字符串）<br>`mapDownloadLink`（字符串）<br>`mapHash`（字符串，可选） | `maps` | 是 | 否 | 否 | 否
`deleteMap` | - | 请求移动机器人从内部存储中删除指定地图。 | 是 | `mapId`（字符串）<br>`mapVersion`（字符串） | `maps` | 是 | 否 | 否 | 否
`downloadZoneSet` | - | 请求移动机器人下载区域集。下载期间，该动作保持活动；下载失败时，移动机器人应在状态中报告错误。移动机器人验证下载成功、完成区域集使用准备并在状态中登记该区域集后，该动作结束。 | 是 | `zoneSetId`（字符串）<br>`zoneSetDownloadLink`（字符串）<br>`zoneSetHash`（字符串，可选） | `zoneSets` | 是 | 否 | 否 | 否
`enableZoneSet` | - | 显式启用此前下载的区域集，以便在订单中使用。 | 是 | `zoneSetId`（字符串） | `zoneSets` | 是 | 是 | 否 | 否
`deleteZoneSet` | - | 请求移动机器人从内部存储中删除指定区域集。 | 是 | `zoneSetId`（字符串） | `zoneSets` | 是 | 否 | 否 | 否
`clearInstantActions` | - | 移除移动机器人状态中所有已完成或失败的即时动作。 | 是 | - | `instantActionStates` | 是 | 是 | 否 | 否
`clearZoneActions` | - | 移除移动机器人状态中所有已完成或失败的区域动作。 | 是 | - | `zoneActionStates` | 是 | 是 | 否 | 否
`stateRequest` | - | 请求移动机器人发送新的状态消息。 | 是 | - | - | 是 | 否 | 否 | 否
`logReport` | - | 请求移动机器人生成并存储日志报告。 | 是 | `reason`（字符串） | - | 是 | 否 | 否 | 否
`pick` | `drop`<br><br>（自动执行时） | 请求移动机器人拾取载荷。<br>配备多个载荷搬运装置的移动机器人可以并行执行多个拾取动作；在这种情况下，必须提供 `loadHandlingDevice` 参数（例如 `"LHD1"`）。<br>`stationType` 用于说明拾取动作的具体处理方式（例如地面位置、货架位置、无动力输送机或动力输送机）。<br>`loadType` 用于说明载荷单元，例如 `"EPAL"` 或 `"INDU"`，并可用于切换防护区域。<br>为了预先准备载荷搬运装置（例如根据 `height` 参数预先提升），可以提前在预测区中下发该动作。由于关联节点此时尚未释放，预先提升等准备操作不应在移动机器人状态中报告为 'RUNNING'。<br>在边上执行该动作时，移动机器人可以使用传感装置检测与节点相关的拾取位置。 | 否 | `loadHandlingDevice`（字符串，可选）<br>`stationType`（字符串，可选）<br>`stationName`（字符串，可选）<br>`loadType`（字符串，可选）<br>`loadId`（字符串，可选）<br>`height`（float64，可选）<br>定义载荷底部相对于地面的高度<br>`depth`（float64，可选，仅适用于叉车）<br>`side`（字符串，可选），例如 `"conveyor"` | `loads` | 否 | 是 | 是 | 否
`drop` | `pick`<br><br>（自动执行时） | 请求移动机器人放下载荷。<br>更多详细信息请参见 `pick` 动作。 | 否 | `loadHandlingDevice`（字符串，可选）<br>`stationType`（字符串，可选）<br>`stationName`（字符串，可选）<br>`loadType`（字符串，可选）<br>`loadId`（字符串，可选）<br>`height`（float64，可选）<br>`depth`（float64，可选）<br>… | `loads` | 否 | 是 | 是 | 否
`detectObject` | - | 移动机器人检测物体（例如货物、充电位或空闲停车位）。 | 是 | `objectType`（字符串，可选） | - | 否 | 是 | 是 | 是
`finePositioning` | - | 在节点上执行时，移动机器人应相对于目标进行精确定位，并允许偏离节点位置。<br>在边上执行时，移动机器人可在沿边行驶的同时与固定设备对齐。 | 是 | `stationType`（字符串，可选）<br>`stationName`（字符串，可选） | - | 否 | 是 | 是 | 是
`waitForTrigger` | - | 移动机器人应等待 `triggerType` 参数所指定类型的触发信号。`triggerType` 是字符串数组：如果触发信号来自车队控制，应使用预定义值 'FLEET_CONTROL'；如果来自移动机器人上的输入（例如按下按钮或手动装载），应使用 'LOCAL'。如果两个预定义值均不适用，可以定义自定义值。<br>车队控制负责处理超时，并应在必要时取消订单。 | 是 | `triggerType` [字符串]（数组） | - | 否 | 是 | 否 | 是
`trigger` | - | 车队控制系统通知移动机器人已释放 `waitForTrigger` 动作。通常，当车队控制系统从第三方系统收到信息，表明移动机器人等待的流程已完成时，就会发生这种情况。 | 是 | - | - | 是 | 否 | 否 | 否
`retry` | - | 移动机器人重试由 `actionId` 指定且当前处于 'RETRIABLE' 状态的动作。 | 是 | `actionId`（字符串） | - | 是 | 否 | 否 | 否
`skipRetry` | - | 移动机器人应跳过由 `actionId` 指定且当前处于 'RETRIABLE' 状态的动作，并将该动作置为 'FAILED'。 | 是 | `actionId`（字符串） | - | 是 | 否 | 否 | 否
`cancelOrder` | - | 请求移动机器人尽快停下；移动机器人可能立即停止，也可能在下一个节点停止。参见 [6.1.3 订单取消](#613-订单取消)。 | 是 | `orderId`（字符串，可选） | - | 是 | 否 | 否 | 否
`factsheetRequest` | - | 请求移动机器人发送信息表。 | 是 | - | - | 是 | 否 | 否 | 否
`updateCertificate` | - | 请求移动机器人下载并激活新的证书集。`service` 参数是可扩展枚举，其中预定义值 'MQTT' 用于 MQTT 连接。 | 是 | `service`（字符串）<br>`keyDownloadLink`（字符串）<br>`certificateDownloadLink`（字符串）<br>`certificateAuthorityDownloadLink`（字符串，可选） | - | 是 | 否 | 否 | 否

>表 4 - 预定义动作及其范围（即时、节点、边、区域）


#### 6.2.3.2 动作状态

动作类型 | 'INITIALIZING' | 'RUNNING' | 'PAUSED' | 'FINISHED' | 'FAILED' | 'RETRIABLE'
--- | --- | --- | --- | --- | --- | ---
`startPause` | - | 正在准备启用暂停模式。<br>如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 移动机器人未移动。<br>所有可暂停的动作均已暂停。<br>暂停模式已启用。<br>移动机器人报告 `paused`：`true`。 | 由于某种原因无法启用暂停模式（例如被硬件开关覆盖）。 | -
`stopPause` | - | 正在准备停用暂停模式。<br>如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 暂停模式已停用。<br>所有已暂停的动作均已恢复。<br>移动机器人报告 `paused`：`false`。 | 由于某种原因无法停用暂停模式（例如被硬件开关覆盖）。 | -
`startHibernation` | - | 正在准备启用休眠模式。如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 移动机器人未移动。如有活动订单，该订单已被清除。移动机器人不再发送状态消息。<br>休眠模式已启用。移动机器人报告连接状态 'HIBERNATING'。 | 无法发布 'HIBERNATING' 连接状态（例如被硬件开关覆盖）。 | -
`stopHibernation` | - | 正在准备停用休眠模式。如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 休眠模式已停用。<br>移动机器人报告 `connectionState` 为 'ONLINE'。 | 无法停用休眠模式（例如被硬件开关覆盖）。 | -
`shutdown` | - | 正在准备启用 'OFFLINE' 连接状态。如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 移动机器人未移动。移动机器人与代理之间的连接以协调方式终止。<br>移动机器人报告连接状态 'OFFLINE'。 | 由于某种原因无法执行关机（例如移动机器人未处于空闲状态，或被硬件开关覆盖）。 | -
`startCharging` | - | 正在启用充电过程（正在与充电器通信）。<br>如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 充电过程已启动。<br>移动机器人报告 `powerSupply.charging`：`true`。 | 由于某种原因无法启动充电过程（例如未与充电器对齐）。充电问题应伴随相应错误。 | 无法启动充电过程。移动机器人正在等待车队控制或操作员干预。
`stopCharging` | - | 正在停用充电过程（正在与充电器通信）。<br>如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 充电过程已停止。<br>移动机器人报告 `powerSupply.charging`：`false` | 由于某种原因无法停止充电过程（例如未与充电器对齐）。<br>充电问题应伴随相应错误。 | -
`initializePosition` | - | 正在初始化新位姿（置信度检查等）。<br>如果移动机器人支持即时状态转换，则可以省略此状态。 | - | 位姿已重置。<br>移动机器人报告：<br>`mobileRobotPosition.x = x`，<br>`mobileRobotPosition.y = y`，<br>`mobileRobotPosition.theta = theta`，<br>`mobileRobotPosition.mapId = mapId`，<br>`lastNodeId = lastNodeId` | 位姿无效或无法重置。<br>一般定位问题应伴随相应错误。 | -
`downloadMap` | 正在初始化与地图服务器的连接。 | 移动机器人正在下载地图。 | - | 下载已完成。移动机器人将该地图的 `mapId`、`mapVersion` 和对应的 `mapStatus` 写入状态，其中 `mapStatus` 设置为 'DISABLED'。 | 下载失败，移动机器人应在状态中反映错误（例如连接中断、地图服务器无法访问，或服务器上不存在指定的 `mapId`/`mapVersion`）。 | 下载失败或中断；移动机器人正在等待车队控制干预。
`enableMap` | - | 移动机器人正在启用由 `mapId` 和 `mapVersion` 指定的地图，并禁用具有相同 `mapId` 的其他地图版本。 | - | 地图已启用。移动机器人将指定地图的 `mapStatus` 更新为 'ENABLED'，并将相同 `mapId` 的其他版本更新为 'DISABLED'。 | 指定的 `mapId`/`mapVersion` 组合不存在。 | -
`deleteMap` | - | 移动机器人正在从内部存储中删除由 `mapId` 和 `mapVersion` 指定的地图。 | - | 地图已删除。移动机器人从状态中移除对应的 `mapId`/`mapVersion`。 | 无法删除地图，例如地图正在使用，或指定的 `mapId`/`mapVersion` 组合已被删除。 | -
`downloadZoneSet` | 初始化与区域集服务器的连接。 | 移动机器人正在下载区域集。 | - | 下载已完成。移动机器人通过在状态中设置相应的 `zoneSet` 对象并将其 `zoneSetStatus` 置为 'DISABLED' 来更新状态。 | 下载失败，并在移动机器人状态中更新（例如连接丢失、服务器无法访问、区域集不存在，或移动机器人上已存在具有相同 `zoneSetId` 的区域集）。 | 下载失败或中断。移动机器人正在等待车队控制干预。
`enableZoneSet` | - | 移动机器人启用具有所请求 `zoneSetId` 的区域集，并禁用同一 `mapId` 的所有其他区域集。 | - | 区域集已启用。移动机器人将所请求 `zoneSet` 的相应 `zoneSetStatus` 更新为 'ENABLED'，并将同一 `mapId` 的其他区域集更新为 'DISABLED'。 | 所请求的区域集不存在。 | -
`deleteZoneSet` | - | 移动机器人正在从内部存储中删除由 `zoneSetId` 指定的区域集。 | - | 区域集已删除。移动机器人从状态中移除对应的 `zoneSet` 对象。 | 无法删除区域集，例如该区域集正在使用，或指定的区域集已被删除。 | -
`clearInstantActions` | - |  | - | 已从即时动作数组中清除所有处于 'FINISHED' 或 'FAILED' 状态的 `instantAction`。 | - | -
`clearZoneActions` | - |  | - | 已从区域动作数组中清除所有处于 'FINISHED' 或 'FAILED' 状态的区域动作。 | - | -
`stateRequest` | - | - | - | 状态已传达。 | - | -
`logReport` | - | 报告正在生成中。<br>如果移动机器人支持即时生成，则可以省略该状态。 | - | 报告已被保存。<br>日志名称作为动作状态的一部分被报告。 | 报告无法存储（例如存储空间不足）。 | -
`pick` | 正在初始化取货流程，例如等待升降操作完成。 | 取货流程正在进行（移动机器人正驶入站点、载荷搬运装置忙碌、正在与站点通信等）。 | 取货流程已暂停，例如防护区域被侵入时。<br>侵入解除后，取货流程继续。 | 取货完成。<br>载荷已进入移动机器人，移动机器人报告新的载荷状态。 | 取货失败，例如站点意外为空。<br>取货失败应伴随相应错误。 | 取货失败但可重试。移动机器人正在等待车队控制或操作员干预。
`drop` | 正在初始化放货流程，例如等待升降操作完成。 | 放货流程正在进行（移动机器人正驶入站点、载荷搬运装置忙碌、正在与站点通信等）。 | 放货流程已暂停，例如防护区域被侵入时。<br>侵入解除后，放货流程继续。 | 放货完成。<br>载荷已离开移动机器人，移动机器人报告新的载荷状态。 | 放货失败，例如站点意外被占用。<br>放货失败应伴随相应错误。 | 放货失败但可重试。移动机器人正在等待车队控制或操作员干预。
`detectObject` | - | 物体检测正在进行。 | - | 已检测到物体。 | 未能检测到物体。 | 物体检测失败但可重试。移动机器人正在等待车队控制或操作员干预。
`finePositioning` | - | 移动机器人正在相对于目标进行精确定位。 | 精确定位流程已暂停，例如防护区域被侵入时。<br>侵入解除后，精确定位流程继续。 | 已到达相对于站点的目标位置。 | 未能到达相对于站点的目标位置。 | 精确定位失败但可重试。移动机器人正在等待车队控制或操作员干预。
`waitForTrigger` | - | 移动机器人正在等待触发信号。 | - | 已收到触发信号。 | 如果订单被取消，`waitForTrigger` 动作失败。 | -
`cancelOrder` | - | 移动机器人正在停车，或者继续行驶直至到达下一个节点。 | - | 移动机器人未移动。移动机器人已取消执行订单并处于空闲状态。 | 移动机器人没有活动订单。<br>之前的订单已被取消。<br>传入的 `orderId` 与当前活动的 `orderId` 不匹配。 | -
`factsheetRequest` | - | - | - | 信息表已传达。 | - | -
`updateCertificate` | - | 移动机器人正在下载并安装证书。 | - | 证书已下载、安装并激活。 | 证书下载或安装失败。 | -

>表 5 - 预定义动作的动作状态下的预期行为

#### 6.2.3.3 更新移动机器人证书

出于安全考虑，移动机器人通信（至少用于车队管理）应当受到保护。通常，与 MQTT 代理的通信通过 TLS 保护，这需要一个或多个根证书以及移动机器人专用的密钥对。参数 `service` 指定证书所用于的服务（例如 'MQTT'）。参数 `certificateAuthorityDownloadLink` 指定根证书的 URL。参数 `certificateDownloadLink` 和 `keyDownloadLink` 分别指定移动机器人专用公钥和私钥的 URL。

下载也应通过 TLS 进行保护，因为无法验证 `instantAction` 的发送者。同时建议在激活证书链之前对其进行验证。


## 6.3 地图

为了确保不同类型移动机器人之间的导航一致，位置总是以项目特定的坐标系为参考来指定（见图 12）。项目专用坐标系指的是为车队控制与移动机器人之间交互所定义的坐标系。
为了区分场地或地点的不同层级，使用唯一的 `mapId`。
地图坐标系应定义为 z 轴指向天空的右手坐标系。
因此，正旋转应理解为逆时针旋转。
移动机器人坐标系也被指定为右手坐标系（ISO 9787 4.1），其中 x 轴指向移动机器人的前方，z 轴向上（ISO 9787 5.5）。移动机器人参考点定义为移动机器人参考系中的（0, 0, 0），除非另有说明。

![图 12 坐标系和示例移动机器人的朝向](./assets/coordinate_system_vehicle_orientation.png)
>图 12 - 坐标系和示例移动机器人的朝向

x、y 和 z 坐标应以米为单位表示。
朝向角应以弧度为单位，且在 -Pi 和 +Pi 之间。


### 6.3.1 地图分发

为了实现自动地图分发并在必要时智能管理移动机器人的重启，车队控制可以管理移动机器人上的地图。

待分发的地图文件存储在移动机器人可以访问的专用地图服务器上。为了确保传输效率，每次传输应仅包含一个文件。如果需要多个地图或文件，应将它们捆绑或打包成一个文件。将地图从地图服务器传输到移动机器人的过程是一个拉取操作，由车队控制通过 `instantAction` 触发下载命令来启动。

每张地图通过地图标识符（字段 `mapId`）和地图版本（字段 `mapVersion`）的组合唯一标识。地图标识符描述移动机器人物理工作区中的特定区域，地图版本表示对先前版本的更新。在接受新订单之前，移动机器人应检查自身是否具有所请求订单中每个地图标识符对应的地图。如果可用地图列表中缺少相应的 `mapId`，移动机器人应报告类型为 'UNKNOWN_MAP_ID'、级别为 'WARNING' 的错误。车队控制负责确保启用了操作移动机器人所需的正确地图。

为了尽量缩短停机时间，并便于车队控制同步新地图的启用过程，地图应预先加载或缓存在移动机器人上。移动机器人上的地图状态反映在移动机器人的状态消息中。将地图传输到移动机器人和启用地图是两个不同的过程。要在移动机器人上启用预加载的地图，车队控制应发送即时动作。因此，移动机器人应禁用任何地图标识符相同但地图版本不同的其他地图。

车队控制也可以通过即时动作删除地图。

图 13 展示了地图分发过程。

![图 13 地图分发过程](./assets/map_distribution_process.png)
>图 13 - 车队控制、移动机器人和地图服务器之间下载、启用和删除地图所需的通信

### 6.3.2 移动机器人状态中的地图

状态中 `mobileRobotPosition` 的 `mapId` 字段表示当前启用的地图。

移动机器人上可用的地图信息以 `maps` 数组形式呈现，该数组是状态消息的一个组成部分。该数组中的每个条目都是一个 JSON 对象，包含必填字段 `mapId`、`mapVersion` 和 `mapStatus`，其中 `mapStatus` 的取值为 'ENABLED' 或 'DISABLED'。如有必要，移动机器人可以使用 'ENABLED' 地图。不得使用 'DISABLED' 地图。下载进程的状态由当前未完成的操作来表示。状态中也会报告错误。
请注意，可以同时启用多张 `mapId` 不同的地图。对于同一个 `mapId`，同一时间只能启用一个地图版本。如果 `maps` 数组为空，则移动机器人当前没有可用地图。


### 6.3.3 地图下载

地图下载应由车队控制发出的 `downloadMap` 即时动作触发。该动作应包含必填参数 `mapId` 和 `mapDownloadLink`；`mapDownloadLink` 指向地图服务器上存储地图且移动机器人可以访问的位置。

移动机器人在开始下载地图文件后立即将 `actionStatus` 设为 'RUNNING'。如果下载成功，`actionStatus` 将更新为 'FINISHED'。如果下载失败，状态将设置为 'FAILED'。下载成功完成后，地图将被添加到状态的 `maps` 数组中。地图在准备好启用前不得在状态中报告。

下载地图的过程不得修改、删除、启用或禁用移动机器人上的任何现有地图。
移动机器人应拒绝下载已在移动机器人上带有 `mapId` 和 `mapVersion` 的地图。应报告 'DUPLICATE_MAP' 型且级别 'WARNING' 的错误，且当前动作状态设为 'FAILED'。车队控制应先删除移动机器人上的地图，然后重新开始下载。


### 6.3.4 启用已下载的地图

在移动机器人上启用地图有两种方式：

1. **车队控制启用地图**：使用 `enableMap` 即时动作将移动机器人上的地图设置为 'ENABLED'。具有相同 `mapId` 但 `mapVersion` 不同的其他地图版本应设置为 'DISABLED'。
2. **手动在移动机器人上启用地图**：在某些情况下，可能需要直接在移动机器人上启用地图。结果应以移动机器人状态报告。

车队控制应确保在订单中 `nodePosition` 发送相应的 `mapId` 时，移动机器人激活的地图正确。
如果移动机器人需要在新地图上被设定为特定位置，则应使用 `initializePosition` 即时动作。


### 6.3.5 删除移动机器人上的地图

车队控制可以请求移动机器人删除特定地图。这应通过使用即时动作 `deleteMap` 来实现。当移动机器人内存耗尽时，应向车队控制报告，车队控制中心随后可以启动地图删除。移动机器人本身不得删除地图。
成功删除地图后，移动机器人应从状态消息中的 `maps` 数组中移除相应条目。

## 6.4 区域

区域用于为移动机器人工作区中的特定范围定义规则。区域既允许移动机器人在节点之间自由导航，也使车队控制能够管理交通。区域可用于局部禁止移动机器人进入某些范围，或将准入与条件关联起来（区域类型：'BLOCKED' 和 'RELEASE'）。区域还可用于强制移动机器人在区域内遵循特定行为（区域类型：'LINE_GUIDED'、'SPEED_LIMIT'、'COORDINATED_REPLANNING' 和 'ACTION'），通过奖励或惩罚某些范围来影响行驶行为（区域类型：'PRIORITY' 和 'PENALTY'），或规定行驶方向（区域类型：'DIRECTED' 和 'BIDIRECTED'）。以下小节将定义这些区域类型。

由区域重叠或区域属性与边属性组合引起的潜在订单冲突及其解决方法，见 [6.4.4 区域之间的交互](#644-区域之间的交互)。对于订单中已释放但受区域限制的节点（例如位于 'BLOCKED' 或 'RELEASE' 区域内的节点），机器人应按照区域规则行动，例如不得进入，或等待请求状态变为 'GRANTED'。
有些移动机器人完全无法处理区域，而另一些移动机器人可能只能处理部分区域类型，例如 'BLOCKED'。因此，所有移动机器人都应在信息表中 `typeSpecification` 下的 `supportedZones` 数组中添加相应的区域名称，以向车队控制报告其能够理解的区域类型。
此外，（虚拟）线导引移动机器人如果能够实现下文定义的相应区域类型逻辑，也可以选择支持基于区域的导航。
为保持系统一致性，区域集只应由车队控制更改和分发。

### 6.4.1 区域类型

区分两类区域：基于轮廓的区域和基于运动学中心的区域。这种区分基于移动机器人进出区域时的不同条件。

#### 6.4.1.1 基于轮廓的区域

对于基于轮廓的区域，移动机器人的轮廓（包括其载荷）决定何时进入和离开区域。轮廓的任何部分进入区域即视为进入区域；当移动机器人轮廓的所有部分均不在区域内时，即视为离开区域。

![图 14 基于轮廓进入区域的移动机器人（左），以及带有相应扩展包围框、正在离开区域的载货移动机器人（右）的示意图](./assets/contour_entry.png)
>图 14 - 基于轮廓进入区域的移动机器人（左），以及带有相应扩展包围框、正在离开区域的载货移动机器人（右）的示意图

定义了以下基于轮廓的区域：

| **区域类型** | **区域参数** | **数据类型** | **描述** |
| --- | --- | --- | --- |
| BLOCKED | 无 |  | 移动机器人不得进入该区域。如果移动机器人已进入或发现自身位于该区域内，应立即停止，并报告类型为 'BLOCKED_ZONE_VIOLATION'、级别为 'CRITICAL' 的错误。 |
| LINE_GUIDED | 无 |  | 该区域内不允许自由导航，移动机器人必须沿边的预定义轨迹行驶。只有当车队控制以节点-边图的形式明确指定路线时，移动机器人才可进入该区域。进入区域时，移动机器人应位于穿越该区域的边轨迹上。进入该区域的边以及位于区域内的边，都必须具有车队控制下发的轨迹或移动机器人上的预定义轨迹。车队控制可以下发走廊，允许移动机器人在走廊范围内偏离轨迹。 |
| RELEASE |  | - | 移动机器人只有在通过车队控制获得许可后才能进入该区域。 |
|  | *releaseLossBehavior* | string | 枚举 {'STOP', 'CONTINUE', 'EVACUATE'}<br>定义移动机器人已位于该区域内时，该区域的通行许可被撤销或过期后移动机器人的行为。如果未定义该参数，移动机器人应按照定义为 'STOP' 时的方式处理。<br>'STOP'：移动机器人停止行驶，并报告类型为 'RELEASE_LOST'、级别为 'CRITICAL' 的错误。<br>'EVACUATE'：移动机器人执行自身的撤离行为以离开该区域。移动机器人在状态中保留该请求，相应地将其 `requestStatus` 设置为 'REVOKED' 或 'EXPIRED'，并持续报告类型为 'RELEASE_LOSS_HANDLING'、级别为 'WARNING' 的错误，直至离开该区域。<br>'CONTINUE'：移动机器人继续沿当前路径行驶。移动机器人在状态中保留该请求，相应地将其 `requestStatus` 设置为 'REVOKED' 或 'EXPIRED'，并持续报告类型为 'RELEASE_LOSS_HANDLING'、级别为 'WARNING' 的错误，直至离开该区域或订单在该区域内结束。如果订单在该区域内结束，移动机器人应等待新订单。 |
| COORDINATED_REPLANNING | 没有 |  | 该区域内不允许自主重新规划。移动机器人只有在获得车队控制许可后才允许调整路径。 |
| SPEED_LIMIT |  |  | 移动机器人不得超过该区域内规定的最大速度。 |
|  | maximumSpeed | float64 | 移动机器人在该区域内的最大允许速度（m/s）。进入该区域时，应已满足该限速要求。 |
| ACTION |  |  | 移动机器人在进入、穿越或离开区域时应执行预定义动作。信息表定义了哪些动作可在何时执行。 |
|  | entryActions[action] | array | 进入区域时触发的动作。如果不需要操作，则为空数组。 |
|  | duringActions[action] | array | 穿越区域时需执行的动作。如果不需要操作，则为空数组。 |
|  | exitActions[action] | array | 离开区域时触发的动作。如果不需要操作，则为空数组。 |

>表 6 - 基于轮廓的区域类型及其参数

#### 6.4.1.2 基于运动学中心的区域

在基于运动学中心的区域中，移动机器人的运动学中心决定其进入和退出区域。当移动机器人的运动学中心位于某一区域时，移动机器人应遵循定义的行为。
'PRIORITY' 区和 'PENALTY' 区仅影响移动机器人的路径规划。
'DIRECTED' 区定义了该区内的首选行车方向。'BIDIRECTED' 区定义了行进方向及其反方向。其他方向应避免。`directedLimitation` 和 `bidirectedLimitation` 枚举规定了移动机器人偏离其行进方向的界限。行进方向是项目特定坐标系中的速度矢量。

![图 15 移动机器人基于其运动学中心进入区域（左）和载货移动机器人基于其运动学中心离开区域（右）的示意图](./assets/kinematic_center_entry.png)
>图 15 - 移动机器人基于其运动学中心进入区域（左）和载货移动机器人基于其运动学中心离开区域（右）

| **区域类型** | **区域参数** | **数据类型** | **描述** |
| --- | --- | --- | --- |
| PRIORITY |  |  | 与地图上条件相同但未设置该区域的工作空间相比，该区域会提高移动机器人规划路径穿越此处的优先级。 |
|  | priorityFactor | float64 | [0.0 ... 1.0]<br>该相对因子表示与未设置区域的工作空间相比，规划路径时对该区域的偏好程度。0.0 表示无额外偏好，相当于未设置区域；1.0 表示最高偏好。 |
| PENALTY |  |  | 与地图上条件相同但未设置该区域的工作空间相比，该区域会降低移动机器人规划路径穿越此处的优先级。 |
|  | penaltyFactor | float64 | [0.0 ... 1.0]<br>该相对因子表示与未设置区域的工作空间相比，规划路径时对该区域的避让程度。0.0 表示无额外惩罚，相当于未设置区域；1.0 表示最高惩罚，移动机器人只有在找不到其他可行路线时才会选择穿越该区域。 |
| DIRECTED |  |  | 移动机器人应沿特定行驶方向穿越该区域。 |
|  | direction | float64 | 区域内的首选行驶方向，单位为弧度。该方向是移动机器人速度矢量在项目特定坐标系中的角度。 |
|  | directedLimitation | string | 枚举 {'SOFT', 'RESTRICTED', 'STRICT'}<br>'SOFT'：移动机器人可以偏离规定的行驶方向，但应尽量避免偏离；'RESTRICTED'：移动机器人可以偏离规定的行驶方向，例如为了避开障碍物，但绝不得沿规定方向的反方向穿越；'STRICT'：移动机器人应在其技术能力允许的范围内尽可能精确地保持规定的行驶方向。 |
| BIDIRECTED |  |  | 在该区域内，移动机器人应仅沿规定的行驶方向及其正相反方向（+Pi）移动，不应沿其他方向穿越该区域。 |
|  | direction | float64 | 区域内的首选行驶方向，单位为弧度。该方向是移动机器人速度矢量在项目特定坐标系中的角度。 |
|  | bidirectedLimitation | string | 枚举 {'SOFT', 'RESTRICTED'}<br>'SOFT'：移动机器人可以偏离规定的两个行驶方向，但应尽量避免偏离；'RESTRICTED'：除避障外，移动机器人不得沿规定行驶方向以外的其他方向移动。 |

>表 7 - 基于运动学中心的区域类型及其参数

### 6.4.2 区域集传输

为保持系统一致性，区域集只应由车队控制更改和分发。分发区域集的首选方式是使用 `zoneSet` 主题。如果移动机器人支持区域，则应支持通过 `zoneSet` 主题进行更新。较大的区域集也可以通过 `downloadZoneSet` 即时动作共享，并遵循图 13 所示的地图分发方式。

`zoneSet` 是由 `zone` 对象组成的数组，并具有全局唯一标识符 `zoneSetId`。它与 `mapId` 所引用的单张地图关联。不得引用 `mapVersion`，因为同一区域集可能供一张地图的多个版本使用。通常，一张地图可以定义多个区域集，车队控制负责确保移动机器人上的每张地图都启用了正确的区域集。与地图相同，`zoneSetStatus` 指示移动机器人当前使用的区域集。对于移动机器人上的每个 `mapId`，同一时间只能有一个活动区域集。区域不得超出地图的空间边界。
具有唯一 `zoneSetId` 的区域集，其内容不得改变。如果需要更改区域集中的内容，则应使用新的 `zoneSetId` 引用该区域集。

新添加区域集的 `zoneSetStatus` 应始终设置为 'DISABLED'，并应在使用前通过 `enableZoneSet` 即时动作启用。

如果移动机器人通过 `zoneSet` 主题或 `downloadZoneSet` 即时动作接收到与现有区域集具有相同 `zoneSetId` 的新区域集，则不得用该区域集替换内部存储器中的区域集，并应在一段合理时间内报告类型为 'DUPLICATE_ZONE_SET'、级别为 'WARNING' 的错误，以便车队控制发现区域集更新失败。


### 6.4.3 交互式区域通信

对于 'RELEASE' 和 'COORDINATED_REPLANNING' 两类交互式区域，移动机器人通过状态消息中的 `zoneRequests` 字段发出请求，车队控制通过独立的 `responses` 主题响应。

进入交互式区域之前，移动机器人应发出请求。
即使订单包含区域内的已释放节点，也必须在进入交互式区域之前发出请求。
移动机器人会在进入区域前决定在何时提出请求。
如果未能及时收到响应，移动机器人不得进入该区域。

只能针对已启用区域集中的区域发出请求。也可以针对属于移动机器人当前所在地图之外其他地图的区域集发出区域请求。

通过 `requestId`，车队控制可以区分不同请求，移动机器人也可以同时针对同一区域发出多个备选请求。
每次请求尝试都应使用对该移动机器人唯一的标识符。移动机器人重启后可以重复使用 ID。

对于进入 'RELEASE' 区域的请求，应在状态消息中添加一个 `requestType` 为 'ACCESS' 的 `zoneRequest` 对象。
请求允许沿规划路径进入 'COORDINATED_REPLANNING' 区域，或在该区域内重新规划路径时，应将 `requestType` 设置为 'REPLANNING'。
对于 'REPLANNING' 请求，应将规划路径以 NURBS 形式写入 `zoneRequest.trajectory`。移动机器人可以针对同一区域提交多条候选轨迹，但每条轨迹都必须使用独立的 `zoneRequest` 对象请求。
如果移动机器人需要进入由两个或以上 'RELEASE' 区域覆盖的工作区，则应在进入该区域前申请并获得所有必要区域的批准。
如果移动机器人要在地图上由两个或更多 'COORDINATED_REPLANNING' 区域覆盖的工作区中导航，则应分别针对每个区域请求在该范围内使用的路径，并在进入或更改路径之前获得车队控制批准。

移动机器人发起请求时，应先将 `requestStatus` 设置为 'REQUESTED'。

车队控制通过 `responses` 主题响应区域请求。
响应消息包含 `response` 对象数组。每个 `response` 只应响应 `requestId` 引用的单个请求。
每个响应的 `grantType` 取值为 'GRANTED'、'QUEUED'、'REVOKED' 或 'REJECTED'。
当 `grantType` 为 'GRANTED' 时，移动机器人可以进入区域或使用请求的轨迹。
车队控制可以将 `grantType` 设置为 'QUEUED'，在尚未授予许可的情况下确认已收到移动机器人的请求，并通知移动机器人该请求正在处理中。
当 `grantType` 为 'REJECTED' 时，移动机器人不得进入该区域或使用请求的轨迹。
`grantType` 为 'REVOKED' 表示此前授予的许可不再有效。在移动机器人将 `requestStatus` 设置为 'REVOKED' 之前，车队控制仍应将该请求视为 'GRANTED'。
`response` 对象可以包含 `leaseExpiry`，用于指定 'GRANTED' 许可的有效截止时间。如需延长有效期，车队控制可以重新发送包含新 `leaseExpiry` 的响应消息。

移动机器人应通过相应更新 `requestStatus` 来确认车队控制的响应，并在该请求信息仍然有效时将其保留在状态中。参见 [6.9 请求/响应机制](#69-请求响应机制)。

对于 'RELEASE' 区域，移动机器人与车队控制之间的交互应符合图 16。

移动机器人停留在 'RELEASE' 区域内时，应在状态中保留 `zoneRequest` 对象，并持续将 `requestStatus` 报告为 'GRANTED'，以通知车队控制其仍在该区域内。移动机器人离开区域后，应从状态消息中移除相应的 `zoneRequest` 条目。
如果移动机器人尚未进入 'RELEASE' 区域，而此时收到 `grantType` 为 'REVOKED' 的响应或 `leaseExpiry` 已到期，则应从状态中移除该请求，并且不得进入该区域。
如果移动机器人已经位于 'RELEASE' 区域内，而此时收到 `grantType` 为 'REVOKED' 的响应或 `leaseExpiry` 已到期，则应按照区域定义中的 `releaseLossBehavior` 作出反应。移动机器人应在状态中保留该请求，并相应地将 `requestStatus` 设置为 'REVOKED' 或 'EXPIRED'，直至离开该区域或重新获得通行许可。若要再次进入该区域，移动机器人应发出新请求。

![图 16 RELEASE 区域的区域请求行为](./assets/request_release_zone_access.png)
>图 16 - 'RELEASE' 区域的区域请求行为

对于 'COORDINATED_REPLANNING' 区域，移动机器人与车队控制之间的交互应符合图 17。

移动机器人应从所有已获得 'GRANTED' 响应的请求中选择一条轨迹，将对应请求的 `requestStatus` 设置为 'GRANTED'，并从状态中移除其他请求。
如果移动机器人尚未进入 'COORDINATED_REPLANNING' 区域，而此时收到 `grantType` 为 'REVOKED' 的响应或 `leaseExpiry` 已到期，则应从状态中移除该请求，并且不得进入该区域。
如果移动机器人已经位于 'COORDINATED_REPLANNING' 区域内，而此时收到 `grantType` 为 'REVOKED' 的响应或 `leaseExpiry` 已到期，则应停止行驶，并报告类型为 'RELEASE_LOST'、级别为 'CRITICAL' 的错误。移动机器人应在状态中保留该请求，并相应地将 `requestStatus` 设置为 'REVOKED' 或 'EXPIRED'，直至离开该区域或重新获得通行许可。若要继续沿当前路径穿越该区域，移动机器人应发出新请求。

![图 17 COORDINATED_REPLANNING 区域的区域请求行为](./assets/request_coordinated_replanning_zone_replanning.png)
>图 17 - 'COORDINATED_REPLANNING' 区域的区域请求行为


### 6.4.4 区域之间的交互

下表描述了区域之间可能的交互。该矩阵是对称的，因为无论按何种顺序考量两个区域，它们之间的交互都相同。对于每种组合，要么一个区域的行为覆盖另一个区域的行为（例如 'BLOCKED' 区域覆盖 'LINE_GUIDED' 区域），要么两者不存在冲突（例如 'LINE_GUIDED' 区域与 'COORDINATED_REPLANNING' 区域）。'DIRECTED' 区域与 'BIDIRECTED' 区域不得重叠，否则可能导致未定义的行为。“无区域”列定义基于轮廓的区域与无区域范围之间的行为，因为移动机器人可以同时处于已定义的区域类型和无区域范围内。对于基于运动学中心的区域，移动机器人只能完全位于区域内或区域外，因此不存在这种交互。

|  | **BLOCKED** | **RELEASE** | **LINE_GUIDED** | **COORDINATED_REPLANNING** | **SPEED_LIMIT** | **ACTION** | **PRIORITY** | **PENALTY** | **DIRECTED** | **BIDIRECTED** | **无区** | **EDGE-PROPERTIES**
--- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ---
**BLOCKED** | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED
**RELEASE** |  | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突 | 无冲突
**LINE_GUIDED** |  |  | 没有冲突 | LINE_GUIDED | 无冲突 | (1) | LINE_GUIDED | LINE_GUIDED | LINE_GUIDED | 没有冲突 | LINE_GUIDED | 没有冲突
**COORDINATED_REPLANNING** |  |  |  | (2) | 没有冲突 | (1) | 没有冲突 | 没有冲突 | 没有冲突 | 没有冲突 | COORDINATED_REPLANNING | (3)
**SPEED_LIMIT** |  |  |  |  | (4) | 没有冲突 | 没有冲突 | 没有冲突 | 没有冲突 | 没有冲突 | SPEED_LIMIT | (4)
**ACTION** |  |  |  |  |  | (5) | 没有冲突 | 没有冲突 | 没有冲突 | 没有冲突 | ACTION | (5)
**PRIORITY** |  |  |  |  |  |  | (6) | (6) | 没有冲突 | 没有冲突 | (7) | 没有冲突
**PENALTY** |  |  |  |  |  |  |  | (6) | 没有冲突 | 没有冲突 | (7) | 没有冲突
**DIRECTED** |  |  |  |  |  |  |  |  | (8) | (8) | (7) | (9)
**BIDIRECTED** |  |  |  |  |  |  |  |  |  | (8) | (7) | (9)

>表 8 - 区域交互矩阵

1) 如果操作与其他区域的行为冲突，报告类型为 'ZONE_ACTION_CONFLICT'、错误级别为 'CRITICAL' 的错误（订单错误），并停止移动机器人。
2) 所有 'COORDINATED_REPLANNING' 区需批准计划轨迹。
3) 如果边预定义了轨迹，则应在区域请求中发送。
4) 适用竞争 `maximumSpeed` 中最低的数值。
5) 执行所有操作。
6) 这里总是选择最严格的一种；对于 'PRIORITY' 区域，使用最低的 `priorityFactor`；对于 'PRIORITY' 和 'PENALTY' 区域重叠的情况，使用最高的 `penaltyFactor`；对于重叠的 'PENALTY' 区域，也使用最高的 `penaltyFactor`。
7) 对于基于运动学中心的区域，移动机器人只能完全在区域内或外部，因此这种重叠是不可能的。
8) 区域不应重叠，因为行为尚未定义。
9) 作为边属性一部分的 `trajectory` 应覆盖 'DIRECTED' 和 'BIDIRECTED' 区域所规定的行为。

### 6.4.5 区域内的错误处理

如果在订单执行过程中的任何时刻，移动机器人发现无法到达订单中的某个节点，则应向车队控制报告类型为 'NODE_UNREACHABLE'、级别为 'CRITICAL' 的错误。车队控制随后应决定如何继续。移动机器人不得再次尝试到达该节点，而应等待车队控制的进一步指令。


## 6.5 连接

在移动机器人客户端与代理服务器连接过程中，应设置最后遗嘱主题和消息，并在移动机器人客户端与代理服务器断开连接后由代理服务器发布。
因此，车队控制通过订阅所有移动机器人的连接主题来检测断线事件。
断线通过代理服务器与客户端之间交换的心跳来检测。
因此，车队控制可以通过订阅每个移动机器人的 `connection` 主题来检测断线事件。


因此，时间戳和 `headerId` 字段总是过时的。

移动机器人需要有序断开连接时：

1. 移动机器人向主题 `vda5050/v3/manufacturer/serialNumber/connection` 发送消息，并将 `connectionState` 设置为 'OFFLINE'。
2. 用断开连接命令断开 MQTT 连接。

移动机器人上线：

1. 创建 MQTT 连接时，将最后遗嘱主题设置为 `vda5050/v3/manufacturer/serialNumber/connection`，并将字段 `connectionState` 设置为 'CONNECTION_BROKEN'。
2. 向主题 `vda5050/v3/manufacturer/serialNumber/connection` 发送消息，并将 `connectionState` 设置为 'ONLINE'。

所有关于此主题的消息都应带有 `retained` 标志位。

当移动机器人与代理之间的连接意外中断时，代理会将最后遗嘱发送到主题 `vda5050/v3/manufacturer/serialNumber/connection`，其中字段 `connectionState` 设置为 'CONNECTION_BROKEN'。


## 6.6 状态

移动机器人状态应以单一主题发布。
与单独消息（例如当前订单进度、电池状态和错误）相比，使用单一主题可以减轻代理和车队控制系统在处理消息时的工作负荷，同时保持移动机器人状态信息的同步。

当相关事件发生时，移动机器人状态消息应发布一次，或至少每 30 秒发布一次。

以下事件应触发状态消息的传输：

- 接收订单
- 收到订单更新
- `loads` 数组的变化
- `errors` 数组的变化
- `operatingMode` 字段的变化
- `driving` 字段的变化
- `paused` 字段的变化
- `safetyState` 对象的变化
- `newBaseRequest` 字段的变化
- `lastNodeId` 或 `lastNodeSequenceId` 字段的变化
- `edgeRequests` 或 `zoneRequests` 数组的变化
- `powerSupply.charging` 字段的变化
- `nodeStates` 或 `edgeStates` 数组的变化
- `actionStates`、`instantActionStates` 或 `zoneActionStates` 数组的变化
- `zoneSets` 数组的变化
- `maps` 数组的变化

*备注：对于上述数组，数组单个项的更改以及添加或删除条目都应触发状态消息传输。*

应尽量减少不必要的通信量。
如果多个事件相互关联，宜将它们合并为一次状态更新，而不是连续发送多条状态消息。例如，接收新订单或通过节点时，通常会同时更新 `nodeStates` 和 `edgeStates`。相邻两条状态消息的最小发送间隔由信息表中的 `protocolLimits.timing.minimumStateInterval` 定义（参见 [7.10 factsheet 消息的实现](#710-factsheet-消息的实现)）。


### 6.6.1 概念与逻辑

订单进度由 `nodeStates` 和 `edgeStates` 跟踪。
此外，如果移动机器人能够确定其当前位置，它应通过 `mobileRobotPosition` 字段发布该信息。

`nodeStates` 和 `edgeStates` 包含移动机器人接下来要遍历的所有节点和边。

![图 18 状态主题提供的订单信息。仅传输最后一个节点的 ID 以及剩余的节点和边](./assets/order_information_state_topic.png)
>图 18 - 状态主题提供的订单信息。仅传输最后一个节点的 ID 以及剩余的节点和边


### 6.6.2 节点与边的遍历

移动机器人自行决定节点何时应计为穿越。
穿越的一个要求是移动机器人的控制点必须位于节点的 `allowedDeviationXY` 内，且其方向必须在 `allowedDeviationTheta` 内。
`allowedDeviationXY` 定义了线导引移动机器人在何时可以偏离预定轨迹，从而沿更平滑的路径切入拐角，而非到达节点的精确位置。离开 `allowedDeviationXY` 时，移动机器人应回到其预定的下一边轨迹上。
如果设置了下一条边的边属性 `corridor`，则应额外满足这些边界。

如果移动机器人距离订单的第一个节点太远，车队控制可以为该节点添加扩展 `allowedDeviationXY`，以包含移动机器人当前的位置。

移动机器人应通过将节点的 `nodeState` 从 `nodeStates` 数组中移除，并将 `lastNodeId` 和 `lastNodeSequenceId` 设置为经过节点的值来报告节点的遍历情况。

一旦移动机器人报告节点已遍历，移动机器人应触发与该节点关联的动作（如有）。
节点的遍历也必然意味着要离开通往该节点的边。
随后，该边也应从 `edgeStates` 中移除，并且边上处于活动状态的动作应结束。

节点的穿越也标志着移动机器人进入下一条边的时刻（如果有的话）。
边的动作应被触发（如有）。
例外情况是，如果移动机器人因 'SOFT' 或 'HARD' 阻塞动作而应停在节点上，则只有在重新开始行驶时才进入下一条边。

当存在活动订单时，仅当移动机器人遍历属于该订单的已释放节点时，才应更新 `lastNodeId` 和 `lastNodeSequenceId` 字段。例如，如果物理线导引移动机器人检测到不属于活动订单 `nodes` 的物理标记或标签，则此次检测不得导致 `lastNodeId` 或 `lastNodeSequenceId` 发生变化。

![图 19 订单处理过程中 nodeStates、edgeStates 和 actionStates 的表现](./assets/states_during_order_handling.png)
>图 19 - 订单处理过程中 `nodeStates`、`edgeStates` 和 `actionStates` 的表现

#### 6.6.2.1 `allowedDeviationXY` 作为椭圆的定义

`allowedDeviationXY` 定义为节点位置周围的椭圆，以便移动机器人能够更灵活地接近节点。

![图 20 allowedDeviationXY 椭圆](./assets/ellipse.png)
>图 20 - `allowedDeviationXY` 椭圆


### 6.6.3 基础区请求

如果移动机器人检测到其基础区即将耗尽，则可以将 `newBaseRequest` 标志设置为 `true`，以尝试避免不必要的制动。

### 6.6.4 信息

移动机器人可通过 `information` 数组向车队控制提交任意额外信息。
由移动机器人决定在信息消息中报告某项信息的时长。

车队控制不得将这些信息用于逻辑处理；它们只能用于可视化和调试。

### 6.6.5 错误

移动机器人通过 `errors` 数组报告任何问题。

#### 6.6.5.1 错误级别

问题可分为四个层级： 'WARNING'、 'URGENT'、 'CRITICAL'和 'FATAL'。

- 'WARNING' 级别的问题不需要立即处理。移动机器人可以继续执行当前订单，并能接收新订单。错误可能会自行消除，例如 LiDAR 扫描器上的污物被清除。
- 'URGENT' 级别的问题需要立即处理，例如电量不足。移动机器人可以继续执行当前订单，并能接收新订单。
- 'CRITICAL' 级别的问题需要立即处理，例如尝试拾取并不存在的物体。移动机器人不得继续行驶，因为它无法继续当前订单，但仍可接收新订单。
- 'FATAL' 级别的问题需要用户介入，例如定位丢失。移动机器人不得继续驾驶，因为它既无法继续当前的订单，也无法接收新的订单。

移动机器人可以在 `errorReferences` 数组中添加错误关联项，以帮助定位错误原因。
`errorDescription` 和 `errorHint` 字段可能提供人类可读的文本，解释错误或建议可能的解决方案。

无论问题的严重程度如何，移动机器人都不得因此清除订单。


#### 6.6.5.2 错误关联项

如果错误由订单内容有误或执行失败引起，移动机器人可以在 `errorReferences` 字段中返回有意义的关联项，以帮助定位错误原因。
这可能包括以下信息：

- `headerId`
- 主题（`order` 或 `instantActions`）
- 如果错误由订单更新引起，则包括 `orderId` 和 `orderUpdateId`
- 如果错误由动作引起，则包括 `actionId`
- 如果错误由错误的动作参数引起，则包括参数列表


#### 6.6.5.3 错误翻译

对于 `errorDescription` 和 `errorHint`，移动机器人可以通过 `errorDescriptionTranslations` 和 `errorHintTranslations` 数组提供翻译。
每个翻译都包含一个 ISO 639-1语言代码及相应的翻译文本。


#### 6.6.5.4 预定义错误类型

移动机器人应使用预定义的错误类型来报告具体问题。下表列出了预定义的错误类型及其描述。

错误类型 | 错误级别 | 描述 | 关联项 | 报告时长
--- | --- | --- | --- | ---
'UNSUPPORTED_PARAMETER' | 'CRITICAL' | 收到包含不受支持的可选参数的消息。 | 参数名称 | 直到接受新订单。
'NO_ORDER_TO_CANCEL' | 'WARNING' | 移动机器人收到了 `cancelOrder` 动作，但当前没有可取消的活动订单。 | `cancelOrder` 的 `actionId` | 直到新订单被接受。
'VALIDATION_FAILURE' | 'WARNING' | 收到格式错误的订单。 | 如果可能，提供被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'INVALID_ORDER_ACTION' | 'WARNING' | 收到包含不受支持动作的订单。 | 被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'INVALID_INSTANT_ACTION' | 'WARNING' | 收到不受支持的即时动作。 | `instantAction` 的 `actionId` | 直到接受新的即时动作。
'OUTDATED_ORDER_UPDATE' | 'WARNING' | 收到 `orderId` 正确但 `orderUpdateId` 已过时的订单。 | 被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'SAME_ORDER_UPDATE_ID' | 'WARNING' | 收到重复的订单消息（`orderId` 和 `orderUpdateId` 均相同）。 | 被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'ORDER_UPDATE_FOLLOWING_CANCEL' | 'WARNING' | 收到已取消订单的订单更新。 | 被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'OUTSIDE_OF_CORRIDOR' | 'CRITICAL' | 离开为边定义的走廊。 | `edgeId` | 直到移动机器人不再违反走廊边界。
'INSUFFICIENT_MEMORY' | 'URGENT' | 移动机器人没有足够内存处理收到的订单。 | 如果可能，提供被拒消息的 `orderId` 和 `orderUpdateId`。 | 直到接受新订单。
'DUPLICATE_MAP' | 'WARNING' | 收到的地图具有已经存在的 `mapId` 和 `mapVersion`。 | 重复的 `mapId` 和 `mapVersion` | 直到接受新的地图相关 `instantAction`。
'BLOCKED_ZONE_VIOLATION' | 'CRITICAL' | 进入 'BLOCKED' 区域。 | `zoneId` | 直到移动机器人不再位于 'BLOCKED' 区域内。
'DUPLICATE_ZONE_SET' | 'WARNING' | 接收到 `zoneSetId` 已存在的区域集。 | `zoneSetId` 或 `instantAction` 的 `actionId` | 足以让车队控制注意到区域更新失败的合理时长。
'RELEASE_LOSS_HANDLING' | 'WARNING' | 移动机器人位于 'RELEASE' 区域内时失去通行许可后，正在执行 'EVACUATE' 或 'CONTINUE' 释放丢失行为。 | `zoneId` | 直到移动机器人离开该区域、再次获得通行许可，或订单在该区域内结束。
'RELEASE_LOST' | 'CRITICAL' | 移动机器人位于 'RELEASE' 或 'COORDINATED_REPLANNING' 区域内时，因失去通行许可而在区域内停止。 | `zoneId` | 直到移动机器人离开该区域，或再次获得通行许可。
'ZONE_ACTION_CONFLICT' | 'CRITICAL' | 区域行为与区域动作之间的冲突。 | 'ACTION'区 `zoneId` | 直到移动机器人不再违反区域行为。
'NODE_UNREACHABLE' | 'CRITICAL' | 移动机器人无法按其订单到达节点。 | `nodeId` | 直到新订单被接受。
'LOCALIZATION_ERROR' | 'FATAL' | 移动机器人未定位。 |  | 直到重新获得定位。
'NO_ROUTE_TO_TARGET' | 'WARNING' | 收到包含至少一个不可到达节点的订单。 | `orderId` | 直到接受新订单。
'OTHER_ORDER_ACTIVE' | 'WARNING' | 在另一个订单仍处于活动状态时收到新订单。 | `orderId` | 直到接受新订单。
'START_NODE_OUT_OF_RANGE' | 'WARNING' | 收到首个节点不可到达的订单。 | `orderId` | 直到接受新订单。
'MOBILE_ROBOT_NOT_AVAILABLE' | 'WARNING' | 在非 'AUTOMATIC'、'SEMIAUTOMATIC' 或 'INTERVENED' 操作模式下收到订单。 | `orderId` | 直到操作模式允许接收新订单
'UNKNOWN_MAP_ID' | 'WARNING' | 收到包含引用未知 `mapId` 的节点的订单。 | `orderId` | 直到接受新订单。
'INSTANT_ACTION_STATES_FULL' | 'URGENT' | `instantActionStates` 数组过长，难以管理。 | - | 直到车队控制清除此列表。
'ZONE_ACTION_STATES_FULL' | 'URGENT' | `zoneActionStates` 数组过长，难以管理。 | - | 直到车队控制清除此列表。

>表 9 - 预定义错误类型


### 6.6.6 操作模式

在正常执行订单期间，车队控制应完全控制移动机器人。但在某些情况下无法做到这一点，例如需要对移动机器人进行手动操作时。移动机器人应通过 `operatingMode` 字段报告这种情况。

以下列表描述了 `operatingMode` 字段的值、其含义以及对移动机器人与车队控制相互作用的影响：

操作模式 | 描述
--- | ---
AUTOMATIC | 车队控制对移动机器人拥有完全控制权。<br>移动机器人根据车队控制下发的订单行驶并执行动作。
SEMIAUTOMATIC | 车队控制仍控制移动机器人。<br>移动机器人根据车队控制下发的订单行驶并执行动作。<br>行驶速度由 HMI 控制，转向由自动系统控制。
INTERVENED | 车队控制不控制移动机器人。移动机器人仍正确报告其状态。<br>可以使用 HMI 控制移动机器人的转向、速度和载荷处理装置。<br>车队控制可以向移动机器人发送订单或订单更新，待移动机器人切换回 'AUTOMATIC' 或 'SEMIAUTOMATIC' 操作模式后执行。除 `cancelOrder` 外，车队控制不得发送任何即时动作。<br>移动机器人不得清除订单，但应从状态中移除所有区域请求，即使移动机器人已经位于 'RELEASE' 区域内。(*备注：如有必要，车队控制可以继续追踪移动机器人的位置，并决定是否可以为其他移动机器人放行。*) 移动机器人不得请求进入 'RELEASE' 区域的许可，也不得请求在 'COORDINATED_REPLANNING' 区域内重新规划的许可。<br>如果进入 'INTERVENED' 操作模式会影响正在执行的动作，移动机器人应在状态消息中相应反映。<br>如果移动机器人离开该操作模式但未直接切换到 'AUTOMATIC' 或 'SEMIAUTOMATIC'，则应按照新的操作模式行动。如果直接切换到 'AUTOMATIC' 或 'SEMIAUTOMATIC'，则移动机器人应继续执行当前订单。如果移动机器人在 'INTERVENED' 操作模式期间检测到当前订单无法继续执行，则应切换到 'MANUAL' 操作模式并按其规则行动。
MANUAL | 车队控制不控制移动机器人。<br>车队控制不得向移动机器人发送订单或动作。<br>HMI 可以控制移动机器人的转向、速度和载荷搬运装置。<br>移动机器人应向车队控制发送其位置。<br>进入该模式时，移动机器人应立即清除当前订单。<br>在该模式下，如果移动机器人检测到自身已被移至某个位置，导致当前 `lastNodeId` 无法作为新订单的起始节点，则应将 `lastNodeId` 设置为空字符串（""）。
STARTUP | 车队控制不控制移动机器人。移动机器人正在启动，尚未准备好接收订单。在启动完成之前，状态消息中的参数可能不完整或无效。
SERVICE | 车队控制不控制移动机器人。<br>车队控制不得向移动机器人发送订单或动作。<br>进入该模式时，移动机器人应立即清除当前订单，并将 `lastNodeId` 设置为空字符串（""）。<br>授权人员可以重新配置移动机器人。
TEACH_IN | 车队控制不控制移动机器人。<br>车队控制不得向移动机器人发送订单或动作。<br>进入该模式时，移动机器人应立即清除当前订单，并将 `lastNodeId` 设置为空字符串（""）。<br>移动机器人处于示教过程中，例如由操作员执行地图绘制。

>表 10 - 移动机器人的操作模式


操作模式 | 车队控制是否控制 | 状态消息内容是否有效 | 进入时是否清除订单 | 是否将 `lastNodeId` 设为空 | 进入时是否清除区域请求 | 是否允许发送即时动作 | 是否允许发送订单
--- | --- | --- | --- | --- | --- | --- | ---
AUTOMATIC | 是 | 是 | 否 | 否 | 否 | 是 | 是
SEMIAUTOMATIC | 是 | 是 | 否 | 否 | 否 | 是 | 是
INTERVENED | 否 | 是 | 否 | 否 | 是 | 仅允许 `cancelOrder` | 是
MANUAL | 否 | 是 | 是 | 是，如果无法再开始新订单 | 是 | 否 | 否
STARTUP | 否 | 否 | 是 | 是 | 是 | 否 | 否
SERVICE | 否 | 是 | 是 | 是 | 是 | 否 | 否
TEACH_IN | 否 | 是 | 是 | 是 | 是 | 否 | 否

>表 11 - 操作模式概述及其影响


### 6.6.7 清除移动机器人上的订单

针对以下任一事件，移动机器人应停止执行当前订单：

- 移动机器人的操作模式切换为 'MANUAL'、'STARTUP'、'SERVICE' 或 'TEACH_IN'（参见 [6.6.6 操作模式](#666-操作模式)）。
- 移动机器人收到车队控制发出的 `cancelOrder` 即时动作。
- 移动机器人收到 `startHibernation` 即时动作。

在这些情况下，移动机器人应清除当前订单，具体要求如下：

- `actionStates` 中所有已计划但尚未执行的动作都应取消，并在 `actionStates` 中报告为 'FAILED'。
- 对于 `actionStates` 中所有正在执行的动作：
		- 可以取消（`cancelAllowed = true`）的动作应取消，并在 `actionStates` 中报告为 'FAILED'。
		- 无法取消（`cancelAllowed = false`）的动作在执行期间应报告为 'RUNNING'，执行结束后再报告相应状态；成功时为 'FINISHED'，否则为 'FAILED'。
- `orderId`、`orderUpdateId`、`lastNodeId` 和 `lastNodeSequenceId` 的值保持不变。
- `nodeStates` 和 `edgeStates` 数组应设置为空列表。
- 所有请求都应从状态中移除。

只要订单中仍有动作尚未处于 'FINISHED' 或 'FAILED' 状态，移动机器人就不得报告 'MANUAL'、'SERVICE' 或 'TEACH_IN' 操作模式。在报告 'MANUAL'、'SERVICE'、'STARTUP' 或 'TEACH_IN' 操作模式之前，不得清空 `nodeStates` 和 `edgeStates`。

订单取消只能由车队控制触发。


### 6.6.8 移动机器人的空闲状态

如果移动机器人的 `nodeStates` 和 `edgeStates` 为空，且 `actionStates` 中的所有动作均处于 'FINISHED' 或 'FAILED' 状态，则该移动机器人处于空闲状态。只有当移动机器人处于空闲状态时，才应接受新订单。移动机器人处于空闲状态或正在执行订单时均可接受订单更新。处于空闲状态时，移动机器人可以执行即时动作。


### 6.6.9 动作状态

当移动机器人收到作为订单一部分的 `action`（附加到订单的 `node` 或 `edge`）时，应在 `actionStates` 数组中使用相应的 `actionState` 报告该 `action`。
当移动机器人收到 `instantAction` 时，应在 `instantActionStates` 数组中使用相应的 `actionState` 报告该动作。
当移动机器人执行 `zoneAction` 时，应在 `zoneActionStates` 数组中使用相应的 `actionState` 报告该动作。移动机器人也可以选择在此报告任何已计划的 `zoneAction`。

动作当前阶段应反映在对应 `actionState` 的 `actionStatus` 字段中（见表 12）。

actionStatus | 描述
--- | ---
'WAITING' | 移动机器人已收到动作，但对应节点尚未遍历或对应边尚未进入。
'INITIALIZING' | 动作被触发，准备措施启动。
'RUNNING' | 动作正在进行。
'PAUSED' | 动作因 `startPause` 即时动作或外部触发器（移动机器人上的暂停按钮）而暂停。
'RETRIABLE' | 动作执行失败但可以重试，这由订单动作中的 `retriable` 参数指定。通过 `retry` 或 `skipRetry` 即时动作或外部触发器触发从该状态的转换。
'FINISHED' | 动作结束了。<br>结果通过 `actionResult` 报告。
'FAILED' | 无论出于什么原因，动作都无法完成。

>表 12 - `actionStatus` 字段的可行值

图 21 展示了所有可能的动作状态转换，下表给出了相应示例：


| **从/到→** | **WAITING** | **INITIALIZING** | **PAUSED** | **RUNNING** | **RETRIABLE** | **FAILED** | **FINISHED** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **初始状态** | 排队等待稍后执行 | 立即开始初始化（例如 `instantAction`） | - | 立即开始执行（例如 `instantAction`） | - | 即时动作执行失败（移动机器人无法识别该动作或参数无效） | 动作立即完成（例如设置参数） |
| **WAITING** | - | 需要准备（提升、启动传感器） | - | 无需准备 | - | 因取消或切换到手动模式而中止 | 动作立即成功，例如到达节点或进入边后 |
| **INITIALIZING** | - | - | 外部触发器 | 初始化完成，动作开始执行 | - | 初始化失败、因取消而中止或切换到手动模式 | - |
| **PAUSED** | - | 外部触发器 | - | 外部触发器 | - | 因 `cancelOrder` 而中止或切换到手动模式 | - |
| **RUNNING** | - | - | 外部触发器 | - | 动作未成功完成，但可重试 | 因取消而中止、切换到手动模式，或动作最终因未返回预期结果而失败 | 动作返回预期结果；如果动作无法中断而必须执行完毕，也可能在通过 `cancelOrder` 中止订单后进入此状态。 |
| **RETRIABLE** | - | 通过 `retry` 重试动作或由外部触发器触发 | - | 通过 `retry` 重试动作或由外部触发器触发 | - | 通过 `skipRetry`、`cancelOrder`、外部触发器或切换到手动模式而失败 | 操作员通过外部输入修复问题 |

>表 13 - 可能的动作状态转换示例

![图 21 actionStates 的所有可能状态转换](./assets/action_state_transition.png)
>图 21 - `actionStates` 的所有可能状态转换


#### 6.6.9.1 移动机器人状态下的预测区动作报告

移动机器人的状态应始终表示当前订单的完整状态。因此，机器人应始终报告基础区和预测区内动作的 `actionStates`。所有预测区动作均报告为 'WAITING'。如果移动机器人收到的订单更新移除或更改了原预测区的一部分，则应从 `actionStates` 中移除附加在这些节点和边上的所有动作，以反映该变化。基础区动作的 `actionStates` 在 `orderUpdate` 过程中绝不得移除，因为基础区一旦释放便无法修改。

### 6.6.10 走廊使用请求

如果移动机器人当前活动订单中的走廊将 `releaseRequired` 标志设置为 `true`，则移动机器人应在偏离边的预定义轨迹之前发出请求。为此，机器人应在状态消息中添加一个 `edgeRequest` 对象。`requestId` 在移动机器人发出的所有请求（例如 `zoneRequest`、`edgeRequest`）中应保持唯一。

`requestStatus` 设置为 'REQUESTED'，`edgeId` 和 `sequenceId` 的组合引用机器人请求偏离其轨迹的边。只要这些边属于当前基础区，移动机器人就可以同时请求批准多条边。每条走廊的使用都应通过单独的 `edgeRequest` 请求，并由车队控制通过 `responses` 主题分别批准（见 [6.9 请求/响应机制](#69-请求响应机制)）。

车队控制只应批准属于基础区的边所对应的走廊。在收到车队控制的 `response` 之前，机器人应始终位于当前边的预定义轨迹上。机器人获得开始机动的批准后，应将 `requestStatus` 设置为 'GRANTED'，此时便可使用走廊。

只要机器人仍需使用走廊，就应在状态中保留 `edgeRequest`。如果移动机器人不再需要使用走廊，例如已成功完成避让且不再需要绕过障碍物，则应从状态中移除相应的 `edgeRequest` 对象，以此通知车队控制。此后，移动机器人应再次按照线导引移动机器人的规则行动。如果希望再次偏离预定义轨迹，则应发出新的 `edgeRequest`。
如果机器人在避让过程中到达当前边的 `corridor` 末端，并计划继续进入尚未获准使用的下一条走廊，则应在当前 `corridor` 的边界处停止，发送单独的边请求，并等待车队控制批准。

如果移动机器人的许可因响应中的 `leaseExpiry` 到期，或车队控制撤销了已批准的请求，则移动机器人应启动该边走廊的 `releaseLossBehavior` 中预定义的回退动作。
失去许可后的恢复策略包括：移动机器人沿偏离轨迹时所经过的路径返回该边的预定义轨迹，或者停在当前位置等待人工干预。


## 6.7 可视化

为了近实时地更新位置和规划轨迹，移动机器人可以通过 `visualization` 主题广播其位置、速度和规划轨迹。

可视化对象的字段使用与状态中位置、速度、计划路径和中间路径对象相同的结构。
更多信息请参见 [可视化消息的实现](#79-visualization-消息的实现)。
该主题的更新速率由系统集成方定义。


## 6.8 自由导航式移动机器人规划路径的共享

自由导航的移动机器人应通过状态消息向车队控制系统传达其规划轨迹。需要以更高频率共享时，可以使用 `visualization` 主题。

移动机器人共享的 `intermediatePath` 表示到达较近航点的预计时间，这些航点是移动机器人能够通过传感器感知的；`plannedPath` 则表示移动机器人当前活动订单中的一段较长路径。无论订单包含哪些节点，两条路径都应从移动机器人的当前位置开始。共享路径的长度可能取决于具体情况，因此可由移动机器人决定。如果移动机器人采用自由导航，则应在每条状态消息中同时共享 `intermediatePath` 和 `plannedPath`。

- `plannedPath` 按照 `edgeState` 的 `trajectory` 字段定义为 NURBS。`plannedPath` 可以包含一个节点数组，这些节点通过各自的 `nodeId` 引用，并将作为当前路径的一部分被遍历。移动机器人的 `plannedPath` 发生显著变化时应予以更新。`plannedPath` 应至少覆盖移动机器人当前的基础区。
- `intermediatePath` 被定义为折线。该折线由航点之间的线段组成。每个 `waypoint` 包括其 `x` 和 `y` 位置、移动机器人的可选方向以及指示预计到达时间的 `ETA`。

`intermediatePath` 应随着每次发送的状态或可视化消息更新，并且始终从移动机器人当前位置开始。

参数 `plannedPath` 和 `intermediatePath` 应仅用于移动机器人规划的轨迹。`edgeState` 中的 `trajectory` 字段应仅用于“确认”已经在布局或订单中预先定义的轨迹。


## 6.9 请求/响应机制

移动机器人与车队控制之间的某些协调任务需要获得车队控制的明确许可，移动机器人才允许执行操作。对于这些情况，采用请求/响应机制。请求的生命周期如图 22 所示。

![图 22 请求状态转换的可视化](./assets/request_state_transitions.png)
>图 22 - 请求生命周期：请求状态及可能转移的逻辑

请求总是由移动机器人发起，并作为状态消息的一部分进行传达。车队控制应评估请求并通过 `responses` 主题返回决定。

每个请求都应在移动机器人上表示为状态消息中包含的请求对象（例如 `zoneRequest`）。请求对象至少应包含：

- 一个 `requestId`，它在每台移动机器人当前的所有活动请求中保持唯一；
- 一个 `requestType`，指定请求所指的操作类型（访问、重新规划、走廊使用）；
- 对请求所涉及资源的引用（例如区域、区域集、地图、`edgeId`、`sequenceId`）；以及
- 一个 `requestStatus`。

字段 `requestStatus` 描述请求的生命周期，并应支持以下值：

- 'REQUESTED'：移动机器人发出请求。
- 'GRANTED'：车队控制批准了请求。
- 'REVOKED'：车队控制撤销之前批准的请求。
- 'EXPIRED'：请求已过期。
- 'QUEUED'：车队控制确认收到移动机器人的请求，但尚未授予许可。请求已被加入某种队列。

车队控制接收来自状态主题的请求，并应通过包含响应对象的 `responses` 主题来回答，该对象包括：

- 相应请求的 `requestId`，
- 一个取值为 'GRANTED'、'QUEUED'、'REJECTED' 或 'REVOKED' 的决定，以及
- 可选的 `leaseExpiry` 时间戳，用于限制 'GRANTED' 决定的有效期。

如果请求收到 'QUEUED' 响应，则车队控制确认已收到请求，但尚未授予许可。移动机器人随后应继续等待，并且不得执行所请求的操作。如果请求收到 'REJECTED' 响应，则移动机器人不得执行所请求的操作，并可在不再需要相应请求对象时将其从状态中移除。

如果请求收到 'GRANTED' 响应，则移动机器人可以按照请求类型的语义执行所请求的操作。如果存在 `leaseExpiry`，则许可应仅在该时间之前视为有效。车队控制可以发送具有相同 `requestId` 和新 `leaseExpiry` 的更新响应来延长许可有效期。

如果请求被回答为 'REVOKED'，或达到 `leaseExpiry`，移动机器人应按照请求资源定义的 `releaseLossBehavior`（释放丢失处理）行动。
如果所请求的操作已开始，移动机器人应将该请求保留在状态中，并相应地将 `requestStatus` 设置为 'REVOKED' 或 'EXPIRED'，直到为请求资源定义的释放丢失处理完成。如果所请求的操作尚未开始，移动机器人应立即从状态中移除该请求。

如果在应用要求的时间范围内未收到响应，移动机器人应按照请求尚未获批的情况行动，不得执行需要明确许可的操作。超时和重试的处理应在集成期间定义。

一旦相应操作完成、中止或被拒绝，并且不再需要车队控制作出进一步决定，就应从移动机器人的状态中移除该请求。


## 6.10 信息表

信息表提供特定移动机器人型号系列的基本信息。
这些信息可用于比较不同的移动机器人类型，也可用于移动机器人系统的规划、规模设计、仿真或集成。

移动机器人信息表中某些字段的值只能在系统集成期间指定，例如为移动机器人分配其支持的项目特定载荷。
信息表既供人阅读，也供机器处理，例如导入车队控制应用，因此采用 JSON 文档格式。

车队控制可以通过发送即时动作 `factsheetRequest` 向移动机器人请求该信息表。

所有关于此主题的消息都应带有 `retained` 标志位。


# 7 消息规范

不同的消息以表格形式呈现，用于描述 JSON 字段的内容。

此外，可从公开 GitHub 仓库 (https://github.com/VDA5050/VDA5050) 获取用于验证的 JSON Schema。
每次发布 VDA 5050 时都会更新 JSON Schema。如果 JSON Schema 与本文档存在差异，应以本文档所述版本为准。


## 7.1 表格符号与格式含义

对象结构表包含标识符名称、单位、数据类型以及描述（如有）。

标识 | 描述
--- | ---
standard | 变量是基本数据类型
**bold** | 变量是非基本数据类型（例如 JSON 对象或数组），并单独定义
*italic* | 变量为可选
***italic and bold*** | 变量为可选，且为非基本数据类型
arrayName[arrayDataType] | 变量（此处为 `arrayName`）是由方括号内所示数据类型组成的数组（此处的数据类型为 `arrayDataType`）

>表 14 - 表格符号及格式含义

所有关键词均以大小写区分。
所有字段名称均采用 `camelCase`。


### 7.1.1 可选字段

如果变量被标记为可选，则发送方可以省略该变量，因为它在某些情况下可能不适用。例如，车队控制向移动机器人发送订单时，某些移动机器人会自行规划轨迹，因此可以省略订单 `edge` 对象中的 `trajectory` 字段。

如果移动机器人收到的消息包含本协议标记为可选的字段，则应相应处理，不得忽略该字段。
如果移动机器人因不支持的参数而无法处理订单，则应报告类型为 'UNSUPPORTED_PARAMETER'、级别为 'CRITICAL' 的错误，并拒绝该订单。

车队控制应仅发送移动机器人支持的可选字段。

举例：轨迹是可选的。
如果移动机器人无法处理轨迹，车队控制系统不应向移动机器人发送轨迹。

移动机器人应通过移动机器人的 `factsheet` 消息传达其所需的可选参数。


### 7.1.2 允许的字符与字段长度

所有通信均采用 UTF-8 编码，以支持描述文本的国际化。
建议 ID 仅使用以下字符：

A-Z a-z 0-9 _ - . :

最大消息长度未被定义，但受 MQTT 协议规范及可能由信息表中定义的技术约束限制。

如果移动机器人的内存不足以处理收到的订单，则应拒绝该订单，并报告类型为 'INSUFFICIENT_MEMORY'、级别为 'URGENT' 的错误。

字段、字符串的最大长度或值范围由系统集成方确定。

为便于集成，移动机器人供应商应提供移动机器人信息表，详见 [7.10 factsheet 消息的实现](#710-factsheet-消息的实现)。


### 7.1.3 字段、主题与枚举的表示法

本文档中的主题和字段以以下格式突出显示：`exampleField` 和 `exampleTopic`。
枚举应使用大写字母，并用下划线分隔单词，例如 'EXAMPLE_ENUMERATION'。本文档使用单引号包裹这些枚举值。
这也适用于 `actionStatus` 字段中的关键字（'WAITING'、'FINISHED' 等）。
可扩展枚举包含但不限于参数的预定义值。


### 7.1.4 JSON 数据类型

在可能的情况下，应使用 JSON 数据类型。
因此，布尔值编码为 `true` 或 `false`，而不是枚举（'TRUE'、'FALSE'）或魔法数字。
数值数据类型通过类型和精度指定，例如 `float64` 或 `uint32`。不支持 IEEE 754 中的特殊数值，如 `NaN` 和无穷大。


## 7.2 协议头

每条 JSON 消息都以协议头开始。
协议头由以下各个元素组成。
头部不是 JSON 对象。

对象结构 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。


## 7.3 订单消息的实现

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
headerId |  | uint32 | 消息的协议头 ID。<br>协议头 ID 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp |  | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version |  | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer |  | string | 移动机器人制造商。
serialNumber |  | string | 移动机器人的序列号。
orderId |  | string | 订单标识符。<br>用于标识属于同一订单的多条订单消息。
orderUpdateId |  | uint32 | 订单更新标识符。<br>`orderUpdateId` 在每个 `orderId` 内应唯一，并且新订单应从 0 开始。<br>如果订单更新被拒绝，该字段的值应包含在相应错误中。
*orderDescription* |  | string | 仅用于可视化的额外人类可读信息；不得用于任何逻辑处理。
**nodes [node]** |  | array | 为完成订单而需要遍历的节点对象数组。
**edges [edge]** |  | array | 为完成订单而需要遍历的边对象数组。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**node** { |  | JSON object |
nodeId |  | string | 节点的唯一标识符。<br>同一节点可以在一个订单消息中多次被引用。`sequenceId` 用于区分穿越序列。
sequenceId |  | uint32 | 用于跟踪订单中节点和边的顺序，并简化订单更新的编号。<br>其主要用途是区分同一 `orderId` 中多次经过的同一节点。<br>`sequenceId` 由节点和边共享，并定义遍历顺序。
*nodeDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
released |  | boolean | `true` 表示该节点是基础区的一部分。<br>`false` 表示该节点属于预测区。
***nodePosition*** |  | JSON object | 节点位置。<br>对于不需要节点位置的移动机器人类型（例如线导引移动机器人），这是可选的。
**actions [action]**<br> } |  | array | 要在节点上执行的动作数组。<br>如果无需执行动作，则为空数组。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**nodePosition** { |  | JSON object | 在项目特定的全局坐标系中定义地图上的位置。<br>每个楼层都有自己的地图。<br>所有地图都应使用同一个项目特定的全局原点。
x | m | float64 | 地图上相对于项目特定全局坐标系的 x 位置。<br>精度取决于具体实现。
y | m | float64 | 地图上相对于项目特定全局坐标系的 y 位置。<br>精度取决于具体实现。
*theta* | rad | float64 | 范围：[-Pi ... Pi]<br><br>移动机器人在节点处应达到的绝对朝向角；达到该朝向角后，该节点才被视为已遍历。<br>如果定义了该值，移动机器人应在此节点处达到该朝向角。<br>如果前一条边不允许旋转，移动机器人应在节点上旋转。<br>如果后一条边定义了不同的朝向角且不允许在边上旋转，则移动机器人应在进入该边之前，在节点上旋转到后一条边要求的朝向角。
***allowedDeviationXY*** | m | JSON object | 指示移动机器人必须以多高的位置精度匹配节点，节点才被视为已遍历。<br>另见 [6.6.2 节点与边的遍历](#662-节点与边的遍历)。
*allowedDeviationTheta* | rad | float64 | 范围：[0.0 ... Pi]<br><br>如果定义，则指示移动机器人必须以多高的朝向角精度匹配节点，节点才被视为已遍历。<br>可接受的最小角度为 *`theta` - `allowedDeviationTheta`*，最大角度为 *`theta` + `allowedDeviationTheta`*。如果未指定 `theta`，则对移动机器人的朝向角没有要求。<br>如果该值为 0.0，则不允许偏差，即移动机器人应在技术能力允许的范围内尽可能精确地达到节点规定的朝向角。即使 `allowedDeviationTheta` 小于移动机器人的技术公差，此规则仍然适用。如果移动机器人支持该属性，但车队控制未为此节点定义该属性，则移动机器人应假定其值为 0.0。
mapId<br>} |  | string | 该位置所引用地图的唯一标识符。<br>每张地图都具有同一个项目特定的全局坐标原点。<br>当移动机器人使用电梯（例如从出发楼层前往目标楼层）时，它会从出发楼层的地图上消失，并出现在目标楼层地图中对应的电梯节点上。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**allowedDeviationXY** { |  | JSON object | 指示移动机器人必须以多高的位置精度匹配节点，节点才被视为已遍历。<br>如果 `a` = `b` = 0.0，则不允许偏差，即移动机器人应使其控制点在技术能力允许的范围内尽可能精确地到达或经过节点位置。即使 `allowedDeviationXY` 小于移动机器人在技术上可实现的公差，此规则仍然适用。如果移动机器人支持该属性，但车队控制未为此节点定义该属性，则移动机器人应假定 `a` 和 `b` 均为 0.0。<br>节点坐标定义椭圆的中心。
a | m | float64 | 椭圆半长轴的长度（米）。
b | m | float64 | 椭圆半短轴的长度（米）。
theta<br>} | rad | float64 | 旋转角（项目特定坐标系内，从正水平轴到椭圆长轴的角度）。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**action** { |  | JSON object | 描述移动机器人能够执行的动作。
actionType |  | string | 动作类型。预定义动作见表 4 第一列。<br>用于标识动作的功能。
actionId |  | string | 用于标识动作，并将其映射到状态消息中相应 `actionState` 的唯一 ID。<br>建议使用 UUID。
*actionDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
blockingType |  | string | 枚举 {'NONE', 'SINGLE', 'SOFT', 'HARD'}：<br>'NONE'：允许行驶和执行其他动作；<br>'SINGLE'：允许行驶，但不允许执行其他动作；<br>'SOFT'：允许执行其他动作，但不允许行驶；<br>'HARD'：该动作是此时唯一允许执行的动作。
***actionParameters [actionParameter]*** |  | array | 指定动作的 `actionParameter` 对象数组，例如 `deviceId`、`loadId`、`external triggers`。<br><br>实现示例见 [7.3.1 动作参数格式](#731-动作参数格式)。
*retriable*<br> } |  | boolean | `true`：动作失败时可以进入 'RETRIABLE' 状态。<br>`false`：动作失败后直接进入 'FAILED' 状态。<br>默认值：`false`。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**edge** { |  | JSON object | 两个节点之间的定向连接。
edgeId |  | string | 边的唯一标识符。<br>同一条边可以在一条订单消息中多次被引用。`sequenceId` 用于区分穿越序列。
sequenceId |  | uint32 | 用于跟踪订单中节点和边的顺序，并简化订单更新的编号。<br>`sequenceId` 由节点和边共享，并定义遍历顺序。
*edgeDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
released |  | boolean | `true` 表示该边是基础区的一部分。<br>`false` 表示边是预测区的一部分。
*maximumSpeed* | m/s | float64 | 边上允许的最大速度。<br>速度以移动机器人上速度最快的测量点为准。
*maximumMobileRobotHeight* | m | float64 | 边上允许的移动机器人最大高度，包括载荷在内。
*minimumLoadHandlingDeviceHeight* | m | float64 | 边上允许的载荷搬运装置最小高度。
*orientation* | rad | float64 | 移动机器人在边轨迹上的朝向角。`orientationType` 的值决定该朝向角应解释为相对于项目特定的全局地图坐标系，还是相对于边轨迹的切线。采用切线解释时，0.0 表示向前行驶，Pi 表示向后行驶。<br>示例：Pi/2 rad 的朝向角可能导致旋转 90 度。<br><br>如果移动机器人的起始朝向角不同，且 `reachOrientationBeforeEntering` 设置为 `false`，则移动机器人应在边上旋转到所需朝向角。<br>如果 `reachOrientationBeforeEntering` 为 `true`，则应在进入边之前完成旋转。<br>如果无法做到，则应拒绝订单。<br><br>如果未定义轨迹，则应将朝向角和任何旋转要求应用于连接该边两个节点的直线路径。<br>如果未定义朝向角，则移动机器人可以在边上采用任意朝向角。
*orientationType* |  | string | 枚举 {'GLOBAL', 'TANGENTIAL'}：<br>'GLOBAL'：相对于项目特定的全局地图坐标系，仅适用于全向移动机器人。<br>'TANGENTIAL'：与边的轨迹相切。示例：全向移动机器人可以采用任意朝向角，而差动驱动移动机器人可能只能采用 0.0（向前）和 Pi（向后）两种朝向角。<br><br>默认值：'TANGENTIAL'。
*direction* |  | string | 为采用物理线导引导航的移动机器人设置路口方向；允许值应针对具体移动机器人预先定义。<br>示例：`"left"`、`"right"`、`"straight"`、`"580 Hz"`。
*reachOrientationBeforeEntering* |  | boolean | 该参数仅适用于全向移动机器人。`true`：进入边之前应达到该边要求的朝向角。<br>`false`：移动机器人可以在边上旋转到所需朝向角。<br>默认值：`false`。
*maximumRotationSpeed* | rad/s | float64 | 最大旋转速度。<br><br>可选：<br>未设置时不限制。
***trajectory*** |  | JSON object | 以 NURBS 表示该边轨迹的 JSON 对象。<br>定义移动机器人应在边的起始节点与结束节点之间行驶的路径。<br><br>可选：<br>如果移动机器人无法处理轨迹或会自行规划轨迹，则可以省略。
*length* | m | float64 | 从起始节点到结束节点的路径长度。<br><br>可选：<br>线导引移动机器人使用该值在到达停止位置之前降低速度。
***corridor*** |  | JSON object | 定义移动机器人可以偏离轨迹的边界，例如为了避开障碍物。<br>
**actions [action]**<br><br><br> } |  | array | 要在边上执行的动作数组。<br>如果无需执行动作，则为空数组。<br>由边触发的动作仅在移动机器人遍历触发该动作的边期间处于活动状态。


对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**trajectory** { |  | JSON object |
*degree* |  | uint32 | 定义轨迹的 NURBS 曲线次数。<br><br>范围：[1 ... uint32.max]<br>默认值：1
***knotVector [float64]*** |  | array | NURBS 的节点值数组。<br>`knotVector` 的元素数量应恰好比 `controlPoints` 多 `degree` + 1。<br>首个和末尾节点值的重数都必须为 `degree` + 1（夹持 NURBS）。<br>除首个和末尾节点值之外，其他节点值的重数不得大于 `degree`（连续性）。<br><br>节点值范围：[0.0 ... 1.0]<br>默认值：节点值从 0.0 到 1.0 等距分布，首个和末尾节点值的重数为 `degree` + 1，其他节点值的重数均为 1（均匀节点矢量）。
**controlPoints [controlPoint]** |  | array | 定义 NURBS 控制点的 `controlPoint` 对象数组，明确包含起点和终点（夹持 NURBS）。<br>控制点数量至少应为 `degree` + 1。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**controlPoint** { |  | JSON object |
x | m | float64 | 项目特定坐标系中的 x 坐标。
y | m | float64 | 项目特定坐标系中的 y 坐标。
*weight* |  | float64 | 控制点在曲线上的权重。<br><br>范围：]0.0 ... float64.max]<br>默认值：1.0
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
***corridor*** { |  | JSON object |
leftWidth | m | float64 | 范围：[0.0 ... float64.max]<br>定义移动机器人轨迹左侧的走廊宽度，单位为米（见图 10）。
rightWidth | m | float64 | 范围：[0.0 ... float64.max]<br>定义移动机器人轨迹右侧的走廊宽度，单位为米（见图 10）。
*corridorReferencePoint* |  | string | 定义边界适用于移动机器人的运动学中心还是轮廓。如果未指定，边界适用于移动机器人的运动学中心。<br>枚举 {'KINEMATIC_CENTER', 'CONTOUR'}。
*releaseRequired* |  | boolean | 指示机器人是否应向车队控制请求批准的可选标志。<br>默认值：`false`。
*releaseLossBehavior*<br> } |  | string | 枚举 {'STOP', 'RETURN'}<br>定义走廊授权到期或被车队控制撤销时机器人应采取的行为。<br>'STOP'：移动机器人应停止并等待人工干预。<br>'RETURN'：移动机器人应返回它所偏离的边的预定义轨迹。<br>默认值：'STOP'。

### 7.3.1 动作参数格式

错误、信息和动作的参数设计为带有键值对的 JSON 对象数组。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
**actionParameter** { | JSON object | 指定动作的 `actionParameter`，例如 `deviceId`、`loadId` 或 `external triggers`。
key | string | 参数的键。
value <br><br><br>} | 以下之一：<br>array、<br>boolean、<br>number、<br>integer、<br>string、<br>object | 与该键对应的参数值。

动作 `someAction` 的 `actionParameter` 示例，其中包含 `stationType` 和 `loadType` 键值对：

    "actionParameters": [
      {"key": "stationType", "value": "floor"},
      {"key": "weight", "value": 8.5},
      {"key": "loadType", "value": "pallet_eu"}
    ]

采用 `"key": "actualKey", "value": "actualValue"` 结构是为了保持实现的通用性。`actualValue` 可以是任意 JSON 数据类型，例如数组、布尔值、整数、数字、字符串或对象。


## 7.4 `instantActions` 消息的实现

对象结构 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>协议头 ID 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。
**actions [action]** | array | 需要立即执行且不属于常规订单的动作数组。

对象 `action` 定义见 [7.3 订单消息的实现](#73-订单消息的实现)。


## 7.5 `responses` 消息的实现

| 对象结构/标识符 | 数据类型 | 描述 |
| --- | --- | --- |
| headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。 |
| timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。 |
| version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。 |
| manufacturer | string | 移动机器人制造商。 |
| serialNumber | string | 移动机器人的序列号。 |
| **responses [response]** | array | 响应对象数组。 |

| 对象结构/标识符 | 数据类型 | 描述 |
| --- | --- | --- |
| response<br> { | JSON object | 包含车队控制对特定请求的回答的对象。 |
| requestId | string | 在每台移动机器人的所有活动请求中保持唯一的标识符。 |
| grantType | enum | 枚举 {'GRANTED', 'QUEUED', 'REVOKED', 'REJECTED'}<br>'GRANTED'：车队控制已批准请求。<br>'QUEUED'：车队控制确认收到移动机器人的请求，但尚未授予许可；该请求已被加入某种队列。<br>'REVOKED'：车队控制撤销之前批准的请求。<br>'REJECTED'：车队控制拒绝请求。 |
| *leaseExpiry*<br><br> } | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。授权到期时间戳只能随批准请求的响应发送。 |


## 7.6 `zoneSet` 消息的实现

对象结构 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>协议头 ID 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。
**zoneSet** | JSON object | 区域集。

| 对象结构 | 数据类型 | 描述 |
| --- | --- | --- |
| zoneSet { | JSON object | 详细描述特定地图的区域集。 |
| mapId | string | 该区域集所描述地图的全局唯一标识符。 |
| zoneSetId | string | 区域集的全局唯一标识符。 |
| *zoneSetDescriptor* | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。 |
| **zones[zone]** <br> } | array | 区域对象数组。 |

单个区域对象具有以下结构：

| **对象结构** | **数据类型** | **描述** |
| --------------------- | ------------- | ------------------- |
| zone { | JSON object |  |
| zoneId | string | 本地（在区域集内）唯一标识符。 |
| zoneType | string | 枚举 {'BLOCKED', 'LINE_GUIDED', 'RELEASE', 'COORDINATED_REPLANNING', 'SPEED_LIMIT', 'ACTION', 'PRIORITY', 'PENALTY', 'DIRECTED', 'BIDIRECTED'}，区域类型见 [6.4.1 区域类型](#641-区域类型)。 |
| *zoneDescriptor* | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。 |
| **vertices [vertex]** | array | 按逆时针方向定义区域几何形状的顶点数组。 |
| *maximumSpeed* | float64 | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'SPEED_LIMIT' 区域需要该字段。 |
| ***entryActions[zoneAction]*** | array | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'ACTION' 区域需要该字段。 |
| ***duringActions[zoneAction]*** | array | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'ACTION' 区域需要该字段。 |
| ***exitActions[zoneAction]*** | array | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'ACTION' 区域需要该字段。 |
| *releaseLossBehavior* | string | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'RELEASE' 区域需要该字段。 |
| *priorityFactor* | float64 | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'PRIORITY' 区域需要该字段。 |
| *penaltyFactor* | float64 | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'PENALTY' 区域需要该字段。 |
| *direction* | float64 | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'DIRECTED' 和 'BIDIRECTED' 区域需要该字段。 |
| *directedLimitation* | string | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'DIRECTED' 区域需要该字段。 |
| *bidirectedLimitation* | string | 仅 [6.4.1 区域类型](#641-区域类型) 中定义的 'BIDIRECTED' 区域需要该字段。 |
| } |  |  |

`zoneAction` 遵循动作的结构，但移动机器人自行生成 `actionId`。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**zoneAction** { |  | JSON object | 描述移动机器人能够执行的动作。
actionType |  | string | 表 4 第一列所述的动作名称。<br>用于标识动作的功能。
*actionDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
blockingType |  | string | 枚举 {'NONE', 'SINGLE', 'SOFT', 'HARD'}：<br>'NONE'：允许行驶和执行其他动作；<br>'SINGLE'：允许行驶，但不允许执行其他动作；<br>'SOFT'：允许执行其他动作，但不允许行驶；<br>'HARD'：该动作是此时唯一允许执行的动作。
***actionParameters [actionParameter]*** |  | array | 指定动作的 `actionParameter` 对象数组，例如 `deviceId`、`loadId`、`external triggers`。<br><br>实现示例见 [7.3.1 动作参数格式](#731-动作参数格式)。
*retriable* <br> } |  | boolean | `true`：动作失败时可以进入 'RETRIABLE' 状态。<br>`false`：动作失败后直接进入 'FAILED' 状态。<br>默认值：`false`。

每个区域对象的形状由通过顶点传递的多边形定义。顶点少于三个的区域无效，应予以拒绝。该多边形视为闭合。只应使用简单多边形（即不存在相交边）。定义区域的顶点数组应以项目特定全局坐标系中的 x-y 坐标对列表提供，并按逆时针方向排列：

| **对象结构** | **数据类型** | **描述** |
| --------------------- | ------------- | ------------------- |
| vertex { | JSON object |  |
| x | float64 | 项目特定坐标系中的 x 坐标。 |
| y<br>} | float64 | 项目特定坐标系中的 y 坐标。 |


## 7.7 `connection` 消息的实现

标识符 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。
connectionState | string | 枚举 {'ONLINE', 'OFFLINE', 'HIBERNATING', 'CONNECTION_BROKEN'}<br><br>'ONLINE'：移动机器人与代理之间的连接处于活动状态。<br><br>'OFFLINE'：移动机器人与代理之间的连接已通过协调方式离线。<br><br>'HIBERNATING'：移动机器人进入低功耗状态并停止发送状态消息。与 MQTT 代理的连接应保持活动。该模式用于节省电量或减少通信。移动机器人随后可以在收到指令时或通过已配置的唤醒机制转换为 'ONLINE'。<br><br>'CONNECTION_BROKEN'：移动机器人与代理之间的连接意外中断。


## 7.8 `state` 消息的实现

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
headerId |  | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp |  | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
version |  | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer |  | string | 移动机器人制造商。
serialNumber |  | string | 移动机器人的序列号。
***maps[map]*** |  | array | 当前存储在移动机器人上的地图对象数组。
***zoneSets[zoneSet]*** |  | array | 当前存储在移动机器人上的 `zoneSet` 对象数组。
orderId |  | string | 当前订单或上一个已完成订单的唯一标识。<br>`orderId` 会保留到收到新订单为止。<br>如果没有可用的上一个 `orderId`，则为空字符串（`""`）。
orderUpdateId |  | uint32 | 用于表明移动机器人已接受订单更新的订单更新标识符。<br>如果没有之前的 `orderUpdateId`，则为 `0`。
lastNodeId |  | string | 最后到达节点的节点 ID；如果移动机器人当前位于节点上，则为当前节点的 ID（例如 `"node7"`）。如果没有可用的 `lastNodeId`，则为空字符串（`""`）。
lastNodeSequenceId |  | uint32 | 最后到达节点的序列 ID；如果移动机器人当前位于节点上，则为当前节点的序列 ID。<br>仅当 `lastNodeId` 不是空字符串（`""`）时，该值才有效。如果 `lastNodeId` 是空字符串（`""`），则 `lastNodeSequenceId` 可以为任意值，并应予以忽略。
**nodeStates [nodeState]** |  | array | 需要遍历以完成订单的 `nodeState` 对象数组。<br>（空闲时为空数组）
**edgeStates [edgeState]** |  | array | 需要遍历以完成订单的 `edgeState` 对象数组。<br>（空闲时为空数组）
***plannedPath*** |  | JSON object | 以 NURBS 表示机器人当前活动订单中的一条路径。
***intermediatePath*** |  | JSON object | 表示移动机器人通过传感器能够感知到的距离较近航点的预计到达时间。
***mobileRobotPosition*** |  | JSON object | 移动机器人在地图上的当前位置。<br><br>可选：仅当移动机器人不具备自主定位能力时才可省略，例如线导引移动机器人。
***velocity*** |  | JSON object | 移动机器人在其自身坐标系中的速度。
***loads [load]*** |  | array | 移动机器人当前正在搬运的载荷。<br><br>可选：如果移动机器人无法确定载荷状态，则应完全省略该字段，不得将其报告为空数组。<br>如果移动机器人能够确定载荷状态，但数组为空，则视为移动机器人未装载载荷。
driving |  | boolean | `true`：表示移动机器人正在行驶（手动或自动）。其他运动（例如升降运动）不包括在内。<br>`false`：表示移动机器人未在行驶。
*paused* |  | boolean | `true`：移动机器人当前处于暂停状态，原因可能是按下了移动机器人上的物理按钮，也可能是收到即时动作。<br>移动机器人可以继续执行订单。<br><br>`false`：移动机器人当前未处于暂停状态。
*newBaseRequest* |  | boolean | `true`：移动机器人即将到达基础区末端；如果未收到新的基础区，它将降低速度。<br>该值用于触发车队控制发送新的基础区。<br><br>`false`：不需要更新基础区。
***zoneRequests [zoneRequest]*** |  | array | 移动机器人上当前处于活动状态的 `zoneRequest` 对象数组。<br>如果没有活动的区域请求，则为空数组。
***edgeRequests [edgeRequest]*** |  | array | 移动机器人上当前处于活动状态的 `edgeRequest` 对象数组。<br>如果没有活动的边请求，则为空数组。
*distanceSinceLastNode* | m | float64 | 线导引移动机器人用于指示其驶过 `lastNodeId` 所标识节点之后的距离。<br>距离以米为单位。
**actionStates [actionState]** |  | array | 包含当前订单中所有动作状态的数组。只要当前订单仍处于活动状态，这些动作状态就会保留；接受新订单时将清除这些状态。<br>其中可能包括之前节点上仍在进行的动作。<br><br>动作完成时，将发布更新后的状态消息，其中 `actionStatus` 设置为 'FINISHED'，并在适用时包含相应的 `actionResult`。
**instantActionStates [actionState]** |  | array | 移动机器人收到的所有即时动作状态组成的数组。即时动作状态应保留在状态消息中，直到执行 `clearInstantActions` 动作。如果列表过长而难以管理，机器人可以报告 `errorType` 为 'INSTANT_ACTION_STATES_FULL'、`errorLevel` 为 'URGENT' 的错误。建议车队控制在实际可行时尽快清除此列表。
***zoneActionStates [actionState]*** |  | array | 所有已处于终止状态或当前正在运行的区域动作状态组成的数组；是否共享尚未执行的动作可由实现选择。区域动作状态应保留在状态消息中，直到执行 `clearZoneActions` 动作。如果支持动作区域，则必须提供该字段。如果列表过长而难以管理，机器人可以报告 `errorType` 为 'ZONE_ACTION_STATES_FULL'、`errorLevel` 为 'URGENT' 的错误。建议车队控制在实际可行时尽快清除此列表。
**powerSupply** |  | JSON object | 包含所有与电源相关的信息。
operatingMode |  | string | 枚举 {'STARTUP', 'AUTOMATIC', 'SEMIAUTOMATIC', 'INTERVENED', 'MANUAL', 'SERVICE', 'TEACH_IN'}<br>更多信息请参见 [6.6.6 操作模式](#666-操作模式) 中的表格。
**errors [error]** |  | array | 错误对象数组。<br>移动机器人的所有活动错误都应存在于数组中。<br>空数组表示移动机器人没有活动错误。
***information [info]*** |  | array | `info` 对象数组。<br>空数组表示移动机器人没有信息。<br>该字段应仅用于可视化或调试，不得用于车队控制逻辑。
**safetyState** |  | JSON object | 包含所有安全相关信息。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**map** { |  | JSON object |
mapId |  | string | 地图 ID，该地图描述移动机器人工作区中的特定区域。
mapVersion |  | string | 地图的版本。
mapStatus |  | string | 枚举 {'ENABLED', 'DISABLED'}<br>'ENABLED'：表示移动机器人当前正在使用该地图。具有相同 `mapId` 的地图中，最多只能有一个地图版本的状态设置为 'ENABLED'。<br>'DISABLED'：表示移动机器人当前未启用该地图版本，因此可根据请求启用或删除该版本。
*mapDescriptor*<br>} |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**zoneSet** { |  | JSON object |
zoneSetId |  | string | 区域集的唯一标识符。
mapId |  | string | 对应地图的标识符。
zoneSetStatus<br>} |  | string | 枚举 {'ENABLED', 'DISABLED'}<br>'ENABLED'：表示移动机器人当前正在使用该区域集。每张地图最多只能有一个区域集的状态设置为 'ENABLED'。<br>'DISABLED'：表示移动机器人当前未启用该区域集，因此车队控制可以启用或删除该区域集。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**nodeState** { |  | JSON object |
nodeId |  | string | 节点的唯一标识符。<br>同一节点可以在一个状态消息中被多次引用。`sequenceId` 用于区分穿越序列。
sequenceId |  | uint32 | 节点的 `sequenceId`，用于区分具有相同 `nodeId` 的多个节点。
*nodeDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
released |  | boolean | `true` 表示该节点是基础区的一部分。<br>`false` 表示该节点属于预测区。
***nodePosition***<br><br>} |  | JSON object | 节点位置。<br>可选：车队控制已经拥有此信息，也可以额外发送该字段，例如用于调试。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**nodePosition** { |  | JSON object | 在项目特定坐标系中定义地图上的位置。<br>每个楼层都有自己的地图。<br>所有地图都应使用同一个项目特定的全局原点。
x | m | float64 | 地图上相对于项目特定坐标系的 x 位置。<br>精度取决于具体实现。
y | m | float64 | 地图上相对于项目特定坐标系的 y 位置。<br>精度取决于具体实现。
*theta* | rad | float64 | 范围：[-Pi ... Pi]<br><br>移动机器人在节点处应达到的绝对朝向角；达到该朝向角后，该节点才被视为已遍历。<br>可选：移动机器人可以自行规划路径。<br>如果定义了该值，移动机器人应在此节点达到 `theta` 指定的朝向角。<br>如果前一条边不允许旋转，移动机器人应在节点上旋转。<br>如果后一条边定义了不同的朝向角且不允许在边上旋转，则移动机器人应在进入该边之前，在节点上旋转到后一条边要求的朝向角。
mapId |  | string | 该位置所引用地图的唯一标识符。<br>每张地图都具有同一个项目特定的全局坐标原点。<br>当移动机器人使用电梯（例如从出发楼层前往目标楼层）时，它会从出发楼层的地图上消失，并出现在目标楼层地图中对应的电梯节点上。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**edgeState** { |  | JSON object |
edgeId |  | string | 边的唯一标识符。<br>同一条边可以在一个状态消息中多次被引用。`sequenceId` 用于区分穿越序列。
sequenceId |  | uint32 | 边的 `sequenceId`，用于区分具有相同 `edgeId` 的多条边。
*edgeDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
released |  | boolean | `true` 表示该边是基础区的一部分。<br>`false` 表示边是预测区的一部分。
***trajectory***<br><br>} |  | JSON object | 报告布局中预先定义或作为订单一部分为该边发送的轨迹。<br><br>轨迹应以 NURBS 形式传输，其定义见 [7.3 订单消息的实现](#73-订单消息的实现)。<br><br>轨迹段从移动机器人进入边的位置开始，到移动机器人报告已遍历结束节点的位置终止。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**plannedPath** { |  | JSON object |
**trajectory** |  | JSON object | 轨迹应以 NURBS 形式传输，定义见 [7.3 订单消息的实现](#73-订单消息的实现)。
***traversedNodes[nodeId]*** |  | array | 当前执行订单中传达的 `nodeId` 数组；这些节点将在共享的规划路径中被遍历。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**trajectory** { |  | JSON object |
*degree* |  | uint32 | 定义轨迹的 NURBS 曲线次数。<br><br>范围：[1 ... uint32.max]<br>默认值：1
***knotVector [float64]*** |  | array | NURBS 的节点值数组。<br>`knotVector` 的元素数量应恰好比 `controlPoints` 多 `degree` + 1。<br>首个和末尾节点值的重数都必须为 `degree` + 1（夹持 NURBS）。<br>除首个和末尾节点值之外，其他节点值的重数不得大于 `degree`（连续性）。<br><br>节点值范围：[0.0 ... 1.0]<br>默认值：节点值从 0.0 到 1.0 等距分布，首个和末尾节点值的重数为 `degree` + 1，其他节点值的重数均为 1（均匀节点矢量）。
**controlPoints [controlPoint]** |  | array | 定义 NURBS 控制点的 `controlPoint` 对象数组，明确包含起点和终点（夹持 NURBS）。<br>控制点数量至少应为 `degree` + 1。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**controlPoint** { |  | JSON object |
x | m | float64 | 项目特定坐标系中的 x 坐标。
y | m | float64 | 项目特定坐标系中的 y 坐标。
*weight* |  | float64 | 控制点在曲线上的权重。<br><br>范围：]0.0 ... float64.max]<br>默认值：1.0
} |  |  |


对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**intermediatePath** { |  | JSON object |
**polyline[waypoint]** |  | array | 折线各线段端点的数组。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**waypoint** { |  | JSON object | 所定义折线中某一线段的端点。
x | m | float64 | 项目特定坐标系中的 x 坐标。
y | m | float64 | 项目特定坐标系中的 y 坐标。
*theta* | rad | float64 | 移动机器人在项目特定坐标系中的绝对朝向角。<br>范围：[-Pi ... Pi]<br>
eta |  | string | 预计到达或经过时间。ETA 采用 `timestamp` 格式（ISO 8601、UTC），即 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。
} |  |  |


对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**mobileRobotPosition** { |  | JSON object | 在项目特定坐标系中定义地图上的位置。每个楼层都有自己的地图。
x | m | float64 | 地图上相对于项目特定坐标系的 x 位置。<br>精度取决于具体实现。
y | m | float64 | 地图上相对于项目特定坐标系的 y 位置。<br>精度取决于具体实现。
theta | rad | float64 | 范围：[-Pi ... Pi]<br><br>移动机器人的朝向角。
mapId |  | string | 该位置所引用地图的唯一标识符。<br><br>每张地图具有相同的坐标原点。<br>当移动机器人乘坐电梯从出发楼层前往目标楼层时，它会离开出发楼层的地图，并出现在目标楼层地图中对应的电梯节点上。
localized |  | boolean | `true`：移动机器人已定位，`x`、`y` 和 `theta` 的值可信。<br>`false`：移动机器人未定位，`x`、`y` 和 `theta` 的值不可信。<br>只有当移动机器人无法再确定自身位置时，才应将该状态改为 `false`。移动机器人应通过错误（`errorType` = 'LOCALIZATION_ERROR'，`errorLevel` = 'FATAL'）报告此状态。当该值为 `false` 时，移动机器人不得恢复自动行驶或继续执行当前订单。
*localizationScore* |  | float64 | 范围：[0.0 ... 1.0]<br>描述定位质量，因此可以被 SLAM 移动机器人用来描述当前位置信息的准确性。<br>0.0：最低可能置信度<br>1.0：最高置信度。<br>仅用于日志记录和可视化。
*deviationRange* | m | float64 | 位置偏差范围的值，单位为米。<br>仅用于日志记录和可视化。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**velocity** { |  | JSON object |
*vx* | m/s | float64 | 移动机器人沿 x 轴方向的速度。
*vy* | m/s | float64 | 移动机器人沿 y 轴方向的速度。
*omega*<br>} | rad/s | float64 | 移动机器人绕 z 轴的角速度。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**load** { |  | JSON object |
*loadId* |  | string | 载荷的唯一标识符（例如条形码或 RFID）。<br><br>如果移动机器人能够识别载荷，但尚未完成识别，则该字段为空。<br><br>如果移动机器人无法识别载荷，则该字段可选。
*loadType* |  | string | 载荷类型。
*loadPosition* |  | string | 指定当前载荷所在的载荷搬运或承载单元，适用于移动机器人具有多个载荷承载点或位置的情况。<br><br>示例：`"front"`、`"back"`、`"positionC1"` 等。<br><br>对于只有一个 `loadPosition` 的移动机器人，该字段可选。
***boundingBoxReference*** |  | JSON object | 包围盒位置的参考点。<br>参考点始终位于包围盒底面中心（高度 = 0），并以移动机器人坐标系中的坐标描述。
***loadDimensions*** |  | JSON object | 载荷包围盒的尺寸（以米为单位）。
*weight*<br>} | kg | float64 | 范围：[0.0 ... float64.max]<br><br>载荷的绝对重量，以千克为单位。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**boundingBoxReference** { |  | JSON object | 包围盒位置的参考点。<br>参考点始终位于包围盒底面中心（高度 = 0），并以移动机器人坐标系中的坐标描述。
x | m | float64 | 参考点的 x 坐标。
y | m | float64 | 参考点的 y 坐标。
z | m | float64 | 参考点的 z 坐标。
*theta*<br> } |  | float64 | 载荷包围盒的朝向角。<br>该字段对牵引车、拖车列车等尤为重要。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**loadDimensions** { |  | JSON object | 载荷包围盒的尺寸（以米为单位）。
length | m | float64 | 载荷包围盒的绝对长度（沿移动机器人坐标系的 x 轴）。
width | m | float64 | 载荷包围盒的绝对宽度（沿移动机器人坐标系的 y 轴）。
*height* <br>} | m | float64 | 载荷包围盒的绝对高度。<br><br>可选：只有已知值时才设定。

| **对象结构** | **数据类型** | **描述** |
| --- | --- | --- |
| zoneRequest <br> { | JSON object | 移动机器人向车队控制发送的区域请求信息。 |
| requestId | string | 在每台移动机器人的所有活动请求中保持唯一的标识符。 |
| requestType | string | 枚举 {'ACCESS', 'REPLANNING'}<br>指定区域请求的类型：'ACCESS' 表示请求进入区域，'REPLANNING' 表示请求使用或重新规划轨迹。 |
| zoneId | string | 在本地（区域集内）唯一标识符，指向请求所关联的区域。 |
| zoneSetId | string | `zoneId` 仅在所属的 `zoneSet` 内唯一，因此请求必须同时包含 `zoneSetId`。 |
| requestStatus | string | 枚举 {'REQUESTED', 'GRANTED', 'REVOKED', 'EXPIRED'}<br>发起请求时设置为 'REQUESTED'；收到车队控制的响应或更新后，设置为 'GRANTED' 或 'REVOKED'；许可有效期到期后，设置为 'EXPIRED'。 |
| ***trajectory***<br> } | object | 仅用于 'COORDINATED_REPLANNING' 请求的可选字段，包含请求穿越该区域的规划轨迹。 |

| **对象结构** | **数据类型** | **描述** |
| --- | --- | --- |
| edgeRequest <br> { | JSON object | 移动机器人向车队控制发送的走廊使用请求信息。 |
| requestId | string | 在每台移动机器人的所有活动请求中保持唯一的标识符。 |
| requestType | enum | 枚举 {'CORRIDOR'}<br>指定请求类型。当移动机器人请求在已定义的工作空间内偏离预定义轨迹时，该字段设置为 'CORRIDOR'。 |
| edgeId | string | 全局唯一标识符，引用请求所关联的边。 |
| sequenceId | uint32 | 订单中该边的序列编号，与 `edgeId` 一起用于唯一标识订单中引用的边。 |
| requestStatus <br><br> } | enum | 枚举 {'REQUESTED', 'GRANTED', 'REVOKED', 'EXPIRED'}<br>发起请求时设置为 'REQUESTED'；收到车队控制的响应或更新后，设置为 'GRANTED' 或 'REVOKED'；许可有效期到期后，设置为 'EXPIRED'。|

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**actionState** { |  | JSON object |
actionId |  | string | 动作的唯一标识符。
*actionType* |  | string | 动作类型。<br><br>可选：仅用于信息展示或可视化。车队控制已知道订单中下发的动作类型。
*actionDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
actionStatus |  | string | 枚举 {'WAITING', 'INITIALIZING', 'RUNNING', 'PAUSED', 'RETRIABLE', 'FINISHED', 'FAILED'}<br><br>参见 [6.6.9 动作状态](#669-动作状态)部分。
*actionResult*<br>} |  | string | 结果描述，例如 RFID 读取的结果。<br><br>错误通过 `errors` 传输。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**powerSupply** { |  | JSON object |
stateOfCharge | % | float64 | 范围：[0 ... 100]<br><br>移动机器人的荷电状态。对于永久供电的移动机器人，该字段应为 100。
*batteryVoltage* | V | float64 | 电池电压。
*batteryCurrent* | A | float64 | 电池电流。
*batteryHealth* | % | int8 | 范围：[0 ... 100]<br><br>描述电池健康状况的状态。
charging |  | boolean | `true`：正在充电。<br>`false`：移动机器人当前未充电。仅当机器人可以接收订单时，才应报告为 `false`。
*range*<br>} | m | uint32 | 范围：[0 ... uint32.max]<br><br>当前电量状态下的预计行驶距离。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**error** { |  | JSON object |
errorType |  | string | 错误类型，可扩展枚举，包括以下预定义值<br>枚举 {'UNSUPPORTED_PARAMETER', 'NO_ORDER_TO_CANCEL', 'VALIDATION_FAILURE', 'INVALID_ORDER_ACTION', 'OUTDATED_ORDER_UPDATE', 'SAME_ORDER_UPDATE_ID', 'ORDER_UPDATE_FOLLOWING_CANCEL', 'OUTSIDE_OF_CORRIDOR', 'INSUFFICIENT_MEMORY', 'DUPLICATE_MAP', 'BLOCKED_ZONE_VIOLATION', 'DUPLICATE_ZONE_SET', 'RELEASE_LOSS_HANDLING', 'RELEASE_LOST', 'ZONE_ACTION_CONFLICT', 'NODE_UNREACHABLE', 'LOCALIZATION_ERROR', 'OTHER_ORDER_ACTIVE', 'START_NODE_OUT_OF_RANGE', 'MOBILE_ROBOT_NOT_AVAILABLE', 'UNKNOWN_MAP_ID', 'INSTANT_ACTION_STATES_FULL', 'ZONE_ACTION_STATES_FULL', ...}。
***errorReferences [errorReference]*** |  | array | 错误关联项数组，例如 `nodeId`、`edgeId`、`orderId` 或 `actionId`，用于提供与错误相关的更多信息。
*errorDescription* |  | string | 详尽的描述，提供错误的细节和可能原因。
***errorDescriptionTranslations [translation]*** |  | array | 错误描述的翻译数组。如果集合中未包含某种语言，则应使用 `errorDescription` 字段的值（如有）作为默认值。
*errorHint* |  | string | 提示如何处理或解决报告错误。
***errorHintTranslations [translation]*** |  | array | 错误提示的翻译数组。如果集合中未包含某种语言，则应使用 `errorHint` 字段的值（如有）作为默认值。
errorLevel<br> } |  | string | 枚举 {'WARNING', 'URGENT', 'CRITICAL', 'FATAL'}<br><br>'WARNING'：无需立即处理；移动机器人可以继续活动订单（如有），并接受订单更新或新订单。<br>'URGENT'：需要立即处理；移动机器人可以继续活动订单（如有），并接受订单更新或新订单。<br>'CRITICAL'：需要立即处理；移动机器人无法继续活动订单，但可以接受新订单。<br>'FATAL'：需要用户干预；移动机器人无法继续活动订单，也无法接受订单更新或新订单。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**errorReference** { |  | JSON object |
referenceKey |  | string | 指定错误关联项的类型，例如 `"nodeId"`、`"edgeId"`、`"orderId"` 或 `"actionId"`。
referenceValue<br>} |  | string | 与 `referenceKey` 对应的值，例如发生错误的节点 ID。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**translation** { |  | JSON object |
translationKey |  | string | 根据 ISO 639-1 指定翻译所使用的语言。
translationValue<br>} |  | string | 使用 `translationKey` 所指定语言的译文。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**info** { |  | JSON object |
infoType |  | string | 信息类型/名称。
***infoReferences [infoReference]*** |  | array | 引用数组。
*infoDescriptor* |  | string | 一个用户定义、人类可读的名称或描述符。此信息不得用于逻辑目的。
infoLevel<br>} |  | string | 枚举 {'DEBUG', 'INFO'}<br><br>'DEBUG'：用于调试。<br>'INFO'：用于可视化。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**infoReference** { |  | JSON object |
referenceKey |  | string | 指定引用类型（例如 `"headerId"`、`"orderId"`、`"actionId"` 等）。
referenceValue<br>} |  | string | 与引用键对应的值。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**safetyState** { |  | JSON object |
activeEmergencyStop |  | string | 枚举 {'MANUAL', 'REMOTE', 'NONE'}<br><br>定义已触发的紧急停止类型：<br>'MANUAL'：应在移动机器人上手动确认紧急停止。<br>'REMOTE'：应远程确认设施紧急停止。<br>'NONE'：未触发紧急停止。
fieldViolation<br>} |  | boolean | 防护区域受到侵入（例如由激光传感器或保险杠检测到）。<br>`true`：防护区域受到侵入。<br>`false`：防护区域未受到侵入。


## 7.9 `visualization` 消息的实现

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。 |
| timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。 |
| version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。 |
| manufacturer | string | 移动机器人制造商。 |
| serialNumber | string | 移动机器人的序列号。 |
| referenceStateHeaderId | uint32 | 此可视化消息所引用状态消息的协议头 ID。 |
| ***plannedPath*** | JSON object | 以 NURBS 表示机器人当前活动订单中的一条路径。 |
| ***intermediatePath*** | JSON object | 表示移动机器人能够通过传感器感知的近端航点，以及到达这些航点的预计时间。 |
| ***mobileRobotPosition*** | JSON object | 移动机器人在地图上的位置。 |
| ***velocity*** | JSON object | 移动机器人在机器人坐标系中的速度。 |

对象 `plannedPath`、`intermediatePath`、`mobileRobotPosition` 和 `velocity` 定义见 [7.8 state 消息的实现](#78-state-消息的实现)。


## 7.10 `factsheet` 消息的实现

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。 |
| timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.fffZ`（例如 `"2017-04-15T11:40:03.123Z"`）。 |
| version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。 |
| manufacturer | string | 移动机器人制造商。 |
| serialNumber | string | 移动机器人的序列号。 |
| **typeSpecification** | JSON object | 这些参数通常指定了移动机器人的类别和能力。 |
| **physicalParameters** | JSON object | 这些参数规定了移动机器人的基本物理属性。 |
| **protocolLimits** | JSON object | MQTT 通信中标识符、数组、字符串等的长度限制。 |
| **protocolFeatures** | JSON object | 支持的 VDA 5050 协议功能。 |
| **mobileRobotGeometry** | JSON object | 移动机器人几何的详细定义。 |
| **loadSpecification** | JSON object | 载荷能力的抽象说明。 |
| ***mobileRobotConfiguration*** | JSON object | 移动机器人当前软硬件版本的汇总，以及可选的网络信息。 |

#### `typeSpecification`

该 JSON 对象描述了移动机器人类型的一般性质。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| seriesName | string | 制造商指定的移动机器人系列名称，为自由文本。 |
| *seriesDescription* | string | 移动机器人型号系列的人类可读描述，为自由文本。 |
| mobileRobotKinematics | string | 移动机器人运动学类型的简化描述。<br>可扩展枚举：{'DIFFERENTIAL', 'OMNIDIRECTIONAL', 'THREE_WHEEL', ...}<br>'DIFFERENTIAL'：差动驱动；<br>'OMNIDIRECTIONAL'：全向移动机器人；<br>'THREE_WHEEL'：三轮驱动的移动机器人或具有类似运动学的移动机器人。 |
| mobileRobotClass | string | 移动机器人类别的简化描述。<br>可扩展枚举：{'FORKLIFT', 'CONVEYOR', 'TUGGER', 'CARRIER', ...}<br>'FORKLIFT'：叉车；<br>'CONVEYOR'：带输送带的移动机器人；<br>'TUGGER'：牵引式移动机器人；<br>'CARRIER'：载荷承载型移动机器人，可带或不带升降单元。 |
| maximumLoadMass | float64 | [kg] 最大可装载质量。 |
| localizationTypes | array of string | 定位类型的简化描述。<br>可扩展枚举：{'NATURAL', 'REFLECTOR', 'RFID', 'DMC', 'SPOT', 'GRID', ...}<br>'NATURAL'：自然地标；<br>'REFLECTOR'：激光反射器；<br>'RFID'：RFID 标签；<br>'DMC'：数据矩阵码；<br>'SPOT'：磁点；<br>'GRID'：磁栅格。 |
| navigationTypes | array of string | 移动机器人支持的路径规划类型数组，按优先级排序。<br>可扩展枚举：{'PHYSICAL_LINE_GUIDED', 'VIRTUAL_LINE_GUIDED', 'FREELY_NAVIGATING', ...}<br>'PHYSICAL_LINE_GUIDED'：无需路径规划，移动机器人沿物理安装路径前进；<br>'VIRTUAL_LINE_GUIDED'：移动机器人沿固定（虚拟）路径前进；<br>'FREELY_NAVIGATING'：移动机器人自行规划路径。 |
| *supportedZones* | array of string | 移动机器人支持的区域类型数组。<br>枚举 {'BLOCKED', 'LINE_GUIDED', 'RELEASE', 'COORDINATED_REPLANNING', 'SPEED_LIMIT', 'ACTION', 'PRIORITY', 'PENALTY', 'DIRECTED', 'BIDIRECTED'}。 |

#### `physicalParameters`

该 JSON 对象描述了移动机器人的物理属性。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| minimumSpeed | float64 | [m/s] 移动机器人的最小可控连续速度。 |
| maximumSpeed | float64 | [m/s] 移动机器人的最高速度。 |
| *minimumAngularSpeed* | float64 | [rad/s] 移动机器人的最小可控连续旋转速度。 |
| *maximumAngularSpeed* | float64 | [rad/s] 移动机器人的最大旋转速度。 |
| maximumAcceleration | float64 | [m/s²] 最大载荷时的最大加速度。 |
| maximumDeceleration | float64 | [m/s²] 最大载荷时的最大减速度。 |
| minimumHeight | float64 | [m] 移动机器人的最低高度。 |
| maximumHeight | float64 | [m] 移动机器人的最大高度。 |
| width | float64 | [m] 移动机器人的宽度。 |
| length | float64 | [m] 移动机器人的长度。 |

#### `protocolLimits`

该 JSON 对象描述了移动机器人的协议限制。
如果参数未定义或设置为 0，则该参数没有显式限制。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| **maximumStringLengths** { | JSON object | 字符串的最大长度。 |
| &emsp;*maximumMessageLength* | uint32 | MQTT 消息的最大长度。 |
| &emsp;*maximumTopicSerialLength* | uint32 | MQTT 主题中序列号部分的最大长度。<br><br>受影响的参数：<br>`order.serialNumber`<br>`instantActions.serialNumber`<br>`state.serialNumber`<br>`visualization.serialNumber`<br>`connection.serialNumber`<br>`zoneSet.serialNumber`<br>`responses.serialNumber` |
| &emsp;*maximumTopicElementLength* | uint32 | MQTT 主题中所有其他部分的最大长度。<br><br>受影响的参数：<br>`order.timestamp`<br>`order.version`<br>`order.manufacturer`<br>`instantActions.timestamp`<br>`instantActions.version`<br>`instantActions.manufacturer`<br>`state.timestamp`<br>`state.version`<br>`state.manufacturer`<br>`visualization.timestamp`<br>`visualization.version`<br>`visualization.manufacturer`<br>`connection.timestamp`<br>`connection.version`<br>`connection.manufacturer`<br>`zoneSet.timestamp`<br>`zoneSet.version`<br>`zoneSet.manufacturer`<br>`responses.timestamp`<br>`responses.version`<br>`responses.manufacturer` |
| &emsp;*maximumIdLength* | uint32 | ID 字符串的最大长度。<br><br>受影响参数：<br>`order.orderId`<br>`node.nodeId`<br>`nodePosition.mapId`<br>`action.actionId`<br>`edge.edgeId`<br>`map.mapId`<br>`zoneSet.zoneSetId`<br>`zone.zoneId`<br>`zoneRequest.requestId`<br>`edgeRequest.requestId` |
| &emsp;*idNumericalOnly* | boolean | 如果为 `true`，包含 ID 的参数应仅包含数字。 |
| &emsp;*maximumLoadIdLength* | uint32 | `loadId` 字符串的最大长度。 |
| } |  |  |
| **maximumArrayLengths** { | JSON object | 数组的最大长度。 |
| &emsp;*order.nodes* | uint32 | 移动机器人可处理的每个订单的最大节点数。 |
| &emsp;*order.edges* | uint32 | 移动机器人可处理的每个订单的最大边数。 |
| &emsp;*node.actions* | uint32 | 移动机器人可处理的每个节点的最大动作数。 |
| &emsp;*edge.actions* | uint32 | 移动机器人可处理的每条边的最大动作数。 |
| &emsp;*action.actionsParameters* | uint32 | 移动机器人可处理的每个动作的最大参数数。 |
| &emsp;*instantActions* | uint32 | 移动机器人可处理的每条消息中的最大即时动作数。 |
| &emsp;*trajectory.knotVector* | uint32 | 移动机器人可处理的每条轨迹中节点值（knot）的最大数量。 |
| &emsp;*trajectory.controlPoints* | uint32 | 移动机器人可处理的每条轨迹的最大控制点数。 |
| &emsp;*zoneSet.zones* | uint32 | 移动机器人可处理的每个 `zoneSet` 的最大区域数。 |
| &emsp;*state.nodeStates* | uint32 | 移动机器人可发送的最大 `nodeStates` 数，即移动机器人基础区的最大节点数。 |
| &emsp;*state.edgeStates* | uint32 | 移动机器人可发送的最大 `edgeStates` 数，即移动机器人基础区的最大边数。 |
| &emsp;*state.loads* | uint32 | 移动机器人发送的载荷对象最大数量。 |
| &emsp;*state.actionStates* | uint32 | 移动机器人发送的 `actionStates` 对象最大数量。 |
| &emsp;*state.instantActionStates* | uint32 | 移动机器人发送的 `instantActionStates` 对象最大数量。 |
| &emsp;*state.zoneActionStates* | uint32 | 移动机器人发送的 `zoneActionStates` 对象最大数量。 |
| &emsp;*state.errors* | uint32 | 移动机器人在单一状态消息中发送的最大错误数。 |
| &emsp;*state.information* | uint32 | 移动机器人在一条状态消息中发送的最大信息条目数。 |
| &emsp;*error.errorReferences* | uint32 | 移动机器人为每个错误发送的最大错误引用数。 |
| &emsp;*information.infoReferences* | uint32 | 移动机器人为每条信息发送的最大信息引用数。 |
| } |  |  |
| **timing** { | JSON object | 时间信息。 |
| &emsp;minimumOrderInterval | float32 | [s] 向移动机器人发送订单消息的最小间隔。 |
| &emsp;minimumStateInterval | float32 | [s] 发送状态消息的最小间隔。 |
| &emsp;*defaultStateInterval* | float32 | [s] 发送状态消息的默认间隔，*如果未定义，则使用主文档中的默认值*。 |
| &emsp;*visualizationInterval* | float32 | [s] 向 `visualization` 主题发送消息的默认间隔。 |
| } |  |  |

#### `protocolFeatures`

该 JSON 对象定义了移动机器人支持的订单处理流程、动作和参数。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| **optionalParameters [optionalParameter]** | array | 移动机器人支持和/或要求的可选参数数组。<br>未在此列出的可选参数视为移动机器人不支持。 |
| **{** |  |  |
| &emsp;parameter | string | 可选参数的全名，例如 `"order.nodes.nodePosition.allowedDeviationTheta"`。 |
| &emsp;support | enum | 可选参数的支持类型，可取以下值：<br>'SUPPORTED'：按照规范支持该可选参数。<br>'REQUIRED'：移动机器人正常运行需要该可选参数。 |
| &emsp;*description*| string | 自由文本：可选参数的描述，例如：<ul><li>说明此移动机器人类型为何需要可选参数 `direction`，以及该参数可以包含哪些值。</li><li>参数 `nodeMarker` 应仅包含无符号整数。</li><li>NURBS 支持仅限于直线段和圆弧段。</li></ul>|
| **}** | | |
| **mobileRobotActions [mobileRobotAction]** | array | 此移动机器人支持的所有动作及其参数组成的数组，包括 VDA 5050 中规定的标准动作和制造商特定动作。 |
| **{** |  |  |
| &emsp;actionType | string | 与 `action.actionType` 对应的唯一动作类型。 |
| &emsp;*actionDescription* | string | 自由形式文本：动作描述。 |
| &emsp;actionScopes | array of enum | 可使用该动作类型的作用域数组。<br><br>'INSTANT'：可用作 `instantAction`。<br>'NODE'：可用于节点。<br>'EDGE'：可用于边。<br>'ZONE'：可用作区域动作。<br><br>例如：`['INSTANT', 'NODE']` |
| &emsp;***actionParameters [actionParameter]*** | array | 动作所具有的参数数组。<br>如果未定义，则该动作没有参数。<br>此处定义的 JSON 对象不同于 [7.3 订单消息的实现](#73-订单消息的实现) 中用于节点和边的 JSON 对象。 |
| &emsp;*{* |  |  |
| &emsp;&emsp;key | string | 参数的键字符串。 |
| &emsp;&emsp;valueDataType | enum | 值的数据类型，可取：'BOOL'、'NUMBER'、'INTEGER'、'STRING'、'OBJECT'、'ARRAY'。 |
| &emsp;&emsp;*description* | string | 自由形式文本：参数描述。 |
| &emsp;&emsp;*isOptional* | boolean | `true`：可选参数。 |
| &emsp;*}* |  |  |
| &emsp;*actionResult* | string | 自由形式文本：结果描述。 |
| &emsp;*blockingTypes* | array of enum | 用于定义动作的可能阻塞类型数组。<br>枚举 {'NONE', 'SINGLE', 'SOFT', 'HARD'}。 |
| &emsp;pauseAllowed | boolean | `true`：可以通过 `startPause` 暂停动作；`false`：无法暂停动作。 |
| &emsp;cancelAllowed | boolean | `true`：可以通过 `cancelOrder` 取消动作；`false`：无法取消动作。 |
| **}** |  |  |

#### `mobileRobotGeometry`

该 JSON 对象定义移动机器人的几何属性，例如轮廓和车轮的位置。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| ***wheelDefinitions [wheelDefinition]*** | array | 车轮定义数组，每个元素包含一个车轮的布置和几何信息。 |
| { |  |  |
| &emsp;type | string | 车轮类型。<br>可扩展枚举 {'DRIVE', 'CASTER', 'FIXED', 'MECANUM', ...}。 |
| &emsp;isActiveDriven | boolean | `true`：该车轮为主动驱动轮。 |
| &emsp;isActiveSteered | boolean | `true`：该车轮可主动转向。 |
| &emsp;**position** { | JSON object |  |
| &emsp;&emsp;x | float64 | [m] 移动机器人坐标系中的 x 位置。 |
| &emsp;&emsp;y | float64 | [m] 移动机器人坐标系中的 y 位置。 |
| &emsp;&emsp;*theta* | float64 | [rad] 移动机器人坐标系中车轮的朝向角。固定轮必须提供该字段。 |
| &emsp;} |  |  |
| &emsp;diameter | float64 | [m] 车轮的标称直径。 |
| &emsp;width | float64 | [m] 车轮的标称宽度。 |
| &emsp;*centerDisplacement* | float64 | [m] 车轮中心到旋转点的标称偏移量（脚轮需要）。<br>如果未定义该参数，则假定为 0。 |
| &emsp;*constraints* | string | 自由形式文本：可由制造商用来定义约束。 |
| } |  |  |
| ***envelopes2d** [**envelope2d**]* | array | 二维移动机器人包络曲线数组，例如空载和载货状态下的机械包络，以及不同速度情形下的安全防护区域。 |
| { |  |  |
| &emsp;envelope2dId | string | 包络曲线集合的标识符。 |
| &emsp;**vertices [vertex]** | array | 以多边形表示包络曲线，并应将其视为闭合多边形。只能使用简单多边形。 |
| &emsp;{ |  |  |
| &emsp;&emsp;x | float64 | [m] 多边形点的 x 位置。 |
| &emsp;&emsp;y | float64 | [m] 多边形点的 y 位置。 |
| &emsp;} |  |  |
| &emsp;*description* | string | 自由形式文本：包络曲线集合的描述。 |
| *}* |  |  |
| ***envelopes3d [envelope3d]*** | array | 三维移动机器人包络曲线数组。 |
| *{* |  |  |
| &emsp;envelope3dId | string | 包络曲线集合的标识符。 |
| &emsp;format | string | 数据格式，例如 `"DXF"`。 |
| &emsp;***data*** | JSON object | 三维包络曲线数据，其格式由 `format` 字段指定。 |
| &emsp;*url* | string | 下载三维包络线数据的协议和 URL 定义，例如 `"ftp://xxx.yyy.com/ac4dgvhoif5tghji"`。 |
| &emsp;*description* | string | 自由形式文本：包络线集合的描述。 |
| *}* |  |  |

#### `loadSpecification`

该 JSON 对象规定移动机器人的载荷搬运能力和支持的载荷类型。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| *loadPositions* | array of string | 移动机器人的载荷位置或载荷搬运装置数组。<br>该数组列出 `state.loads[].loadPosition` 字段的有效值，以及 `pick` 和 `drop` 动作中 `loadHandlingDevice` 参数的有效值。<br>*如果该数组不存在或为空，则移动机器人没有载荷搬运装置。* |
| ***loadSets [loadSet]*** | array | 移动机器人能够搬运的载荷集数组。 |
| { |  |  |
| &emsp;setName | string | 载荷集的唯一名称，例如 `"DEFAULT"`、`"SET1"` 等。 |
| &emsp;loadType | string | 载荷类型，例如 `"EPAL"`、`"XLT1200"` 等。 |
| &emsp;*loadPositions* | array of string | 该载荷集所适用的载荷位置或载荷搬运装置数组。<br>*如果该参数不存在或为空，则该载荷集适用于该移动机器人上的所有载荷搬运装置。* |
| &emsp;***boundingBoxReference*** | JSON object | 状态消息 `loads[]` 参数中定义的包围盒参考点。 |
| &emsp;***loadDimensions*** | JSON object | 状态消息 `loads[]` 参数中定义的载荷尺寸。 |
| &emsp;*maximumWeight* | float64 | [kg] 该载荷类型的最大重量。 |
| &emsp;*minimumLoadhandlingHeight* | float64 | [m] 搬运此类型和重量的载荷时允许的最小高度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*maximumLoadhandlingHeight* | float64 | [m] 搬运此类型和重量的载荷时允许的最大高度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*minimumLoadhandlingDepth* | float64 | [m] 此类型和重量的载荷允许的最小搬运深度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*maximumLoadhandlingDepth* | float64 | [m] 此类型和重量的载荷允许的最大搬运深度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*minimumLoadhandlingTilt* | float64 | [rad] 此类型和重量的载荷允许的最小搬运倾角。 |
| &emsp;*maximumLoadhandlingTilt* | float64 | [rad] 此类型和重量的载荷允许的最大搬运倾角。 |
| &emsp;*maximumSpeed* | float64 | [m/s] 此类型和重量的载荷所允许的最大速度。 |
| &emsp;*maximumAcceleration* | float64 | [m/s²] 此类型和重量的载荷所允许的最大加速度。 |
| &emsp;*maximumDeceleration* | float64 | [m/s²] 此类型和重量的载荷所允许的最大减速度。 |
| &emsp;*pickTime* | float64 | [s] 拾取载荷的大致时间。 |
| &emsp;*dropTime* | float64 | [s] 放下载荷的大致时间。 |
| &emsp;*description* | string | 自由文本：载荷搬运集的描述。 |
| } |  |  |

#### `mobileRobotConfiguration`

该 JSON 对象详细说明移动机器人当前的软件和硬件版本，并提供网络信息摘要。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| ***versions[versionInfo]*** | array | 包含软硬件信息的键值对对象数组。 |
| *{* |  |  |
| &emsp;key | string | 所用软件或硬件版本的键（例如 `"softwareVersion"`）。 |
| &emsp;value | string | 与该键对应的版本（例如 `"v1.12.4-beta"`）。 |
| *}* |  |  |
| ***network*** { | JSON object | 移动机器人网络连接的信息。在移动机器人运行期间，不得更新所列信息。 |
| &emsp;*dnsServers* | array of string | 移动机器人使用的域名服务器（DNS）数组。 |
| &emsp;*ntpServers* | array of string | 移动机器人使用的网络时间协议（NTP）服务器数组。 |
| &emsp;*localIpAddress* | string | 预先分配、用于与 MQTT 代理通信的 IP 地址。注意，运行期间不应修改该 IP 地址。 |
| &emsp;*netmask* | string | 网络配置中与本地 IP 地址对应的子网掩码。 |
| &emsp;*defaultGateway* | string | 移动机器人使用的、与本地 IP 地址对应的默认网关。 |
| *}* |  |  |
| ***batteryCharging*** { | JSON object | 关于电池充电参数的信息。 |
| &emsp;*criticalLowChargingLevel* | float64 | 指定临界电量百分比。当电量小于或等于该值时，车队控制只应发送命令移动机器人驶向充电站的订单。 |
| &emsp;*maximumDesiredChargingLevel* | float64 | 指定最大期望电量（百分比）。 |
| &emsp;*minimumDesiredChargingLevel* | float64 | 指定最小期望电量（百分比）。 |
| &emsp;*minimumChargingTime* | uint32 | 指定期望的最短充电时间（秒）。 |
| *}* |  |  |

# 参考文献

文件 | 版本 | 描述
--- | --- | ---
ISO 3691-4 | 2023 年 12 月 | 工业卡车安全要求与验证——第四部分：无人驾驶卡车及其系统
ISO 9787 | 2013 年 5 月 | 机器人与机器人装置：坐标系与运动命名法
ISO 639 | 2023 年 11 月 | 用于表示世界语言和语言群体的语言代码
ISO 8601 | 2019 年 2 月 | 日期和时间：信息交换的表示
LIF – 布局交换格式 | 2024 年 3 月 | LIF 是一种用于无人驾驶移动机器人系统集成方与（第三方）车队控制系统之间交换布局信息的格式。
