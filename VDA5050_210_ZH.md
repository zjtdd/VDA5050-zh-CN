![标志](./assets/logo.png)

# 移动机器人（AGV）与车队控制之间的通信接口

## VDA 5050

## 版本 2.1.0

![车队控制系统与移动机器人](./assets/csagv.png)



### 简介

无人驾驶运输系统（DTS）通信接口的定义。
本建议描述了内部物流流程中，中央车队控制与移动机器人（AGV）之间用于交换订单和状态数据的通信接口。



### 免责声明

以下说明旨在为实现移动机器人与车队控制之间的通信接口提供指导。这些说明供所有人自由使用，且不具约束力。
采用这些说明的一方应确保其在具体情况下得到正确应用。

采用方应考虑每次发布时适用的现有技术水平。
采用这些建议并不免除任何人对自身行为的责任。
本声明无意穷尽所有情况，也不构成对现行法律的准确解释。
本声明不能替代对相关政策、法律和法规的研究。
此外，还应考虑相应产品的特殊特性及其不同的可能用途。
所有人均自行承担相关风险。
VDA 以及参与制定或应用这些建议的各方均不承担责任。

如果您在应用这些建议时发现任何不准确之处或可能导致错误解释的内容，请立即通知 VDA，以便修正相关缺陷。

**出版商**
德国汽车工业协会（VDA）
Behrenstraße 35, 10117 Berlin,
德国
www.vda.de

**版权**
德国汽车工业协会（VDA）
仅允许在注明来源的情况下复制或以其他形式再现。

版本 2.1.0


## 目录

[1 前言](#1-前言)<br>
[2 文档目标](#2-文档目标)<br>
[3 范围](#3-范围)<br>
[3.1 其他适用文档](#31-其他适用文档)<br>
[4 要求与协议定义](#4-要求与协议定义)<br>
[5 通信流程与内容](#5-通信流程与内容)<br>
[6 协议规范](#6-协议规范)<br>
[6.1 表格符号与格式含义](#61-表格符号与格式含义)<br>
[6.1.1 可选字段](#611-可选字段)<br>
[6.1.2 允许的字符与字段长度](#612-允许的字符与字段长度)<br>
[6.1.3 字段、主题与枚举的表示法](#613-字段主题与枚举的表示法)<br>
[6.1.4 JSON 数据类型](#614-json-数据类型)<br>
[6.2 MQTT 连接处理、安全与 QoS](#62-mqtt-连接处理安全与-qos)<br>
[6.3 MQTT 主题层级](#63-mqtt-主题层级)<br>
[6.4 协议头](#64-协议头)<br>
[6.5 通信主题](#65-通信主题)<br>
[6.6 `order` 主题（从车队控制到移动机器人）](#66-order-主题从车队控制到移动机器人)<br>
[6.6.1 概念与逻辑](#661-概念与逻辑)<br>
[6.6.2 订单与订单更新](#662-订单与订单更新)<br>
[6.6.3 订单取消（由车队控制发起）](#663-订单取消由车队控制发起)<br>
[6.6.4 订单拒绝](#664-订单拒绝)<br>
[6.6.5 走廊](#665-走廊)<br>
[6.6.6 订单消息的实现](#666-订单消息的实现)<br>
[6.7 地图](#67-地图)<br>
[6.7.1 地图分发](#671-地图分发)<br>
[6.7.2 移动机器人状态中的地图](#672-移动机器人状态中的地图)<br>
[6.7.3 地图下载](#673-地图下载)<br>
[6.7.4 启用已下载的地图](#674-启用已下载的地图)<br>
[6.7.5 删除移动机器人上的地图](#675-删除移动机器人上的地图)<br>
[6.8 动作](#68-动作)<br>
[6.8.1 预定义动作的定义、参数、效果与范围](#681-预定义动作的定义参数效果与范围)<br>
[6.8.2 预定义动作的状态](#682-预定义动作的状态)<br>
[6.9 `instantActions` 主题（从车队控制到移动机器人）](#69-instantactions-主题从车队控制到移动机器人)<br>
[6.10 `state` 主题（从移动机器人到车队控制）](#610-state-主题从移动机器人到车队控制)<br>
[6.10.1 概念与逻辑](#6101-概念与逻辑)<br>
[6.10.2 通过节点和进入/离开边，以及动作触发](#6102-通过节点和进入离开边以及动作触发)<br>
[6.10.3 基础区请求](#6103-基础区请求)<br>
[6.10.4 信息](#6104-信息)<br>
[6.10.5 错误](#6105-错误)<br>
[6.10.6 状态消息的实现](#6106-状态消息的实现)<br>
[6.11 动作状态](#611-动作状态)<br>
[6.12 动作阻塞类型与顺序](#612-动作阻塞类型与顺序)<br>
[6.13 `visualization` 主题](#613-visualization-主题)<br>
[6.14 `connection` 主题](#614-connection-主题)<br>
[6.15 `factsheet` 主题](#615-factsheet-主题)<br>
[6.15.1 信息表 JSON 结构](#6151-信息表-json-结构)<br>
[7 最佳实践](#7-最佳实践)<br>
[7.1 错误关联项](#71-错误关联项)<br>
[7.2 参数格式](#72-参数格式)<br>
[8 术语表](#8-术语表)<br>
[8.1 定义](#81-定义)<br>


# 1 前言

本接口由德国汽车工业协会（Verband der Automobilindustrie e.V.，VDA）与德国机械设备制造业联合会（Verband Deutscher Maschinen- und Anlagenbau e. V.，VDMA）共同制定。
双方的目标是建立一个普遍适用的接口。
接口变更建议应提交给 VDA，由 VDA 与 VDMA 共同评估，并在评估结果为肯定时纳入新版本。
非常欢迎通过 GitHub 为本文档作出贡献。
仓库地址为：https://github.com/vda5050/vda5050。


# 2 文档目标

本建议的目标是简化新移动机器人与现有车队控制系统的连接，并使不同制造商的移动机器人和传统系统（存量系统）能够在同一工作环境中并行运行。

本文档应定义车队控制与移动机器人之间的统一接口。
这应通过以下方面实现：

- 描述移动机器人与车队控制之间的通信标准，从而为使用相互协作的运输车辆将运输系统集成到连续过程自动化中奠定基础。
- 通过提高车辆自主性、采用过程模块和接口等方式提升灵活性，并尽可能摆脱僵化的事件控制命令链顺序。
- 通过较高的“即插即用”能力缩短实施时间，因为所需信息（例如订单信息）由中央服务提供并具有普遍有效性。在考虑职业安全要求的情况下，不同制造商的车辆应能够以相同的实施工作量投入运行。
- 通过对所有运输车辆、车辆型号和制造商采用统一、全局的协调方式及相应逻辑，降低系统复杂性并提高其“即插即用”能力。
- 通过在车辆控制层和协调层之间采用通用接口，提高对制造商的独立性。
- 通过在专有车队控制与上级车队控制之间实现纵向通信，集成专有 DTS 存量系统（见图 1）。

![图 1 DTS 存量系统的集成](./assets/concept_DTS.png)
>图 1 - DTS 存量系统的集成

为实现上述目标，本文档描述了移动机器人与车队控制之间用于传输订单和状态信息的接口。

移动机器人与车队控制之间运行所需的其他接口（例如在路径规划方面自由考虑特殊能力等），以及与其他系统组件通信的接口（例如外部外围设备、防火门等），暂不包含在本文档中。


# 3 范围

本建议包含移动机器人与车队控制之间通信的定义和最佳实践。
其目标是使具有不同特性的移动机器人（例如潜入牵引式或叉车式移动机器人）能够以统一语言与车队控制通信。
这为由同一个车队控制系统运行任意组合的移动机器人奠定了基础。
车队控制提供订单并协调移动机器人交通。

本接口以汽车行业生产物流和厂内物流的要求为基础。
按照制定的要求，内部物流涵盖物流部门的需求，即通过控制自由导航式车辆和导引式车辆，实现从收货、生产供给到发货的物流流程。

与自动化车辆不同，自主车辆可以基于相应的传感器系统和算法独立解决出现的问题，并能够相应地响应动态环境的变化或在短时间后适应这些变化。
自由导航式车辆和导引式车辆均可具备自主绕过障碍物等自主特性。
但只要路径规划由车辆自身完成，本文档就将其称为自由导航式车辆（见术语表）。
自主系统并非完全去中心化（群体智能），而是通过预定义规则具备确定的行为。

为实现可持续的解决方案，下文描述了一种结构可扩展的接口。
该接口应能完整覆盖对导引式车辆的车队控制。
自由导航式车辆可以集成到该结构中；为此所需的详细规范不属于本建议的内容。

集成专有存量系统可能需要对接口作出单独定义，这些定义不视为本建议的一部分。


## 3.1 其他适用文档

文档 | 版本 | 描述
--- | --- | ---
VDI 指南 2510 | 2005 年 10 月 | 无人驾驶运输系统（DTS）
VDI 指南 4451 第 7 分册 | 2005 年 10 月 | 无人驾驶运输系统（DTS）的兼容性 - DTS 车队控制
DIN EN ISO 3691-4 | 2023 年 12 月 | 工业车辆安全要求与验证 - 第 4 部分：无人驾驶工业车辆及其系统
LIF - 布局交换格式 | 2024 年 3 月 | 用于无人驾驶运输车辆的系统集成方与（第三方）车队控制系统之间交换轨道布局的格式定义。

# 4 要求与协议定义

通信接口旨在支持以下要求：

- 控制至少 1000 台车辆
- 支持集成具有不同自主程度的车辆
- 支持作出决策，例如有关路线选择或交叉口行为的决策

车辆应当定期传输其状态，或在状态发生变化时传输。

通信通过无线网络进行，并考虑连接中断和消息丢失的影响。

消息协议采用消息队列遥测传输（MQTT），并与 JSON 结构结合使用。
本协议开发期间测试了 MQTT 3.1.1，该版本是保证兼容性所需的最低版本。
MQTT 支持将消息分发到称为“主题”的子通道。
MQTT 网络中的参与方订阅这些主题，并接收与自身相关或感兴趣的信息。

JSON 结构支持在未来通过附加参数扩展协议。
参数使用英文描述，以确保协议在德语地区以外仍然易于阅读、理解和应用。


# 5 通信流程与内容

移动机器人的运行至少涉及以下参与方：

- 移动机器人系统的操作人员提供基本信息
- 车队控制负责组织和管理运行
- 移动机器人执行订单

图 2 描述了运行阶段的通信内容。
在实施或变更期间，需要手动配置移动机器人和车队控制。

![图 2 信息流结构](./assets/information_flow_VDA5050.png)
>图 2 - 信息流结构

在实施阶段，将搭建由车队控制和移动机器人组成的无人驾驶运输系统（DTS）。
操作人员定义必要的框架条件，并由其手动录入所需信息，或通过从其他系统导入的方式将信息存入车队控制。
这主要涉及以下内容：

- 路线定义：可以通过 CAD 导入将路线导入车队控制。
操作人员也可以在车队控制中手动建立路线。
路线可以是单行道，也可以限制特定车辆组通行（基于尺寸比例）等。
- 路网配置：
在路线中定义装卸站、充电站、外围环境（门、电梯、屏障）、等待位置、缓冲站等。
- 车辆配置：操作人员保存移动机器人的物理属性（尺寸、可用的载荷承载装置等）。
移动机器人应通过 `factsheet` 主题，以本文档 [6.15 `factsheet` 主题](#615-factsheet-主题)规定的具体方式传输这些信息。

上述路线和路网配置不属于本文档的内容。
它们构成了车队控制基于这些信息和待完成运输要求进行订单控制及行驶路线分配的基础。
随后，通过 MQTT 消息代理服务器将为移动机器人生成的订单传输给车辆。
移动机器人在执行订单的同时持续向车队控制报告其状态。
该过程同样使用 MQTT 消息代理服务器。

车队控制的功能包括：

- 向移动机器人分配订单
- 计算并引导移动机器人的路线（考虑各台移动机器人自身物理属性的限制，例如尺寸、机动性等）
- 检测并解决阻塞（“死锁”）
- 能源管理：充电订单可以中断运输订单
- 交通控制：缓冲路线和等待位置
- 环境的（临时）变化，例如开放特定区域或更改最高速度
- 与门、电梯等外围系统通信
- 检测并解决通信错误

移动机器人的功能包括：

- 定位
- 沿指定路线导航（导引式或自主式）
- 执行动作
- 持续传输车辆状态

此外，系统集成方在配置整个系统时应考虑以下事项（列表不完整）：

- 地图配置：应匹配车队控制与移动机器人的坐标系。
- 枢轴点：使用移动机器人的不同点或载荷上的不同点作为枢轴点，会导致车辆具有不同的包络。参考点可根据具体情况变化，例如，载货移动机器人与未载货移动机器人可以采用不同的参考点。


# 6 协议规范

以下章节描述通信协议的详细内容。
本协议规定了车队控制与移动机器人之间的通信。
移动机器人与外围设备之间的通信（例如移动机器人与门之间的通信）不在本协议范围内。

不同的消息以表格形式呈现，表格描述作为订单、状态等发送的 JSON 中各字段的内容。

此外，公共 Git 仓库（https://github.com/VDA5050/VDA5050）中提供了用于验证的 JSON Schema。
JSON Schema 会随 VDA 5050 的每次发布进行更新。如果 JSON Schema 与本文档存在差异，以本文档中的版本为准。


## 6.1 表格符号与格式含义

表格包含标识符名称、单位、数据类型和描述（如有）。

标识 | 描述
--- | ---
普通文本 | 变量为基本数据类型
**粗体** | 变量为非基本数据类型（例如 JSON 对象或数组），并在其他位置单独定义
*斜体* | 变量为可选字段
***粗斜体*** | 变量为可选字段且属于非基本数据类型
arrayName[arrayDataType] | 变量（此处为 `arrayName`）是由方括号中所含数据类型（此处为 `arrayDataType`）构成的数组

所有关键字均区分大小写。
所有字段名均采用驼峰式命名法。
所有枚举值均使用大写字母且不含下划线。


### 6.1.1 可选字段

如果变量被标记为可选，表示对发送方而言该变量是可选的，因为它在某些情况下可能不适用（例如，当车队控制向移动机器人发送订单时，某些移动机器人会自行规划轨迹，因此可以省略订单 `edge` 对象中的 `trajectory` 字段）。

如果移动机器人收到的消息包含本协议中标记为可选的字段，则预期移动机器人按该字段执行，不得忽略该字段。
如果移动机器人无法相应地处理该消息，预期行为是通过错误消息进行报告并拒绝该订单。

车队控制应仅发送移动机器人支持的可选信息。

示例：轨迹是可选的。
如果移动机器人无法处理轨迹，车队控制不得向其发送轨迹。

移动机器人应通过 `factsheet` 消息传输其所需的可选参数。


### 6.1.2 允许的字符与字段长度

所有通信均使用 UTF-8 编码，以支持描述内容的国际化适配。
建议 ID 仅使用以下字符：

A-Z a-z 0-9 _ - . :

协议未定义最大消息长度，但其受 MQTT 协议规范以及信息表中可能定义的技术限制约束。
如果移动机器人的内存不足以处理传入订单，则应拒绝该订单。
最大字段长度、字符串长度或数值范围由系统集成方协商确定。
为便于集成，移动机器人供应商应提供一份移动机器人信息表，详见 [6.15 `factsheet` 主题](#615-factsheet-主题)。


### 6.1.3 字段、主题与枚举的表示法

本文档中的主题和字段采用以下样式突出显示：`exampleField` 和 `exampleTopic`。
枚举值应使用大写字母，并在本文档中以单引号括起。
这包括 `actionStatus` 字段中的 `'WAITING'`、`'FINISHED'` 等关键字。


### 6.1.4 JSON 数据类型

应尽可能使用 JSON 数据类型。
因此，布尔值编码为 `true` 或 `false`，而不是枚举值（`'TRUE'`、`'FALSE'`）或魔法数字。
数值数据类型以类型和精度表示，例如 `float64` 或 `uint32`。不支持 IEEE 754 中的 NaN 和无穷大等特殊数值。


## 6.2 MQTT 连接处理、安全与 QoS

MQTT 协议支持为客户端设置遗嘱消息。
如果客户端因任何原因意外断开连接，代理服务器会将该遗嘱消息分发给其他已订阅的客户端。
该功能的使用方式见 [6.14 `connection` 主题](#614-connection-主题)。

如果移动机器人与代理服务器断开连接，它仍会保留所有订单信息，并执行订单直至最后一个已释放节点。

协议安全性需要通过代理服务器配置加以考虑。

为减少通信开销，`order`、`instantActions`、`state`、`factsheet` 和 `visualization` 主题应使用 MQTT QoS 0（尽力而为）。
`connection` 主题应使用 QoS 1（至少一次）。


## 6.3 MQTT 主题层级

由于云服务提供商对主题结构有强制要求，因此本规范未严格定义 MQTT 主题结构。
对于云端 MQTT 代理服务器，必须单独调整主题结构，以匹配本协议定义的主题。
这意味着后续章节定义的主题名称是强制性的。

对于本地代理服务器，建议采用以下 MQTT 主题层级：

**interfaceName/majorVersion/manufacturer/serialNumber/topic**

示例：
```
uagv/v2/KIT/0001/order
```

MQTT 主题层级 | 数据类型 | 描述
--- | --- | ---
interfaceName | string | 所用接口的名称
majorVersion | string | VDA 5050 建议的主版本号，以 `v` 为前缀
manufacturer | string | 移动机器人制造商
serialNumber | string | 移动机器人唯一序列号，由以下字符组成：<br>A-Z<br>a-z<br>0-9<br>_<br>.<br>:<br>-
topic | string | 主题（例如 `"order"` 或 `"state"`），参见 [6.5 通信主题](#65-通信主题)

注意：由于 `/` 字符用于定义主题层级，因此不得在上述任何字段中使用该字符。
某些 MQTT 代理服务器也将 `$` 字符用于特殊的内部主题，因此该字符也不应使用。


## 6.4 协议头

每条 JSON 消息均以协议头开始。
为便于阅读，后续章节将下列字段统称为协议头。
协议头由以下各项组成。
协议头并非 JSON 对象。

对象结构/标识符 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。


### 协议版本

协议版本采用语义化版本控制方案。

主版本变更示例：

- 不兼容变更，例如新增非可选字段

次版本变更示例：

- 新功能，例如增加可视化主题

补丁版本变更示例：

- 提高 `batteryCharge` 的可用精度


## 6.5 通信主题

移动机器人协议使用以下主题在车队控制与移动机器人之间交换信息。

主题名称 | 发布方 | 订阅方 | 用途 | 实现要求 | Schema
--- | --- | --- | --- | --- | ---
`order` | 车队控制 | 移动机器人 | 将行驶订单从车队控制传输到移动机器人 | 强制性 | `order.schema`
`instantActions` | 车队控制 | 移动机器人 | 传输需要立即执行的动作 | 强制性 | `instantActions.schema`
`state` | 移动机器人 | 车队控制 | 传输移动机器人状态 | 强制性 | `state.schema`
`visualization` | 移动机器人 | 可视化系统 | 仅用于可视化目的，以较高频率传输位置 | 可选 | `visualization.schema`
`connection` | 代理服务器/移动机器人 | 车队控制 | 表示移动机器人连接何时丢失。车队控制不得使用该主题检查车辆健康状态；该主题用于在 MQTT 协议层检查连接状态 | 强制性 | `connection.schema`
`factsheet` | 移动机器人 | 车队控制 | 提供参数或供应商特定信息，以帮助在车队控制中配置移动机器人 | 强制性 | `factsheet.schema`


## 6.6 `order` 主题（从车队控制到移动机器人）

`order` 是移动机器人接收由 JSON 封装的订单时所使用的 MQTT 主题。


### 6.6.1 概念与逻辑

订单的基本结构是节点-边图。
预期移动机器人通过这些节点和边来完成订单。
所有相连节点和边构成的完整图保存在车队控制中。

车队控制中的图表示包含各种限制，例如允许哪台移动机器人通过哪条边。
这些限制不会传输给移动机器人。
车队控制只会在移动机器人的订单中加入该移动机器人获准通过的边。

![图 3 车队控制中的图表示以及订单中传输的图](./assets/graph_representation_transmission.png)
>图 3 - 车队控制中的图表示以及订单中传输的图

节点和边在订单消息中以两个列表传递。
节点和边在相应列表中的顺序也决定了应通过这些节点和边的先后顺序。

有效订单应至少包含一个节点，且边的数量应等于节点数减一。

订单的第一个节点应可由移动机器人直接到达。
这意味着移动机器人要么已经停在该节点上，要么位于该节点的偏差范围内。

节点和边都有一个布尔属性 `released`。
如果节点或边已释放，预期移动机器人会通过它。
如果节点或边未释放，移动机器人不得通过它。

只有当边的起始节点和结束节点均已释放时，该边才能被释放。

在未释放的边之后，序列中不得再出现已释放的节点或边。

已释放节点和边的集合称为“基础区”。
未释放节点和边的集合称为“预测区”。

发送不含预测区的订单是有效的。

订单消息不一定描述完整的运输订单。
为了进行交通控制并适应资源受限的车辆，完整的运输订单（可能包含许多节点和边）可以拆分成多个子订单，并通过 `orderId` 和 `orderUpdateId` 将这些子订单关联起来。
订单更新过程将在下一节中说明。


### 6.6.2 订单与订单更新

为支持交通管理，车队控制可以将订单中传输的路径分为两部分：

- *“基础区”*：移动机器人获准行驶的确定路线。基础区路线中的所有节点和边均已由车队控制向该车辆释放。基础区的最后一个节点称为决策点。
- *“预测区”*：车队控制当前为移动机器人规划的、在决策点之后行驶的路线。预测区路线尚未由车队控制释放。

如果基础区没有新增节点和边，移动机器人应在决策点停下。为保证移动机器人连续、平稳行驶，如果交通状况允许，车队控制应当在移动机器人到达决策点之前扩展基础区。

由于 MQTT 是异步协议，且通过无线网络进行的传输并不可靠，因此基础区不能更改。所以，车队控制应假定移动机器人已执行基础区。后文描述了取消订单的流程，但由于上述通信限制，该流程同样被视为不可靠。

车队控制可以向移动机器人发送包含已更改节点和边列表的更新路线，从而更改预测区。更改预测区路线的流程如图 4 所示。

![图 4 更改“预测区”行驶路线的流程](./assets/driving_route_horizon.png)
>图 4 - 更改“预测区”行驶路线的流程

在图 4 中，车队控制首先在时间 `t = 1` 时发送初始订单。
图 5 给出了一个可能订单的伪代码。
为便于阅读，此处省略了完整的 JSON 示例。

```
{
	orderId: "1234",
	orderUpdateId: 0,
	nodes: [
		f {released: true},
		d {released: true},
		g {released: true},
		b {released: false},
		h {released: false}
	],
	edges: [
		e1 {released: true},
		e3 {released: true},
		e8 {released: false},
		e9 {released: false}
	]
}
```
>图 5 - 订单的伪代码

在时间 `t = 3` 时，通过发送订单扩展来更新订单（示例见图 6）。
注意，`orderUpdateId` 会递增，且订单更新的第一个节点对应于前一订单消息最后一个共同的基础区节点。

这可以确保移动机器人也能执行订单更新，即通过执行移动机器人已知的边，可以到达订单更新的第一个节点。

```
{
	orderId: "1234",
	orderUpdateId: 1,
	nodes: [
		g {released: true},
		b {released: true},
		h {released: true},
		i {released: false}
	],
	edges: [
		e8 {released: true},
		e9 {released: true},
		e10 {released: false}
	]
}
```
>图 6 - 订单更新的伪代码；注意 `orderUpdateId` 的变化

这也有助于处理订单更新丢失的情况（例如因无线网络不可靠而丢失）。
移动机器人可以随时检查最后一个已知基础区节点的 `nodeId`（以及 `sequenceId`，详见下文）是否与新基础区的第一个节点相同。

还要注意，节点 `g` 是唯一再次发送的基础区节点。
由于基础区不能更改，重新传输节点 `f` 和 `d` 是无效的。

重要的是不得更改拼接节点（示例中的节点 `g`）的内容。
对于动作、偏差范围等，移动机器人应使用第一个订单（图 5，`orderUpdateId` 为 0）中提供的指令。

![图 7 常规更新流程 - 订单扩展](./assets/update_order_extension.png)
>图 7 - 常规更新流程 - 订单扩展

图 7 描述了订单应如何扩展。
该图显示移动机器人当前可用的信息。
`orderId` 保持不变，`orderUpdateId` 递增。

前一基础区的最后一个节点是更新订单中的第一个基础区节点。
移动机器人可以使用该节点将更新订单附加到当前订单（拼接）。
前一基础区的其他节点和边不会重新发送。

车队控制可以发送完全不同的节点作为新基础区，以更改预测区。
也可以删除预测区。

为使订单可以包含环路（例如从节点 `a` 前往 `b`，然后返回 `a`），需要为节点和边对象分配 `sequenceId`。
`sequenceId` 依次分配给节点和边（订单的第一个节点取 0，第一条边取 1，第二个节点取 2，依此类推）。
这样可以更容易地跟踪订单进度。

`sequenceId` 一经分配，便不会随订单更新而改变（见图 7）。
这是移动机器人确定车队控制所指节点所必需的。

图 8 描述了接受订单或订单更新的流程。

![图 8 接受订单或订单更新的流程](./assets/process_order_update.png)
>图 8 - 接受订单或订单更新的流程

1. **收到的订单有效吗？**：
所有格式和 JSON 数据类型是否正确？

2. **收到的是新订单，还是当前订单的更新？**：
收到订单的 `orderId` 是否不同于车辆当前持有订单的 `orderId`？

3. **车辆仍在执行订单或等待更新吗？**：
`nodeStates` 是否非空，或者 `actionStates` 是否包含既不是 `'FAILED'` 也不是 `'FINISHED'` 的状态？状态中也包含订单预测区的节点、边和相应动作状态。车辆可能仍有预测区，因此正在等待更新并执行订单。

4. **新订单的起点是否足够接近当前位置？**：
车辆是否已经停在该节点上，或者是否位于该节点的偏差范围内（参见 [6.6.1 概念与逻辑](#661-概念与逻辑)）？

5. **收到的订单更新是否已过期？**：
`orderUpdateId` 是否小于车辆当前持有的值？

6. **车辆当前是否已持有收到的订单更新？**：
`orderUpdateId` 是否等于车辆当前持有的值？

7. **收到的更新是否是当前仍在运行的订单的有效延续？**：
收到订单的第一个节点是否等于当前决策点（当前基础区的最后一个节点）？车辆仍在移动或执行与之前订单更新所释放基础区相关的动作，或者仍有预测区，因此正在等待订单继续。在这种情况下，只有当新基础区的第一个节点等于前一基础区的最后一个节点时，才接受订单更新。

8. **收到的更新是否是之前已完成订单的有效延续？**：
收到的订单更新中第一个节点的 `nodeId` 和 `sequenceId` 是否分别等于 `lastNodeId` 和 `lastNodeSequenceId`？车辆不再执行任何动作，也未在等待订单继续（即已经完成基础区及其所有相关动作，并且没有预测区）。如果订单更新从最后一个已通过节点继续，则现在接受该订单更新；因此，新基础区第一个节点的值需要同时匹配车辆的 `lastNodeId` 和 `lastNodeSequenceId`。

9. 填充/附加状态是指填充/附加 `actionStates`、`nodeStates` 和 `edgeStates`。


### 6.6.3 订单取消（由车队控制发起）

如果基础区节点发生计划外变化，应使用即时动作 `cancelOrder` 取消订单。

收到即时动作 `cancelOrder` 后，车辆应根据自身能力停车，例如立即停车或在下一个节点停车。

如果存在已计划的动作，则应取消这些动作，并在相应 `actionState` 中报告 `'FAILED'`。
如果存在正在执行的动作，则应当取消这些动作，并同样报告为 `'FAILED'`。
如果动作无法中断，其 `actionState` 应在动作执行期间报告 `'RUNNING'`；动作结束后报告相应状态（成功时为 `'FINISHED'`，否则为 `'FAILED'`）。
在仍有动作运行期间，`cancelOrder` 动作应报告 `'RUNNING'`，直至所有动作都已取消或完成。
车辆的所有移动及其所有动作均停止后，`cancelOrder` 的动作状态应报告 `'FINISHED'`。

保留 `orderId` 和 `orderUpdateId`。

图 9 展示了具有不同能力的移动机器人的预期行为。

![图 9 cancelOrder 后的预期行为](./assets/process_cancel_order.png)
>图 9 - `cancelOrder` 后的预期行为


#### 6.6.3.1 取消后收到新订单

订单取消后，车辆应处于可接收新订单的状态。

对于通过标签在节点上定位自身的移动机器人，新订单必须从该移动机器人当前停靠的节点开始（另见图 5）。

对于可以停在节点之间的移动机器人，由车队控制决定如何开始下一个订单。
移动机器人应接受以下两种方式。

有两个选项：

- 发送一个订单，其中第一个节点是位于移动机器人当前位置的临时节点。移动机器人应识别出该节点可直接到达，并接受订单。
- 发送一个订单，其中第一个节点是前一订单最后一个已通过节点，但将偏差范围设置得足够大，使移动机器人位于该范围内。因此，移动机器人应识别出该节点应被视为已通过，并接受订单。


#### 6.6.3.2 移动机器人没有订单时收到 `cancelOrder` 动作

如果移动机器人收到 `cancelOrder` 动作，但当前没有订单，或者前一订单已被取消，则 `cancelOrder` 动作应报告为 `'FAILED'`。

移动机器人应报告错误类型为 `"noOrderToCancel"`、`errorLevel` 为 `'WARNING'` 的错误。
应将该 `instantAction` 的 `actionId` 作为 `errorReference` 传递。


### 6.6.4 订单拒绝

在若干情形下应拒绝订单。
这些情形如图 8 所示，并在下文中说明。


#### 6.6.4.1 车辆收到格式错误的新订单

处理方式：

1. 车辆不得将新订单写入其内部缓冲区。
2. 车辆报告警告 `"validationError"`。
3. 在车辆接受新订单之前，应持续报告该警告。


#### 6.6.4.2 车辆收到的订单包含其无法执行的动作或无法使用的字段

示例：

- 无法执行的动作：提升高度高于最大提升高度、未安装升降机构却要求执行提升动作等。
- 无法使用的字段：轨迹等。

处理方式：

1. 车辆不得将新订单写入其内部缓冲区。
2. 车辆报告警告 `"orderError"`，并将错误字段作为错误关联项。
3. 在车辆接受新订单之前，应持续报告该警告。


#### 6.6.4.3 车辆收到的新订单具有相同的 `orderId`，但 `orderUpdateId` 低于当前 `orderUpdateId`

处理方式：

1. 车辆不得将新订单写入其内部缓冲区。
2. 车辆将前一订单保留在缓冲区中。
3. 车辆报告警告 `"orderUpdateError"`。
4. 车辆继续执行前一订单。

如果移动机器人两次收到具有相同 `orderId` 和 `orderUpdateId` 的订单，则忽略第二个订单。
如果状态消息到达车队控制过晚，导致车队控制无法确认第一个订单是否已收到，车队控制可能会重新发送该订单，从而出现这种情况。


### 6.6.5 走廊

可选的 `corridor` 边属性允许车辆偏离边轨迹以避开障碍物，并定义车辆获准运行的边界。
要使用 `corridor` 属性，需要预定义一条在没有 `corridor` 属性时车辆会遵循的轨迹。该轨迹可以是车辆上定义且车队控制已知的轨迹，也可以是订单中发送的轨迹。使用 `corridor` 属性的车辆仍表现为线导引移动机器人，只是允许其临时偏离轨迹以避开障碍物。

*备注：
订单中的边定义两个节点之间的逻辑连接，但不一定表示车辆从起始节点驶向结束节点时遵循的（实际）轨迹。
根据车辆类型，车辆在起始节点和结束节点之间行驶所采用的轨迹，可以由车队控制通过边的 `trajectory` 属性定义，也可以作为预定义轨迹分配给车辆。
所选轨迹可能会随车辆内部状态而变化。*

![图 10 带有 corridor 属性的边](./assets/edges_with_corridors.png)
>图 10 - 带有 `corridor` 属性的边，该属性定义了车辆为避开障碍物而获准偏离预定义轨迹的左右边界。左图以运动学中心定义允许偏差，右图以可能因载荷而扩展的车辆轮廓定义允许偏差。具体方式由 `corridorRefPoint` 参数定义。

车辆获准自主导航（并偏离原始边轨迹）的区域由左边界和右边界定义。
可选字段 `corridorRefPoint` 指定车辆控制点或车辆轮廓应位于已定义的边界内。
边界的定义方式应确保车辆一旦通过节点，就位于新的当前边的边界内。
如果车辆不得偏离轨迹，车队控制不应将走廊边界设置为零，而应不使用 `corridor` 属性。

车辆的运动控制软件应持续检查车辆是否位于已定义的边界内。
如果不在边界内，车辆应停车，因为它已离开允许的导航空间，并应报告错误。
车队控制可以决定是否需要用户干预；也可以取消当前订单，再向车辆发送包含可使其恢复移动的走廊信息的新订单，从而使车辆继续移动。

*备注：允许车辆偏离轨迹会增加车辆行驶期间可能占用的空间。在初始运行期间，以及车队控制基于车辆占用空间作出交通控制决策时，都应考虑这种情况。*

更多信息另见 [6.10.2 通过节点和进入/离开边，以及动作触发](#6102-通过节点和进入离开边以及动作触发)。


## 6.6.6 订单消息的实现

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
headerId |  | uint32 | 消息的协议头 ID。<br>协议头 ID 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp |  | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"`）。
version |  | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer |  | string | 移动机器人制造商。
serialNumber |  | string | 移动机器人的序列号。
orderId |  | string | 订单标识符。<br>用于标识属于同一订单的多条订单消息。
orderUpdateId |  | uint32 | 订单更新标识符。<br>在每个 `orderId` 内唯一。<br>如果订单更新被拒绝，应在拒绝消息中传递该字段。
*zoneSetId* |  | string | 移动机器人必须用于导航或车队控制在规划时使用的区域集的唯一标识符。<br><br>可选：某些车队控制系统不使用区域。<br>某些移动机器人无法理解区域。<br>如果未使用区域，则不在消息中添加该字段。
**nodes [node]** |  | array | 为完成订单而需要通过的节点对象数组。<br>一个节点足以构成有效订单。<br>在这种情况下，边数组留空。
**edges [edge]** |  | array | 为完成订单而需要通过的边对象数组。<br>一个节点足以构成有效订单。<br>在这种情况下，边数组留空。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**node** { |  | JSON object |
nodeId |  | string | 节点的唯一标识符。
sequenceId |  | uint32 | 用于跟踪订单中节点和边的顺序，并简化订单更新的编号。<br>其主要用途是区分同一 `orderId` 中多次经过的节点。<br>`sequenceId` 依次分配给同一订单中的所有节点和边，并在签发新的 `orderId` 时重置。
*nodeDescription* |  | string | 节点的附加信息。
released |  | boolean | `true` 表示该节点是基础区的一部分。<br>`false` 表示该节点属于预测区。
***nodePosition*** |  | JSON object | 节点位置。<br>对于不需要节点位置的车辆类型（例如线导引移动机器人），这是可选的。
**actions [action]**<br> } |  | array | 要在节点上执行的动作数组。<br>如果无需执行动作，则为空数组。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**nodePosition** { |  | JSON object | 在项目特定的全局世界坐标系中定义地图上的位置。<br>每个楼层都有自己的地图。<br>所有地图应使用同一个项目特定的全局原点。
x | m | float64 | 地图上相对于地图坐标系的 x 位置。<br>精度取决于具体实现。
y | m | float64 | 地图上相对于地图坐标系的 y 位置。<br>精度取决于具体实现。
*theta* | rad | float64 | 范围：[-Pi ... Pi]<br><br>移动机器人在节点上的绝对朝向角。<br>可选：车辆可以自行规划路径。<br>如果定义了该值，移动机器人必须在此节点上达到 `theta` 朝向角。<br>如果前一条边不允许旋转，移动机器人应在节点上旋转。<br>如果后一条边定义了不同的朝向角且不允许旋转，则移动机器人应在进入该边之前，在节点上旋转到该边要求的朝向角。
*allowedDeviationXY* | m | float64 | 指示移动机器人必须以多高的位置精度匹配节点，节点才被视为已通过。<br><br>如果等于 0.0：不允许偏差（无偏差是指处于移动机器人制造商的正常公差内）。<br><br>如果大于 0.0：允许的偏差半径，单位为米。<br>如果移动机器人在偏差半径内经过节点，则该节点可被视为已通过。
*allowedDeviationTheta* | rad | float64 | 范围：[0.0 ... Pi]<br><br>指示移动机器人必须以多高的朝向角精度达到 `theta` 中定义的节点朝向角。<br>可接受的最小角度为 `theta - allowedDeviationTheta`，最大角度为 `theta + allowedDeviationTheta`。
mapId |  | string | 该位置所引用地图的唯一标识符。<br>每张地图都具有同一个项目特定的全局坐标原点。<br>当移动机器人使用电梯（例如从出发楼层前往目标楼层）时，它会从出发楼层的地图上消失，并出现在目标楼层地图中对应的电梯节点上。
*mapDescription*<br> } |  | string | 地图的附加信息。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**action** { |  | JSON object | 描述移动机器人能够执行的动作。
actionType |  | string | “动作和参数”第一列中所述的动作名称。<br>用于标识动作的功能。
actionId |  | string | 用于标识动作，并将其映射到状态消息中相应 `actionState` 的唯一 ID。<br>建议使用 UUID。
*actionDescription* |  | string | 动作的附加信息。
blockingType |  | string | 枚举 {'NONE', 'SOFT', 'HARD'}：<br>'NONE'：允许行驶和执行其他动作；<br>'SOFT'：允许执行其他动作，但不允许行驶；<br>'HARD'：该动作是此时唯一允许执行的动作。
***actionParameters [actionParameter]***<br><br> } |  | array | 指定动作的 `actionParameter` 对象数组，例如 `deviceId`、`loadId`、`external triggers`。<br><br>实现示例见 [7.2 参数格式](#72-参数格式)。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**edge** { |  | JSON object | 两个节点之间的定向连接。
edgeId |  | string | 边的唯一标识符。
sequenceId |  | uint32 | 用于跟踪订单中节点和边的顺序，并简化订单更新的编号。<br>`sequenceId` 依次分配给同一订单中的所有节点和边，并在签发新的 `orderId` 时重置。
*edgeDescription* |  | string | 边的附加信息。
released |  | boolean | `true` 表示该边是基础区的一部分。<br>`false` 表示该边属于预测区。
startNodeId |  | string | 订单内第一个节点的 `nodeId`。
endNodeId |  | string | 订单内最后一个节点的 `nodeId`。
*maxSpeed* | m/s | float64 | 边上允许的最大速度。<br>速度以车辆上速度最快的测量点为准。
*maxHeight* | m | float64 | 边上允许的车辆最大高度，包括载荷在内。
*minHeight* | m | float64 | 边上允许的载荷搬运装置最小高度。
*orientation* | rad | float64 | 移动机器人在边上的朝向角。`orientationType` 的值决定应将其解释为相对于项目特定的全局地图坐标系，还是相对于边的切线。采用切线解释时，0.0 表示向前行驶，Pi 表示向后行驶。<br>示例：朝向角 Pi/2 rad 会导致旋转 90 度。<br><br>如果移动机器人的起始朝向角不同，且 `rotationAllowed` 设置为 `true`，则在边上将车辆旋转到所需朝向角。<br>如果 `rotationAllowed` 为 `false`，则在进入边之前旋转。<br>如果无法做到，则拒绝订单。<br><br>如果未定义轨迹，则将旋转应用于连接该边两个节点的直线路径。<br>如果为边定义了轨迹，则将朝向角应用于该轨迹。
*orientationType* |  | string | 枚举 {'GLOBAL', 'TANGENTIAL'}：<br>'GLOBAL'：相对于项目特定的全局地图坐标系；<br>'TANGENTIAL'：与边相切。<br><br>如果未定义，默认值为 'TANGENTIAL'。
*direction* |  | string | 为线导引或导线导引车辆设置路口方向；允许值应针对具体车辆预先定义。<br>示例：`"left"`、`"right"`、`"straight"`。
*rotationAllowed* |  | boolean | `true`：允许在边上旋转。<br>`false`：不允许在边上旋转。<br><br>可选：<br>未设置时不限制。
*maxRotationSpeed* | rad/s | float64 | 最大旋转速度。<br><br>可选：<br>未设置时不限制。
***trajectory*** |  | JSON object | 以 NURBS 表示该边轨迹的 JSON 对象。<br>定义移动机器人应在边的起始节点与结束节点之间行驶的路径。<br><br>可选：<br>如果移动机器人无法处理轨迹或会自行规划轨迹，则可以省略。
*length* | m | float64 | 从起始节点到结束节点的路径长度。<br><br>可选：<br>线导引移动机器人使用该值在到达停止位置之前降低速度。
***corridor*** |  | JSON object | 定义车辆可以偏离轨迹的边界，例如为了避开障碍物。<br>
**actions [action]**<br><br><br> } |  | array | 要在边上执行的动作数组。<br>如果无需执行动作，则为空数组。<br>由边触发的动作仅在移动机器人通过触发该动作的边期间处于活动状态。<br>移动机器人离开该边时，动作将停止，并恢复进入该边之前的状态。


对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**trajectory** { |  | JSON object |
degree |  | float64 | 范围：[1.0 ... float64.max]<br><br>定义轨迹的 NURBS 曲线次数。<br><br>如果未定义，默认值为 1。
**knotVector [float64]** |  | array | 范围：[0.0 ... 1.0]<br><br>NURBS 的节点值数组。<br><br>`knotVector` 的元素数量为控制点数量加 `degree` 再加 1。
**controlPoints [controlPoint]**<br><br> } |  | array | 定义 NURBS 控制点的 `controlPoint` 对象数组，明确包含起点和终点。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**controlPoint** { |  | JSON object |
x |  | float64 | 世界坐标系中的 x 坐标。
y |  | float64 | 世界坐标系中的 y 坐标。
*weight* |  | float64 | 范围：[0.0 ... float64.max]<br><br>控制点在曲线上的权重。<br>如果未定义，默认值为 1.0。
} |  |  |

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
***corridor*** { |  | JSON object |
leftWidth | m | float64 | 范围：[0.0 ... float64.max]<br>定义车辆轨迹左侧的走廊宽度，单位为米（见图 10）。
rightWidth | m | float64 | 范围：[0.0 ... float64.max]<br>定义车辆轨迹右侧的走廊宽度，单位为米（见图 10）。
*corridorRefPoint*<br><br>**}** |  | string | 定义边界适用于车辆的运动学中心还是轮廓。如果未指定，边界适用于车辆的运动学中心。<br>枚举 {'KINEMATICCENTER', 'CONTOUR'}。


### 6.7 地图

为了确保不同类型的移动机器人采用一致的导航方式，位置始终以项目特定的坐标系为参考来指定（见图 11）。
为了区分场地或位置的不同楼层，使用唯一的 `mapId`。
地图坐标系应定义为 z 轴指向天空的右手坐标系。
因此，正旋转应理解为逆时针旋转。
车辆坐标系也定义为右手坐标系，其中 x 轴指向车辆前进方向，z 轴向上。除非另有说明，否则车辆参考点在车辆参考系中定义为 (0, 0, 0)。
这符合 DIN ISO 8855 第 2.11 节的规定。

![图 11 坐标系、移动机器人示例及朝向](./assets/coordinate_system_vehicle_orientation.png)
>图 11 - 坐标系、移动机器人示例及朝向

x、y 和 z 坐标应以米为单位表示。
朝向角应以弧度为单位，且应位于 -Pi 和 +Pi 之间。

![图 12 地图与车辆的坐标系](./assets/coordinate_system_vehicle_map.png)
>图 12 - 地图与车辆的坐标系


### 6.7.1 地图分发

为实现自动地图分发，并在必要时对车辆重启进行智能管理，本文引入了一种标准化的地图分发方式。

待分发的地图文件存储在车辆可访问的专用地图服务器上。为确保传输效率，每次传输应当只包含一个文件。如果需要多个地图或文件，应当将它们捆绑或打包成一个文件。将地图从地图服务器传输到车辆的过程属于拉取操作，由车队控制使用 `instantAction` 触发下载命令来启动。

每张地图通过地图标识符（字段 `mapId`）和地图版本（字段 `mapVersion`）的组合唯一标识。地图标识符描述车辆物理工作区中的特定区域，地图版本表示对先前版本的更新。在接受新订单之前，车辆应检查自身是否具有所请求订单中每个地图标识符对应的地图。车队控制负责确保启用了车辆运行所需的正确地图。

为了尽量缩短停机时间，并便于车队控制同步新地图的启用过程，在车辆上预先加载或缓存地图至关重要。可以通过车辆状态通道访问车辆上的地图状态。需要注意，将地图传输到移动机器人和随后启用该地图是两个不同的过程。为了在车辆上启用预加载的地图，车队控制应发送即时动作。此时，具有相同地图标识符但地图版本不同的其他所有地图会自动禁用。车队控制可以通过另一个即时动作删除地图。该过程的结果显示在车辆状态中。

地图分发流程如图 13 所示。

![图 13 地图分发流程](./assets/map_distribution_process.png)
>图 13 - 车队控制、移动机器人和地图服务器之间下载、启用和删除地图所需的通信

#### 6.7.2 移动机器人状态中的地图

状态中 `agvPosition` 的 `mapId` 字段表示当前启用的地图。车辆上可用的地图信息通过状态消息中的 `maps` 数组提供。该数组中的每个条目都是一个 JSON 对象，包含必填字段 `mapId`、`mapVersion` 和 `mapStatus`；`mapStatus` 可以是 `'ENABLED'` 或 `'DISABLED'`。车辆可以在需要时使用 `'ENABLED'` 地图。不得使用 `'DISABLED'` 地图。下载进程的状态通过当前动作尚未完成来表示。错误也会在状态中报告。

请注意，可以同时启用多张 `mapId` 不同的地图。对于同一个 `mapId`，同一时间只能启用一个地图版本。如果 `maps` 数组为空，则表示车辆当前没有可用地图。


#### 6.7.3 地图下载

地图下载由车队控制的即时动作 `downloadMap` 触发。该命令包含必填参数 `mapId` 和 `mapDownloadLink`；后者指向地图服务器上存储地图且车辆可以访问的位置。

移动机器人一旦开始下载地图文件，就会将 `actionStatus` 设置为 `'RUNNING'`。如果下载成功，`actionStatus` 更新为 `'FINISHED'`。如果下载失败，状态设置为 `'FAILED'`。成功完成下载后，应将地图添加到状态的 `maps` 数组中。地图在准备好启用之前不得在状态中报告。

必须确保地图下载过程不会修改、删除、启用或禁用车辆上的任何现有地图。
车辆应拒绝下载已经存在于车辆上且具有相同 `mapId` 和 `mapVersion` 的地图。应报告错误，并将即时动作状态设置为 `'FAILED'`。车队控制应先删除车辆上的地图，然后重新开始下载。


#### 6.7.4 启用已下载的地图

在车辆上启用地图有两种方式：

1. **车队控制启用地图**：使用即时动作 `enableMap` 将车辆上的地图设置为 `'ENABLED'`。具有相同 `mapId` 但 `mapVersion` 不同的其他地图版本设置为 `'DISABLED'`。
2. **在车辆上手动启用地图**：在某些情况下，可能需要直接在车辆上启用地图。结果应在车辆状态中报告。

车队控制负责确保在订单中将相应 `mapId` 作为 `nodePosition` 的一部分发送时，车辆启用的地图正确。
如果要在新地图上将车辆设置到特定位置，则使用即时动作 `initPosition`。


#### 6.7.5 删除移动机器人上的地图

车队控制可以请求车辆删除特定地图。该操作通过即时动作 `deleteMap` 完成。车辆内存耗尽时，应当向车队控制报告，车队控制随后可以发起地图删除。车辆本身不得删除地图。
成功删除地图后，应从车辆状态的 `maps` 数组中移除该地图的条目。


## 6.8 动作

如果移动机器人支持行驶以外的动作，可以通过附加到节点或边的动作字段执行这些动作，也可以通过单独的 `instantActions` 主题发送（参见 [6.9 `instantActions` 主题](#69-instantactions-主题从车队控制到移动机器人)）。

在边上执行的动作只能在移动机器人位于该边时运行（参见 [6.10.2 通过节点和进入/离开边，以及动作触发](#6102-通过节点和进入离开边以及动作触发)）。

由节点触发的动作可以持续所需的时间，并且应当能够自行结束（例如持续五秒的音频信号，或在拾取载荷后结束的 `pick` 动作）或成对定义（例如 `activateWarningLights` 和 `deactivateWarningLights`），但也可能存在例外。

下一节给出了预定义动作。如果移动机器人的能力与某个动作描述相符，则移动机器人应使用该预定义动作。
如果可以合理使用已定义的参数，则应使用这些参数。
如果成功执行动作还需要其他参数，可以定义附加参数。

如果某项能力无法映射为下文任一动作，移动机器人制造商可以定义由车队控制使用的附加动作。


### 6.8.1 预定义动作的定义、参数、效果与范围

常规 |  | 范围
:---: | --- | :---:
动作、反向动作、描述、幂等、参数 | 关联状态 | 即时、节点、边

动作 | 反向动作 | 描述 | 幂等 | 参数 | 关联状态 | 即时 | 节点 | 边
--- | --- | --- | --- | --- | --- | --- | --- | ---
`startPause` | `stopPause` | 启动暂停模式。<br>需要关联状态，因为许多移动机器人可以通过硬件开关暂停。<br>移动机器人不再行驶，无需到达下一个节点。<br>动作可以继续。<br>订单可以恢复。 | 是 | - | `paused` | 是 | 否 | 否
`stopPause` | `startPause` | 关闭暂停模式。<br>恢复移动及所有其他动作（如有）。<br>需要关联状态，因为许多移动机器人可以通过硬件开关暂停。<br>`stopPause` 还可以重新启动由触发 `startPause` 的硬件按钮停止的车辆（如已配置）。 | 是 | - | `paused` | 是 | 否 | 否
`startCharging` | `stopCharging` | 启动充电过程。<br>可以在充电点（车辆静止）或充电通道（车辆行驶时）充电。<br>车辆负责防止过度充电。 | 是 | - | `.batteryState.charging` | 是 | 是 | 否
`stopCharging` | `startCharging` | 停止充电过程，以便发送新订单。<br>车辆或充电站也可以中断充电过程，例如电池已充满时。<br>只有在移动机器人已准备好接收订单时，电池状态才允许为 `false`。 | 是 | - | `.batteryState.charging` | 是 | 是 | 否
`initPosition` | - | 使用给定参数重置（覆盖）移动机器人的位姿。 | 是 | `x`（float64）<br>`y`（float64）<br>`theta`（float64）<br>`mapId`（string）<br>`lastNodeId`（string） | `.agvPosition.x`<br>`.agvPosition.y`<br>`.agvPosition.theta`<br>`.agvPosition.mapId`<br>`.lastNodeId`<br>`.maps` | 是 | 是<br>（电梯） | 否
`enableMap` | - | 显式启用之前下载的地图，以便在订单中使用，而无需初始化新位置。 | 是 | `mapId`（string）<br>`mapVersion`（string） | `.maps` | 是 | 是 | 否
`downloadMap` | - | 触发新地图的下载。下载期间处于活动状态。错误在车辆状态中报告。验证下载成功、准备好地图并在状态中设置地图后结束。 | 是 | `mapId`（string）<br>`mapVersion`（string）<br>`mapDownloadLink`（string）<br>`mapHash`（string，可选） | `.maps` | 是 | 否 | 否
`deleteMap` | - | 触发从车辆内存中移除地图。 | 是 | `mapId`（string）<br>`mapVersion`（string） | `.maps` | 是 | 否 | 否
`stateRequest` | - | 请求移动机器人发送新的状态报告。 | 是 | - | - | 是 | 否 | 否
`logReport` | - | 请求移动机器人生成并保存日志报告。 | 是 | `reason`<br>（string） | - | 是 | 否 | 否
`pick` | `drop`<br><br>（如果自动执行） | 请求移动机器人拾取载荷。<br>具有多个载荷搬运装置的移动机器人可以并行处理多个拾取操作。<br>在这种情况下，必须提供参数 `lhd`（例如 `"LHD1"`）。<br>参数 `stationType` 说明拾取操作的具体处理方式（例如地面位置、货架位置、无动力输送机、动力输送机等）。<br>载荷类型提供载荷单元的信息，例如可用于切换安全区域（如 `"EPAL"`、`"INDU"` 等）。<br>为准备载荷搬运装置（例如根据 `height` 参数进行预提升操作），可以提前在预测区中公布该动作。<br>但是，由于相关节点尚未释放，预提升操作等不会在移动机器人状态中报告为 `'RUNNING'`。<br>如果动作位于边上，车辆可以使用传感装置检测拾取节点的位置。 | 否 | `lhd`（string，可选）<br>`stationType`（string）<br>`stationName`（string，可选）<br>`loadType`（string）<br>`loadId`（string，可选）<br>`height`（float64，可选），定义载荷底部相对于地面的高度<br>`depth`（float64，可选），用于叉车<br>`side`（string，可选），例如输送机 | `.load` | 否 | 是 | 是
`drop` | `pick`<br><br>（如果自动执行） | 请求移动机器人放下载荷。<br>更多详细信息参见动作 `pick`。 | 否 | `lhd`（string，可选）<br>`stationType`（string，可选）<br>`stationName`（string，可选）<br>`loadType`（string，可选）<br>`loadId`（string，可选）<br>`height`（float64，可选）<br>`depth`（float64，可选）<br>… | `.load` | 否 | 是 | 是
`detectObject` | - | 移动机器人检测对象（例如载荷、充电点、空闲停车位置）。 | 是 | `objectType`（string，可选） | - | 否 | 是 | 是
`finePositioning` | - | 在节点上，移动机器人会精确定位到目标。<br>移动机器人可以偏离其节点位置。<br>在边上，移动机器人可以在通过边时与固定设备对齐。<br>即时动作：移动机器人开始精确定位到目标。 | 是 | `stationType`（string，可选）<br>`stationName`（string，可选） | - | 否 | 是 | 是
`waitForTrigger` | - | 移动机器人必须等待车辆上的触发器（例如按下按钮、手动装载）。<br>车队控制负责处理超时，并必须在必要时取消订单。 | 是 | `triggerType`（string） | - | 否 | 是 | 否
`cancelOrder` | - | 移动机器人尽快停止。<br>可以立即停止，也可以在下一个节点停止。<br>随后删除订单，并取消所有动作。 | 是 | - | - | 是 | 否 | 否
`factsheetRequest` | - | 请求移动机器人发送信息表。 | 是 | - | - | 是 | 否 | 否


### 6.8.2 预定义动作的状态

动作 | 动作状态
--- | :---:
 | `'INITIALIZING'`、`'RUNNING'`、`'PAUSED'`、`'FINISHED'`、`'FAILED'`

动作 | `'INITIALIZING'` | `'RUNNING'` | `'PAUSED'` | `'FINISHED'` | `'FAILED'`
--- | --- | --- | --- | --- | ---
`startPause` | - | 正在准备启动该模式。<br>如果移动机器人支持即时转换，可以省略此状态。 | - | 车辆静止。<br>所有动作都将暂停。<br>暂停模式已启动。<br>移动机器人报告 `.paused` 为 `true`。 | 由于某种原因无法启动暂停模式（例如被硬件开关覆盖）。
`stopPause` | - | 正在准备关闭该模式。<br>如果移动机器人支持即时转换，可以省略此状态。 | - | 暂停模式已关闭。<br>恢复所有暂停的动作。<br>移动机器人报告 `.paused` 为 `false`。 | 由于某种原因无法关闭暂停模式（例如被硬件开关覆盖）。
`startCharging` | - | 正在启动充电过程（正在与充电器通信）。<br>如果移动机器人支持即时转换，可以省略此状态。 | - | 充电过程已启动。<br>移动机器人报告 `.batteryState.charging` 为 `true`。 | 由于某种原因无法启动充电过程（例如未与充电器对准）。充电问题应当伴随相应错误。
`stopCharging` | - | 正在停止充电过程（正在与充电器通信）。<br>如果移动机器人支持即时转换，可以省略此状态。 | - | 充电过程已停止。<br>移动机器人报告 `.batteryState.charging` 为 `false`。 | 由于某种原因无法停止充电过程（例如未与充电器对准）。<br>充电问题应当伴随相应错误。
`initPosition` | - | 正在初始化新位姿（可信度检查等）。<br>如果移动机器人支持即时转换，可以省略此状态。 | - | 位姿已重置。<br>移动机器人报告：<br>`.agvPosition.x = x`<br>`.agvPosition.y = y`<br>`.agvPosition.theta = theta`<br>`.agvPosition.mapId = mapId`<br>`.lastNodeId = lastNodeId` | 位姿无效或无法重置。<br>一般定位问题应当伴随相应错误。
`downloadMap` | 初始化与地图服务器的连接。 | 移动机器人正在下载地图，直至下载完成。 | - | 移动机器人设置 `mapId`/`mapVersion`，并将相应 `mapStatus` 设置为 `'DISABLED'`，以更新其状态。 | 下载失败，并在车辆状态中更新（例如连接丢失、地图服务器不可达，或地图服务器上不存在相应 `mapId`/`mapVersion`）。
`enableMap` | - | 移动机器人启用具有所请求 `mapId` 和 `mapVersion` 的地图，同时禁用具有相同 `mapId` 的其他版本。 | - | 移动机器人将所请求地图的 `mapStatus` 更新为 `'ENABLED'`，并将具有相同 `mapId` 的其他版本更新为 `'DISABLED'`。 | 所请求的 `mapId`/`mapVersion` 组合不存在。
`deleteMap` | - | 移动机器人从内部存储器中删除具有所请求 `mapId` 和 `mapVersion` 的地图。 | - | 移动机器人从其状态中移除该 `mapId`/`mapVersion`。 | 如果地图当前正在使用，则无法删除。所请求的 `mapId`/`mapVersion` 组合之前已被删除。
`stateRequest` | - | - | - | 状态已传输。 | -
`logReport` | - | 正在生成报告。<br>如果移动机器人支持即时生成，可以省略此状态。 | - | 报告已保存。<br>日志名称将在状态中报告。 | 无法保存报告（例如没有可用空间）。
`pick` | 正在初始化拾取过程，例如尚待执行的提升操作。 | 正在执行拾取过程（移动机器人正在驶入工位、载荷搬运装置正忙、正在与工位通信等）。 | 拾取过程已暂停，例如因违反安全区域要求而暂停。<br>消除违规情况后，拾取过程继续。 | 拾取完成。<br>载荷已进入移动机器人，移动机器人报告新的载荷状态。 | 拾取失败，例如工位意外为空。<br>拾取操作失败应当伴随相应错误。
`drop` | 正在初始化放置过程，例如尚待执行的提升操作。 | 正在执行放置过程（移动机器人正在驶入工位、载荷搬运装置正忙、正在与工位通信等）。 | 放置过程已暂停，例如因违反安全区域要求而暂停。<br>消除违规情况后，放置过程继续。 | 放置完成。<br>载荷已离开移动机器人，移动机器人报告新的载荷状态。 | 放置失败，例如工位意外被占用。<br>放置操作失败应当伴随相应错误。
`detectObject` | - | 正在检测对象。 | - | 已检测到对象。 | 移动机器人未能检测到对象。
`finePositioning` | - | 移动机器人正在精确定位到目标。 | 精确定位过程已暂停，例如因违反安全区域要求而暂停。<br>消除违规情况后，精确定位继续。 | 已到达相对于工位的目标位置。 | 未能到达相对于工位的目标位置。
`waitForTrigger` | - | 移动机器人正在等待触发器。 | - | 触发器已触发。 | 如果订单已取消，`waitForTrigger` 失败。
`cancelOrder` | - | 移动机器人正在停车，或行驶至下一个节点。 | - | 移动机器人已静止并取消订单。 | -
`factsheetRequest` | - | - | - | 信息表已传输。 | -


## 6.9 `instantActions` 主题（从车队控制到移动机器人）

在某些情况下，需要向移动机器人发送必须立即执行的动作。
这可以通过向 `instantActions` 主题发布 `instantAction` 消息来实现。
这些动作不得与移动机器人当前订单的内容冲突（例如，`instantAction` 要求降下货叉，而订单要求升起货叉）。

即时动作可能适用的一些示例包括：

- 暂停移动机器人，但不更改当前订单；
- 暂停后恢复订单；
- 激活信号（光学、音频等）。

更多信息参见 [7 最佳实践](#7-最佳实践)。

对象结构 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>协议头 ID 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"`）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。
actions [action] | array | 需要立即执行且不属于常规订单的动作数组。

移动机器人收到 `instantAction` 时，应向移动机器人状态的 `actionStates` 数组中添加相应的 `actionState`。
`actionStatus` 会根据动作进度更新。
`actionStatus` 的不同转换另见图 16。


## 6.10 `state` 主题（从移动机器人到车队控制）

移动机器人状态只通过一个主题传输。
与订单、电池状态和错误等分别使用不同消息相比，使用一个主题可以减少代理服务器和车队控制处理消息的工作量，同时保持移动机器人状态信息同步。

移动机器人状态消息应在相关事件发生时发布，或最迟每 30 秒通过 MQTT 代理服务器发布给车队控制。

触发传输状态消息的事件包括：

- 收到订单
- 收到订单更新
- 载荷状态发生变化
- 发生错误或警告
- 驶过节点
- 切换操作模式
- `driving` 字段发生变化
- `nodeStates`、`edgeStates` 或 `actionStates` 发生变化
- `maps` 字段发生变化

应尽力控制通信量。
如果两个事件彼此相关（例如收到新订单通常会导致 `nodeStates` 和 `edgeStates` 更新，驶过节点也会导致相同情况），则合理的做法是触发一次状态更新，而不是多次更新。


### 6.10.1 概念与逻辑

订单进度由 `nodeStates` 和 `edgeStates` 跟踪。
此外，如果移动机器人能够推导出当前位置，则可以通过 `agvPosition` 字段发布其位置。

如果移动机器人自行规划路径，则应通过状态消息中的 `trajectory` 对象，以 NURBS 形式传输计算得到的轨迹（包括基础区和预测区），除非车队控制无法使用该字段，且双方在集成期间已约定不发送该字段。
节点一旦由车队控制释放，移动机器人不得更改其轨迹。

`nodeStates` 和 `edgeStates` 包含移动机器人仍需通过的所有节点和边。

![图 14 `state` 主题提供的订单信息。仅传输最后一个节点的 ID 以及剩余的节点和边](./assets/order_information_state_topic.png)
>图 14 - `state` 主题提供的订单信息。仅传输最后一个节点的 ID 以及剩余的节点和边



### 6.10.2 通过节点和进入/离开边，以及动作触发

移动机器人自行决定何时将节点视为已通过。
通常，移动机器人的控制点应位于节点的 `allowedDeviationXY` 范围内，且其朝向角应位于 `allowedDeviationTheta` 范围内。
如果后续边设置了 `corridor` 边属性，还应满足该走廊的边界。

移动机器人通过从 `nodeStates` 数组中移除相应的 `nodeState`，并将 `lastNodeId` 和 `lastNodeSequenceId` 设置为已通过节点的值，来报告节点已通过。

移动机器人一旦报告节点已通过，就应触发与该节点关联的动作（如有）。

通过节点也表示离开通往该节点的边。
随后应从 `edgeStates` 中移除该边，并完成在该边上处于活动状态的动作。

通过节点还表示移动机器人进入后续边（如有）的时刻。
此时应触发该边上的动作。
如果移动机器人必须在边上暂停（由于软或硬阻塞边，或其他原因），则例外：移动机器人在重新开始移动后才进入该边。

![图 15 订单处理期间 `nodeStates`、`edgeStates` 和 `actionStates` 的示意图](./assets/states_during_order_handling.png)
>图 15 - 订单处理期间 `nodeStates`、`edgeStates` 和 `actionStates` 的示意图


### 6.10.3 基础区请求

如果移动机器人检测到基础区即将耗尽，可以将 `newBaseRequest` 标志设置为 `true`，以避免不必要的制动。


### 6.10.4 信息

移动机器人可以通过 `information` 数组向车队控制提交任意附加信息。
移动机器人通过信息消息报告信息的持续时间由移动机器人自行决定。

车队控制不得将信息消息用于逻辑处理；该消息只能用于可视化和调试。


### 6.10.5 错误

移动机器人通过 `errors` 数组报告错误。
错误有两个级别：`'WARNING'` 和 `'FATAL'`。
`'WARNING'` 表示可自行恢复的错误，例如字段违规。
`'FATAL'` 错误需要人工干预。
错误可以通过 `errorReferences` 数组传递有助于查找错误原因的关联项。


### 6.10.6 状态消息的实现

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
headerId |  | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp |  | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"`）。
version |  | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer |  | string | 移动机器人制造商。
serialNumber |  | string | 移动机器人的序列号。
*maps[map]* |  | array | 当前存储在车辆上的地图对象数组。
orderId |  | string | 当前订单或上一个已完成订单的唯一订单标识符。<br>在收到新订单之前保留该 `orderId`。<br>如果没有可用的上一个 `orderId`，则为空字符串（`""`）。
orderUpdateId |  | uint32 | 用于标识移动机器人已接受订单更新的订单更新标识符。<br>如果没有可用的上一个 `orderUpdateId`，则为 `0`。
*zoneSetId* |  | string | 移动机器人当前用于路径规划的区域集唯一 ID。<br>应与订单中使用的值相同。<br><br>可选：如果移动机器人不使用区域，可以省略此字段。
lastNodeId |  | string | 最后到达节点的节点 ID，或者如果移动机器人当前位于节点上，则为当前节点的节点 ID（例如 `"node7"`）。如果没有可用的 `lastNodeId`，则为空字符串（`""`）。
lastNodeSequenceId |  | uint32 | 最后到达节点的序列 ID，或者如果移动机器人当前位于节点上，则为当前节点的序列 ID。<br>如果没有可用的 `lastNodeSequenceId`，则为 `0`。
**nodeStates [nodeState]** |  | array | 为完成订单而需要通过的 `nodeState` 对象数组。<br>空闲时为空数组。
**edgeStates [edgeState]** |  | array | 为完成订单而需要通过的 `edgeState` 对象数组。<br>空闲时为空数组。
***agvPosition*** |  | JSON object | 移动机器人当前在地图上的位置。<br><br>可选：没有自定位能力的移动机器人（例如线导引移动机器人）可以省略该字段。
***velocity*** |  | JSON object | 移动机器人在车辆坐标系中的速度。
***loads [load]*** |  | array | 当前由移动机器人搬运的载荷。<br><br>可选：如果移动机器人无法确定载荷状态，则应完全省略该字段，不得将其报告为空数组。<br>如果移动机器人能够确定载荷状态但数组为空，则认为移动机器人未载货。
driving |  | boolean | `true`：表示移动机器人正在行驶和/或旋转。不包含移动机器人的其他运动（例如升降运动）。<br>`false`：表示移动机器人既未行驶也未旋转。
*paused* |  | boolean | `true`：移动机器人当前处于暂停状态，原因可能是按下移动机器人上的实体按钮或执行即时动作。<br>移动机器人可以恢复订单。<br><br>`false`：移动机器人当前未处于暂停状态。
*newBaseRequest* |  | boolean | `true`：移动机器人即将到达基础区末端，如果未传输新的基础区，移动机器人将降低速度。<br>这是车队控制发送新基础区的触发信号。<br><br>`false`：不需要更新基础区。
*distanceSinceLastNode* | meter | float64 | 供线导引车辆指示其从 `lastNodeId` 之后行驶的距离。<br>距离单位为米。
**actionStates [actionState]** |  | array | 包含当前订单中的所有动作，以及自上一个订单以来收到的所有即时动作。动作状态会一直保留到收到新订单为止。除正在运行的即时动作外，其他动作状态在收到新订单时移除。<br>其中可能包含来自先前节点、但仍在执行的动作。<br><br>动作完成时，应发布更新后的状态消息，将 `actionStatus` 设置为 `'FINISHED'`，并在适用时给出相应的 `resultDescription`。
**batteryState** |  | JSON object | 包含所有电池相关信息。
operatingMode |  | string | 枚举 {'AUTOMATIC', 'SEMIAUTOMATIC', 'MANUAL', 'SERVICE', 'TEACHIN'}。<br>更多信息见本节的“操作模式描述”表。
**errors [error]** |  | array | 错误对象数组。<br>所有活动错误都应位于该数组中。<br>空数组表示移动机器人没有活动错误。
***information [info]*** |  | array | 信息对象数组。<br>空数组表示移动机器人没有信息。<br>该字段只能用于可视化或调试，不得用于车队控制的逻辑处理。
**safetyState** |  | JSON object | 包含所有安全相关信息。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**map** { |  | JSON object |
mapId |  | string | 描述车辆工作区中一个已定义区域的地图 ID。
mapVersion |  | string | 地图版本。
*mapDescription* |  | string | 附加地图信息。
mapStatus<br> } |  | string | 枚举 {'ENABLED', 'DISABLED'}。<br>'ENABLED'：表示该地图当前在移动机器人上处于活动状态/正在使用。同一 `mapId` 最多只能有一个地图的状态为 `'ENABLED'`。<br>'DISABLED'：表示该地图版本当前未在移动机器人上启用，因此可以根据请求启用或删除。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**nodeState** { |  | JSON object |
nodeId |  | string | 节点的唯一标识符。
sequenceId |  | uint32 | 用于区分具有相同 `nodeId` 的多个节点的序列 ID。
*nodeDescription* |  | string | 节点的附加信息。
released |  | boolean | `true` 表示该节点是基础区的一部分。<br>`false` 表示该节点属于预测区。
***nodePosition***<br><br> } |  | JSON object | 节点位置。<br>该对象定义见 [6.6 `order` 主题](#66-order-主题从车队控制到移动机器人)。<br><br>可选：<br>车队控制拥有此信息。<br>也可以出于调试等目的附加发送。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**edgeState** { |  | JSON object |
edgeId |  | string | 边的唯一标识符。
sequenceId |  | uint32 | 用于区分具有相同 `edgeId` 的多个边的序列 ID。
*edgeDescription* |  | string | 边的附加信息。
released |  | boolean | `true` 表示该边是基础区的一部分。<br>`false` 表示该边属于预测区。
***trajectory***<br><br> } |  | JSON object | 轨迹以 NURBS 形式传输，定义见 [6.6.6 订单消息的实现](#666-订单消息的实现)。<br><br>轨迹段从车辆进入边的点开始，并在车辆报告已通过结束节点的点终止。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**agvPosition** { |  | JSON object | 以世界坐标定义地图上的位置。每个楼层都有自己的地图。
positionInitialized |  | boolean | `true`：位置已初始化。<br>`false`：位置未初始化。
*localizationScore* |  | float64 | 范围：[0.0 ... 1.0]<br><br>描述定位质量，因此可由 SLAM 移动机器人用于描述当前位置的准确程度。<br><br>0.0：位置未知。<br>1.0：位置已知。<br><br>无法估计定位得分的车辆可选。<br><br>仅用于日志记录和可视化。
*deviationRange* | m | float64 | 位置偏差范围值，单位为米。<br><br>无法估计偏差范围的车辆可选，例如基于栅格定位的车辆。<br><br>仅用于日志记录和可视化。
x | m | float64 | 地图上相对于地图坐标系的 x 位置。<br>精度取决于具体实现。
y | m | float64 | 地图上相对于地图坐标系的 y 位置。<br>精度取决于具体实现。
theta |  | float64 | 范围：[-Pi ... Pi]<br><br>移动机器人的朝向角。
mapId |  | string | 该位置所引用地图的唯一标识符。<br><br>每张地图都具有同一个坐标原点。<br>当移动机器人使用电梯从出发楼层前往目标楼层时，它会离开出发楼层的地图，并出现在目标楼层地图中对应的电梯节点上。
*mapDescription*<br> } |  | string | 地图的附加信息。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**velocity** { |  | JSON object |
*vx* | m/s | float64 | 移动机器人 x 方向的速度。
*vy* | m/s | float64 | 移动机器人 y 方向的速度。
*omega*<br> } | Rad/s | float64 | 绕移动机器人 z 轴的旋转速度。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**load** { |  | JSON object |
*loadId* |  | string | 载荷的唯一标识符（例如条形码或 RFID）。<br><br>如果移动机器人能够识别载荷但尚未识别，则为空字段。<br><br>如果移动机器人无法识别载荷，则为可选字段。
*loadType* |  | string | 载荷类型。
*loadPosition* |  | string | 指示移动机器人使用的载荷搬运/承载单元，例如移动机器人有多个载荷承载位置时。<br><br>示例：`"front"`、`"back"`、`"positionC1"` 等。<br><br>只有一个载荷位置的车辆可选。
***boundingBoxReference*** |  | JSON object | 包围框位置的参考点。<br>该参考点始终是包围框底面的中心（高度 = 0），并以移动机器人坐标系中的坐标表示。
***loadDimensions*** |  | JSON object | 以米为单位表示的载荷包围框尺寸。
*weight*<br> } | kg | float64 | 范围：[0.0 ... float64.max]<br><br>以千克为单位测量的载荷绝对重量。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**boundingBoxReference** { |  | JSON object | 包围框位置的参考点。<br>该参考点始终是包围框底面的中心（高度 = 0），并以移动机器人坐标系中的坐标表示。
x |  | float64 | 参考点的 x 坐标。
y |  | float64 | 参考点的 y 坐标。
z |  | float64 | 参考点的 z 坐标。
*theta*<br> } |  | float64 | 载荷包围框的朝向角。<br>对牵引车、列车等尤其重要。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**loadDimensions** { |  | JSON object | 载荷包围框的尺寸，单位为米。
length | m | float64 | 载荷包围框的绝对长度。
width | m | float64 | 载荷包围框的绝对宽度。
*height*<br> } | m | float64 | 载荷包围框的绝对高度。<br><br>可选：<br><br>仅在已知时设置该值。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**actionState** { |  | JSON object |
actionId |  | string | 动作的唯一标识符。
*actionType* |  | string | 动作类型。<br><br>可选：仅用于信息或可视化。车队控制已知订单中分发的动作类型。
*actionDescription* |  | string | 当前动作的附加信息。
actionStatus |  | string | 枚举 {'WAITING', 'INITIALIZING', 'RUNNING', 'PAUSED', 'FINISHED', 'FAILED'}。<br><br>参见 [6.11 动作状态](#611-动作状态)。
*resultDescription*<br> } |  | string | 结果描述，例如 RFID 读取结果。<br><br>错误将在 `errors` 中传输。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**batteryState** { |  | JSON object |
batteryCharge | % | float64 | 荷电状态。<br>如果移动机器人只能提供好或坏两种电池电量状态，则分别表示为 20%（差）和 80%（好）。
*batteryVoltage* | V | float64 | 电池电压。
*batteryHealth* | % | int8 | 范围：[0 ... 100]<br><br>描述电池健康状况的状态。
charging |  | boolean | `true`：正在充电。<br>`false`：移动机器人当前未充电。
*reach*<br> } | m | uint32 | 范围：[0 ... uint32.max]<br><br>按当前荷电状态估算的可达距离。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**error** { |  | JSON object |
errorType |  | string | 错误的类型/名称。
***errorReferences [errorReference]*** |  | array | 提供与错误相关更多信息的关联项数组（例如 `nodeId`、`edgeId`、`orderId`、`actionId` 等）。<br>更多信息参见 [7 最佳实践](#7-最佳实践)。
*errorDescription* |  | string | 提供错误详细信息和可能原因的详细描述。
*errorHint* |  | string | 关于如何处理或解决所报告错误的提示。
errorLevel<br> } |  | string | 枚举 {'WARNING', 'FATAL'}。<br><br>'WARNING'：移动机器人已准备好启动（例如维护周期到期警告）。<br>'FATAL'：移动机器人不处于运行条件，需要用户干预（例如激光扫描仪被污染）。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**errorReference** { |  | JSON object |
referenceKey |  | string | 指定所使用关联项的类型（例如 `nodeId`、`edgeId`、`orderId`、`actionId` 等）。
referenceValue<br> } |  | string | 与关联项键对应的值。例如发生错误的节点 ID。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**info** { |  | JSON object |
infoType |  | string | 信息的类型/名称。
*infoReferences [infoReference]* |  | array | 关联项数组。
*infoDescription* |  | string | 信息描述。
infoLevel<br> } |  | string | 枚举 {'DEBUG', 'INFO'}。<br><br>'DEBUG'：用于调试。<br>'INFO'：用于可视化。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**infoReference** { |  | JSON object |
referenceKey |  | string | 关联项的类型（例如 `headerId`、`orderId`、`actionId` 等）。
referenceValue<br> } |  | string | 与关联项键对应的值。

对象结构 | 单位 | 数据类型 | 描述
--- | --- | --- | ---
**safetyState** { |  | JSON object |
eStop |  | string | 枚举 {'AUTOACK', 'MANUAL', 'REMOTE', 'NONE'}。<br><br>急停的确认类型：<br>'AUTOACK'：已激活可自动确认的急停，例如由保险杠或保护区域触发。<br>'MANUAL'：必须在车辆处手动确认急停。<br>'REMOTE'：必须远程确认设施急停。<br>'NONE'：未激活急停。
fieldViolation<br> } |  | boolean | 保护区域违规。<br>`true`：保护区域被违反。<br>`false`：保护区域未被违反。

#### 操作模式描述

以下描述列出了 `state` 主题的 `operatingMode`。

标识 | 描述
--- | ---
'AUTOMATIC' | 移动机器人完全由车队控制进行控制。<br>移动机器人根据车队控制的订单行驶并执行动作。
'SEMIAUTOMATIC' | 移动机器人由车队控制进行控制。<br>移动机器人根据车队控制的订单行驶并执行动作。<br>行驶速度由 HMI 控制（速度不能超过自动模式的速度）。<br>转向由自动控制（可以使用非安全 HMI）。
'MANUAL' | 车队控制不控制移动机器人。<br>车队控制不向移动机器人发送行驶订单或动作。<br>HMI 可用于控制移动机器人的转向、速度和载荷搬运装置。<br>移动机器人的位置发送给车队控制。<br>移动机器人进入或离开该模式时，会立即清除所有订单（需要安全 HMI）。
'SERVICE' | 车队控制不控制移动机器人。<br>车队控制不向移动机器人发送行驶订单或动作。<br>授权人员可以重新配置移动机器人。
'TEACHIN' | 车队控制不控制移动机器人。<br>车队控制不向移动机器人发送行驶订单或动作。<br>移动机器人处于示教过程中，例如由车队控制执行地图绘制。

>表 1 - 操作模式及其含义


## 6.11 动作状态

移动机器人收到 `action`（该动作附加到 `node` 或 `edge`，或通过 `instantAction` 发送）时，应在其 `actionStates` 数组中使用 `actionState` 表示该动作。

`actionStates` 通过 `actionStatus` 字段描述动作当前处于生命周期的哪个阶段。

表 2 描述了枚举 `actionStatus` 可以取的值。

actionStatus | 描述
--- | ---
'WAITING' | 移动机器人已收到动作，但尚未到达触发该动作的节点，或尚未进入该动作处于活动状态的边。
'INITIALIZING' | 动作已触发，准备措施已启动。
'RUNNING' | 动作正在运行。
'PAUSED' | 动作因暂停即时动作或外部触发器（移动机器人上的暂停按钮）而暂停。
'FINISHED' | 动作已完成。<br>通过 `resultDescription` 报告结果。
'FAILED' | 无论出于什么原因，动作都无法完成。

>表 2 - `actionStatus` 字段的可接受值

状态转换图见图 16。

![图 16 `actionStates` 的所有可能状态转换](./assets/action_state_transition.png)
>图 16 - `actionStates` 的所有可能状态转换


## 6.12 动作阻塞类型与顺序

列表中多个动作的顺序定义了应执行这些动作的顺序。
动作的并行执行由各自的 `blockingType` 控制。

动作有三种不同的阻塞类型，见表 3。

blockingType | 描述
--- | ---
'NONE' | 动作可以与其他动作并行执行，也可以在车辆行驶时执行。
'SOFT' | 动作可以与其他动作并行执行。车辆不得行驶。
'HARD' | 动作不得与其他动作并行执行。车辆不得行驶。

>表 3 - 动作阻塞类型

如果同一节点上有多个具有不同阻塞类型的动作，图 17 描述了移动机器人应如何处理这些动作。

![图 17 处理多个动作](./assets/handling_multiple_actions.png)
>图 17 - 处理多个动作


## 6.13 `visualization` 主题

为近实时更新位置，移动机器人可以在 `visualization` 主题上广播其位置和速度。

位置对象的结构与状态中的位置和速度对象相同。
有关车辆状态的更多信息参见 [6.10.6 状态消息的实现](#6106-状态消息的实现)。
该主题的更新速率由系统集成方定义。


## 6.14 `connection` 主题

移动机器人客户端连接代理服务器期间，可以设置遗嘱主题和遗嘱消息；移动机器人客户端与代理服务器断开连接时，代理服务器会发布该主题和消息。
因此，车队控制可以通过订阅所有移动机器人的 `connection` 主题来检测断开连接事件。
断开连接通过代理服务器与客户端之间交换的心跳来检测。
大多数代理服务器都可以配置该间隔，建议设置为约 15 秒。
`connection` 主题的服务质量级别应为 1，即至少一次。

建议的遗嘱主题结构为：

**uagv/v2/manufacturer/SN/connection**

遗嘱消息定义为以下字段组成的 JSON 封装消息：

标识符 | 数据类型 | 描述
--- | --- | ---
headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。
timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"）。
version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。
manufacturer | string | 移动机器人制造商。
serialNumber | string | 移动机器人的序列号。
connectionState | string | 枚举 {'ONLINE', 'OFFLINE', 'CONNECTIONBROKEN'}。<br><br>'ONLINE'：移动机器人与代理服务器之间的连接处于活动状态。<br><br>'OFFLINE'：移动机器人与代理服务器之间的连接以协调方式离线。<br><br>'CONNECTIONBROKEN'：移动机器人与代理服务器之间的连接意外结束。

使用 MQTT 断开连接命令以正常方式结束连接时，不会发送遗嘱消息。
仅当连接意外中断时，代理服务器才发送遗嘱消息。

**注意**：由于 MQTT 遗嘱功能的性质，遗嘱消息在移动机器人与 MQTT 代理服务器建立连接期间定义。
因此，`timestamp` 和 `headerId` 字段始终会过期。

移动机器人希望正常断开连接：

1. 移动机器人发送主题 `uagv/v2/manufacturer/SN/connection`，并将 `connectionState` 设置为 `'OFFLINE'`。
2. 使用断开连接命令断开 MQTT 连接。

移动机器人上线：

1. 创建 MQTT 连接时，将遗嘱设置为 `uagv/v2/manufacturer/SN/connection`，并将字段 `connectionState` 设置为 `'CONNECTIONBROKEN'`。
2. 发送主题 `uagv/v2/manufacturer/SN/connection`，并将 `connectionState` 设置为 `'ONLINE'`。

该主题上的所有消息均应设置 retained 标志。

移动机器人与代理服务器之间的连接意外中断时，代理服务器会发送遗嘱主题 `uagv/v2/manufacturer/SN/connection`，并将字段 `connectionState` 设置为 `'CONNECTIONBROKEN'`。


## 6.15 `factsheet` 主题

信息表提供特定移动机器人系列的基本信息。
这些信息可以用于比较不同类型的移动机器人，也可以应用于移动机器人系统的规划、尺寸设计和仿真。
信息表还包含将移动机器人系列集成到符合 VDA 5050 的车队控制所需的移动机器人通信接口信息。

信息表中某些字段的值只能在系统集成期间指定，例如项目特定的载荷类型和工位类型的分配，以及该移动机器人支持的工位类型和载荷类型列表。

信息表既面向人类阅读，也面向机器处理（例如由车队控制应用导入），因此将其指定为 JSON 文档。

车队控制可以通过发送即时动作 `factsheetRequest` 向移动机器人请求信息表。

该主题上的所有消息均应设置 retained 标志。


### 6.15.1 信息表 JSON 结构

信息表由下表所列 JSON 对象组成。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| headerId | uint32 | 消息的协议头 ID。<br>`headerId` 按主题分别定义，每发送一条消息时递增 1（但接收方不一定收到该消息）。 |
| timestamp | string | 时间戳（ISO 8601、UTC），格式为 `YYYY-MM-DDTHH:mm:ss.ffZ`（例如 `"2017-04-15T11:40:03.12Z"`）。 |
| version | string | 协议版本 `[Major].[Minor].[Patch]`（例如 `"1.3.2"`）。 |
| manufacturer | string | 移动机器人制造商。 |
| serialNumber | string | 移动机器人的序列号。 |
| **typeSpecification** | JSON object | 通常用于指定移动机器人类别及其能力的参数。 |
| **physicalParameters** | JSON object | 指定移动机器人基本物理属性的参数。 |
| **protocolLimits** | JSON object | MQTT 通信中标识符、数组、字符串等的长度限制。 |
| **protocolFeatures** | JSON object | VDA 5050 协议支持的功能。 |
| **agvGeometry** | JSON object | 移动机器人几何的详细定义。 |
| **loadSpecification** | JSON object | 载荷能力的抽象说明。 |
| ***vehicleConfig*** | JSON object | 移动机器人当前软硬件版本的汇总以及可选的网络信息。 |

#### `typeSpecification`

该 JSON 对象描述移动机器人类型的一般属性。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| seriesName | string | 制造商指定的系列名称，为自由文本。 |
| *seriesDescription* | string | 移动机器人系列的人类可读描述，为自由文本。 |
| agvKinematic | string | 移动机器人运动学类型的简化描述。<br>枚举 {'DIFF', 'OMNI', 'THREEWHEEL'}。<br>'DIFF'：差动驱动；<br>'OMNI'：全向车辆；<br>'THREEWHEEL'：三轮驱动车辆或具有类似运动学的车辆。 |
| agvClass | string | 移动机器人类别的简化描述。<br>枚举 {'FORKLIFT', 'CONVEYOR', 'TUGGER', 'CARRIER'}。<br>'FORKLIFT'：叉车；<br>'CONVEYOR'：带输送机的移动机器人；<br>'TUGGER'：牵引车；<br>'CARRIER'：带或不带升降装置的载荷承载车。 |
| maxLoadMass | float64 | [kg] 可装载的最大质量。 |
| localizationTypes | array of string | 定位类型的简化描述。<br>示例值：<br>'NATURAL'：自然地标；<br>'REFLECTOR'：激光反射器；<br>'RFID'：RFID 标签；<br>'DMC'：数据矩阵码；<br>'SPOT'：磁点；<br>'GRID'：磁栅格。 |
| navigationTypes | array of string | 移动机器人支持的路径规划类型数组，按优先级排序。<br>示例值：<br>'PHYSICAL_LINE_GUIDED'：无需路径规划，沿物理安装路径行驶；<br>'VIRTUAL_LINE_GUIDED'：沿固定（虚拟）路径行驶；<br>'AUTONOMOUS'：自主规划路径。 |

#### `physicalParameters`

该 JSON 对象描述移动机器人的物理属性。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| speedMin | float64 | [m/s] 移动机器人的最小受控连续速度。 |
| speedMax | float64 | [m/s] 移动机器人的最大速度。 |
| *angularSpeedMin* | float64 | [Rad/s] 移动机器人的最小受控连续旋转速度。 |
| *angularSpeedMax* | float64 | [Rad/s] 移动机器人的最大旋转速度。 |
| accelerationMax | float64 | [m/s²] 最大载荷下的最大加速度。 |
| decelerationMax | float64 | [m/s²] 最大载荷下的最大减速度。 |
| heightMin | float64 | [m] 移动机器人的最小高度。 |
| heightMax | float64 | [m] 移动机器人的最大高度。 |
| width | float64 | [m] 移动机器人的宽度。 |
| length | float64 | [m] 移动机器人的长度。 |

#### `protocolLimits`

该 JSON 对象描述移动机器人的协议限制。
如果参数未定义或设置为零，则该参数没有显式限制。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| **maxStringLens** { | JSON object | 字符串的最大长度。 |
| &emsp;*msgLen* | uint32 | MQTT 消息的最大长度。 |
| &emsp;*topicSerialLen* | uint32 | MQTT 主题中序列号部分的最大长度。<br><br>受影响的参数：<br>`order.serialNumber`<br>`instantActions.serialNumber`<br>`state.serialNumber`<br>`visualization.serialNumber`<br>`connection.serialNumber` |
| &emsp;*topicElemLen* | uint32 | MQTT 主题中所有其他部分的最大长度。<br><br>受影响的参数：<br>`order.timestamp`<br>`order.version`<br>`order.manufacturer`<br>`instantActions.timestamp`<br>`instantActions.version`<br>`instantActions.manufacturer`<br>`state.timestamp`<br>`state.version`<br>`state.manufacturer`<br>`visualization.timestamp`<br>`visualization.version`<br>`visualization.manufacturer`<br>`connection.timestamp`<br>`connection.version`<br>`connection.manufacturer` |
| &emsp;*idLen* | uint32 | ID 字符串的最大长度。<br><br>受影响的参数：<br>`order.orderId`<br>`order.zoneSetId`<br>`node.nodeId`<br>`nodePosition.mapId`<br>`action.actionId`<br>`edge.edgeId`<br>`edge.startNodeId`<br>`edge.endNodeId` |
| &emsp;*idNumericalOnly* | boolean | 如果为 `true`，ID 字符串只能包含数字。 |
| &emsp;*enumLen* | uint32 | 枚举值和键字符串的最大长度。<br><br>受影响的参数：<br>`action.actionType`、`action.blockingType`<br>`edge.direction`<br>`actionParameter.key`<br>`state.operatingMode`<br>`load.loadPosition`<br>`load.loadType`<br>`actionState.actionStatus`<br>`error.errorType`<br>`error.errorLevel`<br>`errorReference.referenceKey`<br>`info.infoType`<br>`info.infoLevel`<br>`safetyState.eStop`<br>`connection.connectionState` |
| &emsp;*loadIdLen* | uint32 | `loadId` 字符串的最大长度。 |
| } |  |  |
| **maxArrayLens** { | JSON object | 数组的最大长度。 |
| &emsp;*order.nodes* | uint32 | 移动机器人可处理的每个订单的最大节点数。 |
| &emsp;*order.edges* | uint32 | 移动机器人可处理的每个订单的最大边数。 |
| &emsp;*node.actions* | uint32 | 移动机器人可处理的每个节点的最大动作数。 |
| &emsp;*edge.actions* | uint32 | 移动机器人可处理的每条边的最大动作数。 |
| &emsp;*actions.actionsParameters* | uint32 | 移动机器人可处理的每个动作的最大参数数。 |
| &emsp;*instantActions* | uint32 | 移动机器人可处理的每条消息中的最大即时动作数。 |
| &emsp;*trajectory.knotVector* | uint32 | 移动机器人可处理的每条轨迹中的最大节点值数量。 |
| &emsp;*trajectory.controlPoints* | uint32 | 移动机器人可处理的每条轨迹的最大控制点数。 |
| &emsp;*state.nodeStates* | uint32 | 移动机器人发送的最大 `nodeStates` 数，即移动机器人基础区中的最大节点数。 |
| &emsp;*state.edgeStates* | uint32 | 移动机器人发送的最大 `edgeStates` 数，即移动机器人基础区中的最大边数。 |
| &emsp;*state.loads* | uint32 | 移动机器人发送的最大载荷对象数。 |
| &emsp;*state.actionStates* | uint32 | 移动机器人发送的最大 `actionStates` 数。 |
| &emsp;*state.errors* | uint32 | 移动机器人在一条状态消息中发送的最大错误数。 |
| &emsp;*state.information* | uint32 | 移动机器人在一条状态消息中发送的最大信息数。 |
| &emsp;*error.errorReferences* | uint32 | 移动机器人为每个错误发送的最大错误关联项数。 |
| &emsp;*information.infoReferences* | uint32 | 移动机器人为每条信息发送的最大信息关联项数。 |
| } |  |  |
| **timing** { | JSON object | 时间信息。 |
| &emsp;minOrderInterval | float32 | [s] 向移动机器人发送订单消息的最小间隔。 |
| &emsp;minStateInterval | float32 | [s] 发送状态消息的最小间隔。 |
| &emsp;*defaultStateInterval* | float32 | [s] 发送状态消息的默认间隔；*如果未定义，则使用主文档中的默认值*。 |
| &emsp;*visualizationInterval* | float32 | [s] 向 `visualization` 主题发送消息的默认间隔。 |
| } |  |  |

#### `protocolFeatures`

该 JSON 对象定义移动机器人支持的动作和参数。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| **optionalParameters [optionalParameter]** | array of JSON object | 移动机器人支持和/或要求的可选参数数组。<br>未在此列出的可选参数视为移动机器人不支持。 |
| { |  |  |
| &emsp;parameter | string | 可选参数的全名，例如 `"order.nodes.nodePosition.allowedDeviationTheta"`。 |
| &emsp;support | enum | 可选参数的支持类型，可取以下值：<br>'SUPPORTED'：按照规范支持该可选参数。<br>'REQUIRED'：移动机器人正常运行需要该可选参数。 |
| &emsp;*description* | string | 自由文本：可选参数的描述，例如：<ul><li>说明此移动机器人类型为何需要可选参数 `direction`，以及该参数可以包含哪些值。</li><li>参数 `nodeMarker` 应仅包含无符号整数。</li><li>NURBS 支持仅限于直线段和圆弧段。</li></ul> |
| } |  |  |
| **agvActions [agvAction]** | array of JSON object | 此移动机器人支持的所有动作及其参数组成的数组，包括 VDA 5050 中规定的标准动作和制造商特定动作。 |
| { |  |  |
| &emsp;actionType | string | 与 `action.actionType` 对应的唯一动作类型。 |
| &emsp;*actionDescription* | string | 自由文本：动作描述。 |
| &emsp;actionScopes | array of enum | 可使用该动作类型的范围数组。<br><br>'INSTANT'：可用作 `instantAction`。<br>'NODE'：可用于节点。<br>'EDGE'：可用于边。<br><br>例如：`['INSTANT', 'NODE']`。 |
| &emsp;***actionParameters [actionParameter]*** | array of JSON object | 动作所具有的参数数组。<br>如果未定义，则该动作没有参数。<br>此处定义的 JSON 对象不同于 [6.6.6 订单消息的实现](#666-订单消息的实现)中用于节点和边的 JSON 对象。 |
| &emsp;*{* |  |  |
| &emsp;&emsp;key | string | 参数的键字符串。 |
| &emsp;&emsp;valueDataType | enum | 值的数据类型，可取：'BOOL'、'NUMBER'、'INTEGER'、'FLOAT'、'STRING'、'OBJECT'、'ARRAY'。 |
| &emsp;&emsp;*description* | string | 自由文本：参数描述。 |
| &emsp;&emsp;*isOptional* | boolean | `true`：可选参数。 |
| &emsp;*}* |  |  |
| &emsp;*resultDescription* | string | 自由文本：结果描述。 |
| &emsp;*blockingTypes* | array of enum | 为已定义动作指定的可能阻塞类型数组。<br>枚举 {'NONE', 'SOFT', 'HARD'}。 |
| *}* |  |  |

### `agvGeometry`

该 JSON 对象定义移动机器人的几何属性，例如轮廓和车轮位置。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| ***wheelDefinitions [wheelDefinition]*** | array of JSON object | 车轮数组，包含车轮布置和几何信息。 |
| { |  |  |
| &emsp;type | enum | 车轮类型。<br>枚举 {'DRIVE', 'CASTER', 'FIXED', 'MECANUM'}。 |
| &emsp;isActiveDriven | boolean | `true`：车轮为主动驱动轮。 |
| &emsp;isActiveSteered | boolean | `true`：车轮可主动转向。 |
| &emsp;**position** { | JSON object |  |
| &emsp;&emsp;x | float64 | [m] 移动机器人坐标系中的 x 位置。 |
| &emsp;&emsp;y | float64 | [m] 移动机器人坐标系中的 y 位置。 |
| &emsp;&emsp;*theta* | float64 | [rad] 移动机器人坐标系中车轮的朝向角。固定轮必须提供该字段。 |
| &emsp;} |  |  |
| &emsp;diameter | float64 | [m] 车轮的标称直径。 |
| &emsp;width | float64 | [m] 车轮的标称宽度。 |
| &emsp;*centerDisplacement* | float64 | [m] 车轮中心到旋转点的标称偏移量（脚轮需要）。<br>如果未定义该参数，则假定为 0。 |
| &emsp;*constraints* | string | 自由文本：制造商可以用来定义约束。 |
| } |  |  |
| ***envelopes2d [envelope2d]*** | array of JSON object | 二维移动机器人包络曲线数组，例如未载货和载货状态下的机械包络，以及不同速度情形下的安全区域。 |
| { |  |  |
| &emsp;set | string | 包络曲线集合的名称。 |
| &emsp;**polygonPoints [polygonPoint]** | array of JSON object | 以 x/y 多边形表示包络曲线；该多边形视为闭合多边形且不得自相交。 |
| &emsp;{ |  |  |
| &emsp;&emsp;x | float64 | [m] 多边形点的 x 位置。 |
| &emsp;&emsp;y | float64 | [m] 多边形点的 y 位置。 |
| &emsp;} |  |  |
| &emsp;*description* | string | 自由文本：包络曲线集合的描述。 |
| *}* |  |  |
| ***envelopes3d [envelope3d]*** | array of JSON object | 三维移动机器人包络曲线数组。 |
| *{* |  |  |
| &emsp;set | string | 包络曲线集合的名称。 |
| &emsp;format | string | 数据格式，例如 `"DXF"`。 |
| &emsp;***data*** | JSON object | 三维包络曲线数据，格式由 `format` 指定。 |
| &emsp;*url* | string | 下载三维包络曲线数据所使用的协议和 URL，例如 <ftp://xxx.yyy.com/ac4dgvhoif5tghji>。 |
| &emsp;*description* | string | 自由文本：包络曲线集合的描述。 |
| *}* |  |  |

#### `loadSpecification`

该 JSON 对象描述移动机器人的载荷搬运能力和支持的载荷类型。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| *loadPositions* | array of string | 移动机器人的载荷位置/载荷搬运装置数组。<br>该数组包含参数 `state.loads[].loadPosition` 的有效值，以及动作 `pick` 和 `drop` 中动作参数 `lhd` 的有效值。<br>*如果该数组不存在或为空，则移动机器人没有载荷搬运装置。* |
| ***loadSets [loadSet]*** | array of JSON object | 移动机器人能够搬运的载荷集数组。 |
| { |  |  |
| &emsp;setName | string | 载荷集的唯一名称，例如 `"DEFAULT"`、`"SET1"` 等。 |
| &emsp;loadType | string | 载荷类型，例如 `"EPAL"`、`"XLT1200"` 等。 |
| &emsp;*loadPositions* | array of string | 该载荷集所适用的载荷位置或载荷搬运装置数组。<br>*如果该参数不存在或为空，则该载荷集适用于此移动机器人上的所有载荷搬运装置。* |
| &emsp;***boundingBoxReference*** | JSON object | 状态消息参数 `loads[]` 中定义的包围框参考点。 |
| &emsp;***loadDimensions*** | JSON object | 状态消息参数 `loads[]` 中定义的载荷尺寸。 |
| &emsp;*maxWeight* | float64 | [kg] 该载荷类型的最大重量。 |
| &emsp;*minLoadhandlingHeight* | float64 | [m] 搬运该类型和重量的载荷时允许的最小高度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*maxLoadhandlingHeight* | float64 | [m] 搬运该类型和重量的载荷时允许的最大高度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*minLoadhandlingDepth* | float64 | [m] 该类型和重量的载荷允许的最小搬运深度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*maxLoadhandlingDepth* | float64 | [m] 该类型和重量的载荷允许的最大搬运深度，<br>以 `boundingBoxReference` 为参考。 |
| &emsp;*minLoadhandlingTilt* | float64 | [rad] 该类型和重量的载荷允许的最小搬运倾角。 |
| &emsp;*maxLoadhandlingTilt* | float64 | [rad] 该类型和重量的载荷允许的最大搬运倾角。 |
| &emsp;*agvSpeedLimit* | float64 | [m/s] 该类型和重量的载荷所允许的最大速度。 |
| &emsp;*agvAccelerationLimit* | float64 | [m/s²] 该类型和重量的载荷所允许的最大加速度。 |
| &emsp;*agvDecelerationLimit* | float64 | [m/s²] 该类型和重量的载荷所允许的最大减速度。 |
| &emsp;*pickTime* | float64 | [s] 拾取载荷的大致时间。 |
| &emsp;*dropTime* | float64 | [s] 放下载荷的大致时间。 |
| &emsp;*description* | string | 自由文本：载荷搬运集的描述。 |
| } |  |  |

#### `vehicleConfig`

该 JSON 对象详细说明车辆上运行的软件和硬件版本，并提供网络信息摘要。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| ***versions [versionInfo]*** | array of JSON object | 包含软硬件信息的键值对对象数组。 |
| *{* |  |  |
| &emsp;key | string | 所用软件或硬件版本的键（例如 `"softwareVersion"`）。 |
| &emsp;value | string | 与该键对应的版本（例如 `"v1.12.4-beta"`）。 |
| *}* |  |  |
| ***network*** { | JSON object | 车辆网络连接的信息。车辆运行期间不得更新所列信息。 |
| &emsp;*dnsServers* | array of string | 车辆使用的域名服务器（DNS）数组。 |
| &emsp;*ntpServers* | array of string | 车辆使用的网络时间协议（NTP）服务器数组。 |
| &emsp;*localIpAddress* | string | 预先分配、用于与 MQTT 代理服务器通信的 IP 地址。注意，运行期间不应修改或更改该 IP 地址。 |
| &emsp;*netmask* | string | 网络配置中与本地 IP 地址对应的子网掩码。 |
| &emsp;*defaultGateway* | string | 车辆使用的、与本地 IP 地址对应的默认网关。 |
| *}* |  |  |


# 7 最佳实践

本章包含附加信息，有助于在遵循协议逻辑的同时形成共同理解。

## 7.1 错误关联项

如果错误由错误订单引起，移动机器人应当在 `errorReferences` 字段中返回有意义的错误关联项（参见 `state` 主题的 [6.10.6 状态消息的实现](#6106-状态消息的实现)）。
其中可以包含以下信息：

- `headerId`
- 主题（`order` 或 `instantAction`）
- 如果错误由订单更新引起，则包含 `orderId` 和 `orderUpdateId`
- 如果错误由动作引起，则包含 `actionId`
- 如果错误由错误的动作参数引起，则包含参数列表

如果动作因外部因素无法完成（例如预期位置没有载荷），则应当关联 `actionId`。


## 7.2 参数格式

错误、信息和动作的参数设计为带有键值对的 JSON 对象数组。

| **字段** | **数据类型** | **描述** |
| --- | --- | --- |
| **actionParameter** { | JSON object | 指定动作的 `actionParameter`，例如 `deviceId`、`loadId`、`external triggers`。 |
| key | string | 参数的键。 |
| value<br> } | 以下之一：<br>array、<br>boolean、<br>number、<br>string、<br>object | 与该键对应的参数值。 |

动作 `someAction` 的 `actionParameter` 示例，其中包含 `stationType` 和 `loadType` 键值对：

    "actionParameters": [
      {"key": "stationType", "value": "floor"},
      {"key": "weight", "value": 8.5},
      {"key": "loadType", "value": "pallet_eu"}
    ]

采用 `"key": "actualKey", "value": "actualValue"` 结构是为了保持实现的通用性。`actualValue` 可以采用任何 JSON 数据类型，例如 `float`、`bool`，甚至对象。


# 8 术语表


## 8.1 定义

概念 | 描述
--- | ---
自由导航式移动机器人 | 使用地图自行规划路径的车辆。<br>车队控制只发送起点和目标坐标。<br>车辆将其路径发送给车队控制。<br>与车队控制的连接中断时，车辆能够继续行驶。<br>自由导航式车辆可以获准绕过局部障碍物。<br>车辆自身也可以对接收/交付位置进行精细调整。
导引式车辆（物理或虚拟） | 由车队控制向其发送路径的车辆。<br>路径计算在车队控制中进行。<br>与车队控制的通信中断时，车辆完成其已释放的节点和边（“基础区”），然后停车。<br>导引式车辆可以获准绕过局部障碍物。<br>车辆自身也可以对接收/交付位置进行精细调整。
中央地图 | 集中保存在车队控制中的地图。<br>最初创建后投入使用。
