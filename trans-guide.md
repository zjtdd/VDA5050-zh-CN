# VDA 5050 中文翻译规范

## 1. 文档说明

本文档用于指导 VDA 5050 英文规范（spec）的中文翻译、复核、版本同步和问题追踪。内容基于 V3.0.0 翻译阶段形成的 `translation-guide.md`，并结合当前仓库 V3.0.1 翻译工作的实际约束整理。

- **适用范围**：VDA 5050 V3.0.0、V3.0.1 中文翻译及后续同类版本维护。
- **英文基准**：[VDA5050_EN.md](./VDA5050_EN.md)。
- **中文目标**：[VDA5050_ZH.md](./VDA5050_ZH.md)。
- **术语字典**：[trans-dict.md](./trans-dict.md)。
- **翻译待办**：[trans-todo-list.md](./trans-todo-list.md)。
- **问题与处理方案**：[trans-issues-sols.md](./trans-issues-sols.md)。
- **对照检查结果**：[trans-check.md](./trans-check.md)。
- **整理日期**：2026-08-07。

翻译工作的首要目标是保持协议语义、数据模型和规范性强度准确，其次才是中文表达自然。不得为了行文顺畅而增删条件、改变主体、扩大或缩小适用范围，也不得翻译或擅自修正协议标识符。

## 2. 基本原则

### 2.1 准确性优先级

翻译和复核时按以下顺序保证内容质量：

1. 协议语义、状态转换和数据契约准确。
2. 规范性措辞的强度与 EN 一致。
3. 字段、主题、动作、枚举和类型的拼写准确。
4. 术语在全文及同类语境中一致。
5. 标题、链接、图表和 Markdown 结构有效。
6. 中文表达清晰、自然且便于技术人员理解。

若“表达自然”与“协议准确”发生冲突，应优先保留协议含义。无法确认时不得自行补充协议定义。

### 2.2 原文保护

- `VDA5050_EN.md` 是只读对照基准，任何情况下均不得修改。
- JSON Schema、英文流程图、UML 源文件及其他官方英文资源同样只用于核验；发现问题时只记录，不直接修正。
- EN 原文中的版本号、拼写、链接、表格、字段或语义问题，不得通过修改 EN 文件来掩盖。
- 中文翻译可以根据充分证据规避已确认的 EN 错误，但必须留下问题编号、核验依据、中文处理方式和官方处理状态。
- 依据不足或官方资料相互冲突时，应采用保守译法并标记“待确认”，不得自行创造字段、枚举、状态或流程规则。

## 3. 翻译基准与版本管理

### 3.1 资料优先级

同一版本的资料发生冲突时，按以下顺序核验：

1. VDA 官方发布的同版本规范文档。
2. 同一 Release 包内的 JSON Schema、模型图、流程图和其他配套文件。
3. 官方代码仓库中与该版本对应的 tag 或 release commit。
4. 尚未正式发布的开发分支，只能用于确认后续修正情况，不得直接覆盖当前版本的定义。

不得混用不同版本的正文和 Schema 得出结论。涉及差异或疑点时，应记录版本、分支或 tag、文件路径、章节或行号；必要时补充 commit 和文件哈希。

### 3.2 当前仓库的版本约束

- 当前翻译工作以当前分支的 `VDA5050_EN.md` 为内容基准，并使用 tag `3.0.0` 与当前分支的 diff 识别 V3.0.1 变化。
- `VDA5050_ZH.md` 不存在于 tag `3.0.0` 时，应将 EN 的版本差异逐项映射到中文稿，不能假定存在可直接套用的 ZH diff。
- 分支名、开发目录名或 Schema 变更不能单独证明文档版本号已经正式更新。
- 当前 EN 文档的标题和元信息仍标记为 V3.0.0 时，中文文档不得仅因分支目标是 V3.0.1 就擅自修改版本号；应等待官方基准明确更新。
- 翻译其他版本时，应重新核对对应版本的 EN、Schema 和字典适用版本，不能机械沿用 V3.0.1 的字段或规则。

## 4. 配套文档与追溯要求

| 文档 | 用途 | 更新时机 |
| --- | --- | --- |
| `trans-dict.md` | 维护统一术语、使用边界和适用版本。 | 新增术语、修改译法或版本适用范围时。 |
| `trans-todo-list.md` | 记录版本 diff、翻译任务、优先级、完成状态和待官方确认事项。 | 开始版本同步、任务状态变化或发现遗漏时。 |
| `trans-issues-sols.md` | 记录 EN、Schema、流程图、Markdown 及中英文一致性问题和处理措施。 | 发现、复核或关闭问题时。 |
| `trans-check.md` | 汇总当前 EN/ZH 对照检查结论和剩余风险。 | 完成一轮全文复核或问题状态显著变化时。 |
| `trans-guide.md` | 维护翻译、复核和验收规则。 | 工作流程或统一规范发生变化时。 |

问题记录应遵循以下要求：

- 翻译 V3.0.0 时发现的问题编号为 `EN-300-xx`。
- 翻译 V3.0.1 时发现的问题编号为 `EN-301-xx`。
- 同一问题跨版本延续时，应双向记录关联编号并说明是“延续”“部分修复”还是“同领域的不同问题”。
- ✅ 表示已确认后续版本完成修复；⚠️ 表示仍需关注或复核；❗ 表示未修复、仅部分修复或存在新增冲突。
- 行号会随文档变更而偏移，问题描述必须同时包含章节、字段名或上下文，不能只记录行号。
- 问题关闭时，应同步更新条目正文、汇总表、关联说明和统计结果，避免状态相互矛盾。

## 5. 语义翻译规则

### 5.1 保持信息边界

翻译每条协议规则前，应识别并逐项保留以下信息：

1. **主体**：车队控制、移动机器人、发送方、接收方或其他参与者。
2. **触发条件**：收到消息、进入区域、字段取特定值或状态发生变化。
3. **约束范围**：规则适用的主题、字段、对象、区域、节点、边或动作。
4. **行为**：发送、接收、停止、继续、拒绝、删除、保留或更新。
5. **完成条件**：流程何时结束、状态何时切换、对象何时移除。
6. **异常结果**：错误类型、错误级别、关联项、报告时长和恢复条件。

不得省略否定词、条件从句、时间关系、数量限制或 `only` 等限定词。不得将“预期行为”增强为强制要求，也不得将强制要求弱化为建议。

### 5.2 规范性措辞

| EN 表述 | ZH 默认表达 | 翻译要求 |
| --- | --- | --- |
| `shall` | 应；必须 | 表示规范要求。通常使用“应”；强调不可缺少的技术约束时可使用“必须”，同类规则应保持一致。 |
| `shall not` | 不得 | 不得弱化为“不应”。 |
| `must` | 必须 | 保持明确的强制性。 |
| `must not` | 不得 | 表示明确禁止。 |
| `should` | 应当；建议 | 表示推荐行为，不得翻译为“必须”。 |
| `should not` | 不应 | 表示不推荐，不得强化为“不得”。 |
| `may` | 可以；可 | 表示许可或允许。 |
| `can` | 可以；能够 | 按语境区分被允许执行和具备能力。 |
| `is expected to` | 预期……；预计…… | 表示预期，不翻译为“应”或“必须”。 |
| `only` | 仅；只有……才…… | 紧邻被限定对象，避免改变作用域。 |
| `unless` | 除非 | 保留例外条件，不得改写为相反逻辑。 |
| `until` | 直到 | 明确行为或状态的持续终点。 |

相同句式中同一规范词应采用一致译法。否定句优先使用“不得”“不应”，避免使用“不能”同时表达禁止和能力不足。

### 5.3 流程、状态与错误描述

流程段落和状态表优先按以下顺序组织：主体、触发条件或前置状态、执行行为、完成条件、失败结果和恢复方式。为改善中文理解可以拆句或调整语序，但不得改变事件先后、状态转换或因果关系。

同一状态表中的各行应尽量使用相同句式。原文提供错误类型和级别时，统一写为：

> 报告类型为 `'ERROR_TYPE'`、级别为 `'ERROR_LEVEL'` 的错误。

原文提供 `errorReferences` 或报告持续条件时，应继续说明错误关联项和报告时长，不能只保留错误名称。

## 6. 协议标识符与字面量

### 6.1 必须保持原文的内容

以下内容必须保持 EN 的原始拼写、大小写和单复数：

- JSON 字段名和对象名，如 `operatingMode`、`nodeStates`。
- MQTT 主题名，如 `order`、`instantActions`、`responses`。
- 动作名，如 `cancelOrder`、`downloadZoneSet`。
- 枚举值和错误类型，如 `'SEMIAUTOMATIC'`、`'INVALID_ORDER_ACTION'`。
- JSON 数据类型和协议类型，如 `string`、`boolean`、`float64`、`uint32`。
- URL、Schema 文件名、单位和版本格式占位符。

正文需要解释含义时，应采用“中文名称 + 原始标识符”，不得将中文写入协议标识符，也不得自行修改标识符的大小写、连字符或单复数。

### 6.2 Markdown 表示法

| 内容类型 | 统一格式 | 示例 |
| --- | --- | --- |
| 字段、对象、主题、动作 | 使用反引号 | `orderUpdateId`、`instantActions`、`cancelOrder` |
| 枚举值、错误类型、错误级别 | 使用单引号并保留原值 | `'AUTOMATIC'`、`'WARNING'` |
| `string` 的具体示例值 | 使用 JSON 双引号，并整体使用反引号 | `"order"`、`"1.3.2"` |
| 布尔字面量 | 使用小写并加反引号 | `true`、`false` |
| 数值 | 不加引号 | `0`、`1.0` |
| 数据类型 | 使用反引号 | `float64`、`array` |

枚举值保持单引号是当前规范正文的既有表示法，不应批量改为双引号。场景举例不是字段字面量时，不强制添加 JSON 双引号。

`version` 字段表示协议版本，示例写为 `"1.3.2"`，不要求以 `v` 开头。软件或硬件版本示例是否包含 `v` 应忠实于原文，例如 `"v1.12.4-beta"`。

### 6.3 对象结构表中的样式语义

对象结构表第一列的样式属于数据模型的一部分：

| 样式 | 含义 |
| --- | --- |
| 普通文本 | 基本数据类型的必填字段 |
| **粗体** | 非基本数据类型的必填字段 |
| *斜体* | 基本数据类型的可选字段 |
| ***粗斜体*** | 非基本数据类型的可选字段 |

不得将这些样式机械替换为反引号。对象层级、数组元素名、开始和结束标记必须与同版本正文及 Schema 核对。

### 6.4 当前版本的高风险标识符

- `responses` 是 MQTT 主题和消息名称；`response` 是单个响应对象，不能因中文不区分单复数而混用。
- `actionState` 是动作状态对象；`actionStatus` 是对象内的状态字段，两者不能互换。
- `actionResult` 是动作结果字段，不得沿用旧说明中的 `resultDescription`。
- `loads` 是状态消息中的载荷数组；独立数组元素仍可称为 `load`。
- `grantType` 是响应决定字段，不得写为 `responseType`。
- V3.0.1 的 `pick`/`drop` 动作参数使用 `loadHandlingDevice`；翻译 V3.0.0 时仍应按对应版本保留 `lhd`。
- EN 正文与 Schema 对字段或数组元素名称不一致时，不得静默选择并删除冲突记录，应按第 10 节流程核验和追踪。

## 7. 术语使用

翻译前必须查询 `trans-dict.md`。同一概念不得随意替换近义词；同一个英文词在不同协议语境下可能采用不同译法，应遵循字典中的“使用边界”和“适用版本”。

以下术语边界需要重点检查：

- `operating mode` 使用“操作模式”，不使用“运行模式”，避免与动作状态 `'RUNNING'` 混淆。
- `orientation` 使用“朝向”；`theta` 或弧度角数值使用“朝向角”；`direction` 使用“方向”。
- `load` 作为实体或能力时使用“载荷”；`loaded` 描述机器人状态时使用“载货”。
- `base` 和 `horizon` 使用“基础区”和“预测区”，不得与 `zone` 的“区域”混淆。
- `traverse` 描述机器人通过节点或边时使用“通过”“驶过”或“穿越”，避免正文使用偏算法语境的“遍历”。
- `error reference` 使用“错误关联项”，避免与文献“引用”混淆。
- `mobile robot` 默认使用“移动机器人”；只有在同一句或紧邻上下文不会产生歧义时才可简称“机器人”。

字典未覆盖的术语应先检索同一章节和同类对象的既有译法，再核对 EN、同版本 Schema 和上下文。确定新译法后，应更新字典并注明使用边界及适用版本。

## 8. 数学、坐标与数据格式

- 坐标轴统一写为小写 x/y/z，不使用 X/Y/Z 变体。
- 数学常量沿用 EN 的文本形式 `Pi`，不替换为 `π`。
- 数值范围统一写为 `[下限 ... 上限]`；开区间边界应忠实保留，例如 `]0.0 ... float64.max]`。
- 单位不得翻译为非标准缩写，字段表中的单位应与 EN 和同版本 Schema 一致。
- 布尔值只使用 `true`/`false`，不得写为 `'TRUE'`/`'FALSE'` 或中文“真/假”。
- 时间戳、URL、版本号和其他 `string` 示例按 JSON 字符串格式书写。
- ISO 8601、UTC、坐标系和角度单位等限制属于字段契约，不能因正文已有示例而省略。

## 9. 中文排版与 Markdown

### 9.1 空格和标点

- 中文与英文缩写、数字、协议标识符之间保留合理空格，例如“通过 TLS 保护”“字段 `orderId` 的值”。
- 中文标点前不留空格；中文句子使用全角逗号、句号、冒号和括号。
- 代码标识符内部不增加空格，不改变连字符、下划线或大小写。
- URL 后紧接中文标点时，标点不得放入 URL 或反引号内。
- 英文单复数属于协议标识符的一部分，不得因中文不区分单复数而改写。

### 9.2 标题、目录和链接

- 章节编号和标题层级应与 EN 对应。
- 目录链接必须与实际中文标题生成的 Markdown 锚点一致；修改标题后应同步更新目录和正文引用。
- 不使用空的 `<a></a>` 标签维护锚点。
- 链接使用单层 Markdown 括号，不得保留 `]((#anchor))` 等损坏格式。
- 表号变化后应全文检查所有正文交叉引用，不能只修改表题。

### 9.3 图、表和换行

- 图表编号统一写为“图 1”“表 1”，不使用“图1”“表格 1”等变体。
- 图题、图注和正文引用对同一对象使用相同术语。
- 图题和表题的分隔符应在全文保持一致。
- 同一 Markdown 表格块的首尾管道风格应一致；每个内容行的列数必须与表头一致。
- 空单元格也必须保留，避免后续内容移动到错误列。
- 表格内换行统一使用 `<br>`，不得使用 `</br>`、`<\\br>` 等错误标记。
- 流程图中的英文标识符和主题名也要参与一致性检查；发现共享图片问题时只记录，不修改官方英文资源。

## 10. EN 原文或共享资源问题的处理

发现疑似问题时，按以下顺序处理：

1. 确认 EN、Schema、流程图和其他依据属于同一版本。
2. 对照当前章节上下文、字段表、对象层级和同类定义。
3. 对照同版本 Release 包和对应 tag 或 release commit。
4. 必要时查看后续开发分支，只用于判断官方是否已经修复。
5. 在 `trans-issues-sols.md` 新建或更新问题条目，记录位置、问题、证据、中文处理和官方状态。
6. 在 `trans-todo-list.md` 记录仍需执行或确认的工作；完成全文复核后同步更新 `trans-check.md`。

中文稿的处理取决于证据强度：

- **已确认错误**：中文稿可采用有充分依据的正确语义，同时保留完整追溯记录。
- **资料冲突**：优先保持当前版本正文的可追溯表达，并明确记录正文与 Schema 或流程图的差异。
- **依据不足**：保留原译或采用不扩大含义的保守译法，标记“待确认”。
- **纯英文语言问题**：中文正常表达其可确认含义；只记录 EN 的拼写或语法问题，不在 EN 中修正。
- **Markdown 或表格问题**：中文稿应保证自身结构有效，同时记录 EN 问题，不复制损坏的链接、错位列或错误标签。

任何情况下都不得修改 `VDA5050_EN.md` 来完成问题闭环。

## 11. 翻译与复核流程

### 11.1 版本同步准备

1. 确认当前分支、目标版本、基准 tag 或 release commit。
2. 获取 EN 的版本 diff，并区分协议语义、字段契约、Schema、图示和纯排版变化。
3. 将 diff 逐项映射到 ZH，记录章节、影响和初始状态。
4. 按风险建立 TODO：P0 为协议语义和字段契约，P1 为章节层级、字面量和交叉引用，P2 为排版和语言校对。

### 11.2 单项翻译

1. 标出主体、条件、行为、完成条件、失败结果和规范性词汇。
2. 区分自然语言术语、协议标识符、枚举和字符串字面量。
3. 查询翻译字典，应用统一译法和版本边界。
4. 按中文技术文档习惯拆分长句，但逐项核对信息没有增减。
5. 核对字段名、主题名、动作名、枚举、类型、单位和示例值格式。
6. 核对标题锚点、内部链接、图片引用、表格列数和对象样式。
7. 涉及 EN 或共享资源问题时，同步更新问题追溯记录。
8. 完成一项后立即更新 TODO 状态，不应等全部完成后一次性勾选。

### 11.3 全文复核

- **语义复核**：主体、条件、否定、时序、范围、错误和恢复规则与 EN 一致。
- **契约复核**：字段、主题、动作、枚举、类型、可选性、单位及对象层级与当前版本一致。
- **术语复核**：全文检索核心术语、禁用译法和易混淆标识符。
- **结构复核**：比较 EN/ZH 标题层级、图片数量、代码围栏、表格结构和内部链接。
- **问题复核**：确认问题正文、汇总表、关联编号、状态标记和统计结果一致。
- **变更复核**：确认修改范围仅包含获准编辑的中文及翻译维护文档。

### 11.4 验收命令

根据实际变更至少执行以下检查：

```bash
git diff --check
git diff -- VDA5050_EN.md
rg -n '<a>|</a>|</br>|<\\\\br>|\]\(\(#' VDA5050_ZH.md
```

其中 `git diff -- VDA5050_EN.md` 应无输出。若需要比较所有未提交变更与当前提交，应使用 `git diff HEAD -- VDA5050_EN.md`。字段残留、标题数量、图片数量、代码围栏和表格列数检查应根据本轮 TODO 补充执行。

## 12. 提交前检查清单

- [ ] EN、Schema 和其他核验资料属于同一版本，版本来源可追溯。
- [ ] 主体、条件、否定、数量、时序和限定范围与 EN 一致。
- [ ] `shall`、`should`、`may`、`is expected to` 和 `only` 的强度未改变。
- [ ] 字段、主题、动作和枚举未被翻译或改变拼写。
- [ ] `string` 示例使用 `"..."`，枚举使用 `'...'`，布尔值使用 `true`/`false`。
- [ ] 核心术语符合 `trans-dict.md`，且没有混淆语境边界或版本边界。
- [ ] x/y/z、`Pi`、数值范围、单位、时间戳和版本示例格式正确。
- [ ] 图表编号、标题锚点、内部链接和图片引用有效。
- [ ] 表格行列数一致，对象层级和粗体/斜体语义未被破坏。
- [ ] EN 原文或共享资源疑点已核验并留下可追溯记录。
- [ ] TODO、问题汇总、关联编号和对照检查结论已同步更新。
- [ ] `git diff --check` 通过。
- [ ] `git diff HEAD -- VDA5050_EN.md` 无输出，确认 EN 原文未被修改。
